*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal
__pycache__/
*.py[cod]
.coverage
.coverage.*
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
# The worktree's .venv is a symlink to the main checkout's, and a
# symlink is a file: the trailing-slash pattern above does not match
# it, so it was committed once. This line is the one that catches it.
.venv
venv/
build/
dist/
*.egg-info/
.DS_Store
.idea/
.vscode/
.env
.env.*
!.env.example

# Runtime authority and imported research data never belong in source control.
.dalton/
data/
state/
temp/
legacy-artifacts/
