# Conviction prompt and content retry review

The 19:20 UTC read-only live inspection found two distinct Conviction failures: an older draft exceeded the 300-character signal bound, and a newer independently verified draft was rejected for an unsupported market-view citation. The latest repeated ticket reports zero incremental cost; repeated task launch is established, repeated vendor billing is not.

The draft previously omitted its parser's text/count limits and explicitly said agreement with a bullish market was worthless. Task `task:conviction-call-draft:0.2` states the existing bounds, asks for a concise observable signal and distinguishes shared direction from differences in magnitude, timing, probability and valuation. It tells the model to choose its current judgement and its falsifying condition without inventing contrarianism. The verifier applies the same distinction and receives its detail limit. Parser/evidence requirements have not been weakened or text silently truncated.

The new task hash participates in both child identity and the coordinator hold key. A completed verifier rejection, mechanical rubric failure or explicit missing market view is content-terminal for the same evidence and task contract. New evidence or a revised task can be considered normally; model/transport unavailability keeps its existing recovery classification. Published proposals remain deduplicated by their existing evidence identity.

Validation: 160 Conviction draft, authority, lane and Cockpit tests passed in 4.732 seconds. Tests cover the actual signal character boundary, the revised instruction, a verifier content hold and a new task releasing that hold. This change is integrated for the next foundation/model release and is not in deployed R14a or frozen R14b.

## Remaining method and data boundaries

The existing versioned Conviction policy still requires a demonstrated pricing difference and contains historical multi-year risk/reward categories. This prompt correction does not migrate that policy, manufacture current price/buy-side expectations or implement the owner's full probability/payoff method. Those remain explicit follow-up work after financial and source foundations. The current event-judgement prompt already states its 1,200-character bound; old held failures are not evidence that a fresh prompt repair has failed. The latest event run is waiting for enough unreserved capacity in its configured event-response budget pool, a separate condition.
