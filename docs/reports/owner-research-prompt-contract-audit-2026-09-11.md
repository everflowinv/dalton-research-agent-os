# Owner research prompt-contract audit — 2026-09-11

This review follows each model call from its admitted context through prompt, deterministic validation, and replay identity. It does not change any signed investment threshold or human gate.

| Call | Actual context | Prompt / verifier | Identity and finding |
|---|---|---|---|
| Earnings preview | forecast, consensus when present, guidance, watch rows, theses, cited claims | Producer asks watch/confirm/break signals; independent verifier checks support, period, date caveat, and numbers | Caller request identity is external. The present shape can express tracked observations but has no closed primary-case or return-source fields. A prompt-only A/B requirement would be unverifiable and was not added. |
| Earnings calibration | reconciliation, guidance, consensus, theses, market-view rows, sources and claims | Producer returns thesis decisions and structured `followup_tracking`; verifier checks the same context | Tracking is real only when the downstream tracking consumer persists these rows. Source rows currently do not carry a closed sell-side/desk/crowd/buy-side taxonomy, so the prompt must not infer buy-side positioning. |
| Zero-base review | bounded current theses/debates/events/claims, latest calibration and prior review | Four-answer producer plus independent verifier; output has one `next_verification` | It can persist one observation/date, but cannot encode both operating/earnings/valuation consequences for A and B. Existing human thesis-revision authority remains the gate. |
| Conviction call | active theses, debates, forecast-consensus gaps, catalysts, dossier variant slots, signed standards | v1 producer and verifier require a sourced market disagreement and `where_market_is_wrong` | **Policy migration required.** `conviction-policy:p15d:v1`, `precheck`, draft validator, verifier, proposal authority, lane identity, and deployed policy bytes all require divergence. Relaxing only the prompt would corrupt policy meaning. A v2 must separately represent current state/assumptions, what is priced in, upside/downside outcomes, decision conditions, and tracked observations; it may admit agreement when payoff is attractive. Its probabilities, evidence confidence, and investment attractiveness must remain distinct. It requires owner signature before activation. |
| Thesis impact | exact immutable ClaimVersion and ThesisVersion, optional formal forecast reconciliation | Closed supports/weakens/no_change/insufficient assessment plus independent verifier | Correctly evaluates evidence against a long-lived mechanism and does not require contrarianism. It is an evidence-impact primitive, not a return decision, so adding payoff thresholds here would mix authorities. |
| Transcript polish | exact transcript chunks | Normalizes text under source/citation validation | No investment judgment or source-position inference; no change indicated. |
| Street estimate extraction | source governance has already classified an eligible sell-side note | Extracts broker estimate table; deterministic parser and source checks | Correctly stays sell-side-specific. It must not be broadened to desk, crowd, or buy-side material. |
| Document numeric extraction | filing/transcript/research window plus requested numeric slots | Extracts cited values; digits/labels are checked | Source-neutral and makes no market-position claim; no change indicated. |
| Metric discovery extraction | eligible document window and quotes, but no author-category field | Old prompt asserted the window was sell-side research or a company release | Fixed as task v0.2: prompt names an eligible evidence source, states that sell-side, desk, crowd, management and buy-side are distinct, and refuses to infer an unavailable category. `TASK_HASH` changes, so old work remains immutable and the corrected contract gets a new identity. |

## Next bounded foundation slice

Version Conviction end to end rather than editing v1 in place: add an unsigned v2 policy candidate and closed schema, validate it without making it current, bind its hash through precheck/draft/verifier/proposal/lane, and require the existing human policy publication authority to activate it. The schema should require a clear current decision and conditions, while allowing one return source to be sufficient and preserving long-term business/moat evidence. A separate tracking authority must persist the named condition/observation; prose in a prompt is not evidence that tracking exists.
