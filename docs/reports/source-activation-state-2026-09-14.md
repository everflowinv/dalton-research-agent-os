# 数据来源启用状态（2026-09-14）

本记录区分已经写入运行权威的数据治理变更，与仍需 R25e 部署后才会生效的运行修复。

## 已生效

- 覆盖任务 v15 已从 v14 正常追加。每日总预算为 135 美元，资金池分别为覆盖研究 55 美元、事件研判 50 美元、临时任务 25 美元、维护 5 美元；原有当日支出没有重置。任务内容哈希为 `7934e4837eeaa85ec26bf2099bf8bd05ac33bde7d725269d5c6e32ad82b5b12d`。
- yfinance 的每日价格和日历读取、卖方销售快报的列表和单条读取，共四项已有连接器权限，已按所有者指令由 `human:lumos` 通过正常治理接口批准。执行收据 SHA-256 为 `bdade3f65de7dc7406ec021d3da3d255623d72f0558c4cc1645cec80fd17360d`。
- 覆盖任务 v16 已从 v15 正常追加，预算原样保留；Company Wiki 标记为已连接，AlphaEngine 与销售快报的角色说明改为自然中文。任务内容哈希为 `089586ac0e315f074834895fa29c069803ccc9dee1117a0b4dbbaf6fbd15a491`。
- Company Wiki 的列表和读取权限已批准；资料入口指向现有 OpenClaw 工作区，实际只读列举和读取各成功一次，没有重建向量库，也没有模型调用。收据 SHA-256 为 `9c242352b374cd50bff4e272da7a0d9b23363825f223a18d2eeab3d9d56cd35d`。
- 当前所需 SEC Form 4 交易读取权限已批准，收据 SHA-256 为 `0d4a14554caa226fae652ae4b519ab63a30208e8e45dd27242f79e228da5330d`。未顺带批准无关的受益所有权能力。
- 文档提取配置通过精确 CAS 从路由政策 13 更新到已存在的政策 14；仅 `routing_policy_ref` 变化，零模型调用。配置 SHA-256 从 `256dac3c96213b63f0ebfa6b8f0894bc8be7faf3508f22163f0fbf0cbc87eb28` 变为 `ef2208959472f0c539eb203462ff394b8e988cd82a2765168d4c9315c53364de`。

## 待 R25e 部署后验证

- 当前 R25d 写入进程在 Company Wiki 激活前已创建，因此其旧心跳仍可能显示该通道未配置。R25e 的正常重启需要重新装载通道，并以真实只读读取验证运行状态。
- 销售快报一次完整读取超过旧的 60 秒客户端等待时间，但后台随后成功完成 50 条读取。R25e 候选让客户端采用已配置的 180 秒超时，并把没有规划调用的预算状态显示为“不适用”。这些是代码候选，尚未部署。
- AlphaEngine 的失败分页冷却修复也在 R25e 候选中，尚未部署；事实与影响见同日 AlphaEngine 审计更新。

