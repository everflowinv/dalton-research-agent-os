# Foundation execution and reading closure — 2026-09-11

This register preserves the ongoing foundation/Dossier/deployment work and the owner’s new requirements. A source commit or passing unit test does not by itself close a row; actual runtime acceptance is recorded separately.

The following table preserves the original intake baseline; the dated checkpoints below record subsequent deployments and acceptance results.

| Work item | Evidence and state at intake | Completion criterion / next action at intake |
| --- | --- | --- |
| Historical stage vs current source/readiness | API/UI separation merged; acquired/read counts displayed separately | Deploy and check actual five-company page |
| Missing sources classified unplanned by writer | Wrong coordinator.load_plan call repaired; actual socket test passed | Actual stage tick must show configured source gaps |
| Shortfall retry regressed during pagination | Restored configured short cadence, cursor and open-child rules preserved | Same-freeze tests plus real scheduling evidence |
| Planner STOP ignored by acquisition | STOP now constrains fresh/retry selection, exact mission-scoped, rows retained | Confirm surplus web queues stop while other work remains eligible |
| Repeated terminal URL failure | Typed retryability persisted; unknown historical failures remain unknown | Explicit false must not launch again; no broad prose heuristics |
| Repeated failing hosts | Configurable 0.5 plan; guarded append-only attempts; exact source/host cooldown with success reset | Review/activate private 3-URL/24-hour/6-hour candidate, verify cooldown/status and recovery |
| Annual read falsely completed after model failure | ACN 33/33 failed; CTSH 27/32 failed; DXC 38/39 failed, historical Sep10 22:22–22:46 UTC | Newly generated AND cached failures keep review incomplete; 11 focused tests passed, integrated acceptance pending |
| Proof of successful whole-document reading | Formal per-window proof, transaction CAS, CLI publication and full schema install merged; truncated source refused | Deploy and confirm legacy unknown stays unread and a fresh full successful pass becomes read |
| Historical incorrect dismissals | Review ledger lacks resolver identity; cannot infer it from registered_by | Prepare exact review/hash/failure evidence and bounded supplemental repair approval; preserve history and human decisions |
| Capacity and queue | Historical BUSY was broker concurrency limit; current cap8, owner now authorizes higher | Configurable bounded FIFO, duplicate joining, queue timeout before invocation, reviewed cap16 candidate |
| Retry then fallback | Existing capacity retry and chain logic require audit against owner sequence | Transient failure retries same provider before fallback; unknown completion cannot be blindly repeated; exhausted work stays pending/blocked |
| Token ceiling/timeout | Some requests estimated16038 vs work limit16000; no model invoked; extraction config has no explicit call overrides | Exact prompt-budget accounting/window fit, configurable larger reviewed time/token limits without overriding daily authority |
| Directed vs broad reading | Existing annual pass enumerates windows; research-task probes exist | Verify general read coverage AND question-driven local annual/web retrieval, including revenue recognition question; implement missing executor route |
| Hardcoded limits | Source inventory799 matching lines,15 modules use common call/run resolvers | Review all budget/concurrency/queue/retry/timeout/window/cadence defaults and actual runtime overrides; fix unreasonable/disconnected constraints |
| Configurable tracking execution | Installed cadence, write cap, all five candidate scan caps and lookback now consumed; 70 tests passed | Deploy; existing policy hash/defaults preserved; live configuration unchanged |
| Configurable document reading | Source length/PDF pages/window/quote bounds now reach source verification, context and Cockpit; 56 tests passed | Explicit reviewed release config; full-text reread must retain same bound limits and hash |
| Every model prompt and analyst expectations | New owner requirement: inspect producer, context, verifier and semantic identity together | Root handles extraction/ask/debate/events; Sol agents handle foundations/deliverables, planner/quality, and forecasts/earnings/conviction; complete matrix and fix stale assumptions |
| Decisive, testable views and tracking | Owner clarified: understand market thinking and anticipate its next move, rather than force disagreement | State main judgement, conditional A/B triggers and consequences, what changes the judgement, and actual tracking observations; no uncommitted “both possible” prose or fictitious scheduling |
| Probability and payoff | Owner clarified evidence confidence, scenario probability and price-implied payoff are distinct; low-confidence upside may still be attractive when downside is priced | Audit actual decision structures and tracking consumers; no mandatory disagreement/high-confidence gate, no blanket prompt template; foundational work remains first |
| SEC annual index setup | Existing v1 quota50 conflicts with current template200 under same immutable key | Reuse only exact validated stricter existing authority, preserve spend; refuse other drift; no silent budget increase |
| Dossier variant/0.3 product | Variant prompt fix merged; live R8a remains no_v03_records | Same-freeze release, actual new formal producer/verification/provenance and HTML checks |
| Deploy and sustained health | R8a health45/45 passed; next freeze not yet accepted | Full Python/JS tests, wheel bytes, copied-state rehearsal, config preservation, deployed bytes and sustained runtime/product evidence |

The owner explicitly asked to finish these tasks along with prior ongoing work, audit unreasonable hardcoded limits, retain progress/next steps, commit/push, and deploy autonomously. Model and budget choices must remain cockpit/config driven. Human research signatures remain human. Advanced investment-methodology work, final visual redesign, reference-file import/export polish, and multi-workspace activation remain sequenced after foundations; these are not removed from the broader backlog.

## Integration checkpoint — 05:12 UTC

The first R9 freeze is `050a044c18570431bdd7bbf782cd44ff5ef94596`. Its full suite is diagnostic and still running; failures mean it is not accepted. Wheel/source equality passed for 561 runtime files (400 Python, 68 SQL, 90 JSON, 3 HTML) and three JavaScript syntax checks. Root subsequently fixed preservation of explicit reading limits across installation (`10340e8`); this requires a new freeze and fresh acceptance, not reuse of the old suite result.

Transport scope remains explicit: extraction and Cockpit support the reviewed queue/retry contract; other direct production adapter entry points need their own configuration, identity and lease review. Agenda/Human Intent/Thesis Impact/Planner gaps predate R9; different immutable Scheduler policies can coexist in one database. A Sol agent is implementing the Agenda slice first.

The annual directed probe currently proves retrieval into a governed backlog question only. A new independent Sol task is implementing the versioned qualitative ResearchPlan contract and registered-filing retrieval node; model drafting, independent verification and candidate staging must follow through real formal authority.

Conviction v2 candidate code remains under review and is not activated or accepted. Initial review found incorrect omission of earnings-expectation revisions from return sources, incomplete prompt/schema and verifier identities, and insufficient formal governance/provenance/ongoing-tracking admission. These are engineering work to complete before presenting any signature request. The owner explicitly requested methodology to strengthen decisions rather than a universal prompt checklist.

R9 diagnostic suite completed: 6,944 tests in 582.034 seconds, 6 failures, 1 error, 1 skip. Closure: update obsolete prompt/default-budget assertions without weakening bounds; build the oversized preflight input from the actual WorkOrder limit; bind the Guidepoint test fixture to its real excerpt hash; add the missing document-read-proof MigrationSpec and its connection constructor. The 170-test affected suite passed; six migration tests additionally prove the real proof table is created on the shared connection. No failed R9 acceptance has been promoted.

## R9a deployment checkpoint — 05:40 UTC

The unchanged R9a source `b6cbddc1867fd1853f4870a04a777d2f79e1b869` passed 6,946 Python tests in 601.083 seconds (one skip), 29 actual Node broker tests, wheel/source comparison for 561 runtime files, and a copied-live-state rehearsal with 68 schemas, 40 tick entries and zero escaped calls. The accepted wheel SHA-256 is `6f3e21b0a27642a8330b4c18a019e7fcf6afed31c0e5cf9273acd6ad16116319`. The earlier failed R9 result remains separate.

Core deployment finished successfully at 05:32:23 UTC. All 561 installed runtime files matched the accepted wheel and retained source archive. All twelve complete model configuration files matched their predeployment contents. Backup `deploy-backup-20260911T052940Z` and retained source `.release-source.MsUxXg` are preserved in the private owner activation packet. The original main workspace's unrelated DeepSeek diff remains untouched.

A second bounded maintenance window drained in-flight tasks, applied the reviewed document transport settings and broker queue, and restored writer/control/controller. It finished successfully at 05:40:00 UTC. Broker settings are now concurrent 16, queued 64, maximum queue wait 600,000 ms. Document transport adds one definitely-not-sent retry, 600 seconds queue wait and two seconds backoff. This is not evidence that all provider-failure retry paths are complete. New controller PID 96478 started at 05:38:21 UTC; sustained 45-sample health observation began at 05:40:20 UTC and remains pending at this checkpoint.

Private evidence is under `/Users/everflow/Projects/dalton-owner-activation-20260910`: `foundation-followup-r9a-deploy-execution.json`, `foundation-followup-r9a-installed-bytes-verification.json`, `foundation-followup-r9a-config-preservation-verification.json`, `foundation-followup-r9a-activation-execution.json`, and `foundation-followup-r9a-runtime-activation.json`. Live health and actual new Dossier 0.3 product provenance are separate acceptance rows; neither is inferred from installer exit zero.

Next foundation work remains assigned in parallel: (1) returned provider failure proof, independently budgeted same-profile retry and fallback through real workers; (2) Agenda/Human Intent transport configuration and lease consumers, with generic failure envelopes kept conservative until the shared proof exists; (3) registered annual filing retrieval followed by real qualitative drafting, independent verification and candidate staging. Research signatures and historical human/unknown dismissals are not rewritten under operational deployment authorization.

The owner's probability/payoff clarification remains a decision-system requirement, not a universal prompt template. Scenario probability, confidence in evidence, conditional returns and price-implied assumptions must remain distinct. A low-probability but materially underpriced upside can justify a positive recommendation; neither consensus agreement nor modest confidence is an automatic rejection. The eventual decision must explain downside, valuation/earnings/revision assumptions, what would change the judgement and actual observations that update it. Foundation execution is still first; the unaccepted Conviction v2 candidate is not activated.

### Actual Dossier acceptance finding

The R9a post-activation read-only check still found zero Dossier 0.3 records (`dossier-v03-acceptance/r9a-post-activation.json`, SHA-256 `2e074ed9f302c6eb1ff159b1a0d4fcb85796e493862811d3fde95d602f6b4f4c`). The first ACN run after activation (`e82d8c3ac2547bc071d4b1b8`, 05:38:52 UTC) produced three units and received an independent pass, but correctly refused publication with `unit_provenance.business_model.producer authority binding drifted`.

Root traced this to a real caller/validator mismatch: Cockpit bound the mission content hash only for memo purposes, while Dossier 0.3 requires it for both producer and verifier. A new integration fix adds those purpose bindings and therefore gives corrected work a new identity instead of rewriting or borrowing old paid results. A second mismatch appeared only when exercising the actual Router/Scheduler: the verifier request includes a deterministic producer-route suffix, while the Dossier proof stores its semantic request ID. Validation now accepts the exact suffix derived from the formally bound producer routes, preserving legacy unsuffixed records and all other provenance checks.

The regression calls the actual dossier draft and verifier functions through Cockpit, Scheduler, Router and budget store with a fake provider, then invokes the same formal provenance validator used for publication. It checks replay and changed mission-hash isolation. The affected 203-test suite passed. This repair is integration code, not part of installed R9a; deployment and actual new product acceptance remain next steps. Explicit transport/capacity recovery request decorations also need coverage before enabling those options on the Dossier-specific configuration.

### Runtime observation and selected web plan

R9a's first sustained observation completed 45 samples over 671.055 seconds. One sample at 05:49:29 UTC was degraded with `OperationalError: database is locked`; the controller stayed alive with a fresh heartbeat and recovered by the next sample. The last successful sweep timestamp preceded the failed tick. The observation is **not accepted** and its raw evidence remains intact. Root is investigating unnecessary Scheduler sweep write-lock acquisition; no failed sample is removed or relabeled.

The failure-host cooldown candidate also exposed an installation boundary: LaunchAgent rendering still selected the fixed web v3 filename. Integration now supports a hash-bound `web-search-plan-selection-v1.json` alongside the existing SEC selector, so a separately retained web plan can be selected without rewriting historical plan files or resetting to v3 after regeneration. Missing selection preserves the previous default; proposed, source-mismatched, hash-drifted or escaped selections fail before plist publication. Seventeen web/SEC selection tests passed. The private cooldown candidate is not activated yet; activation must bind the exact new plan/selector and leave source permissions and budgets unchanged.

The completed health summary hash is `3457f8764737d88b6841a61ebe6350bca5248fc4b278164c8d302ab64dc5f395`; it is retained as failed acceptance. Source review found that every sweep first acquired SQLite's write lock even when no lease was due. Integration now performs an advisory read first, and keeps the original transaction plus a fresh lease-state check whenever expiration may be necessary. A real WAL lock-contention test proves an idle sweep can finish while another writer holds the lock; a due sweep still refuses contention and later expires exactly once. A competing-sweeper test proves stale advisory reads do not duplicate expiry. Seventy-one Scheduler/service tests passed. This removes a concrete contention source; it does not claim to prove every possible lock holder or that live acceptance has passed.

### First actual complete annual-read proof

EPAM `sec:filing:0001352010-26-000015` produced `document-read-proof:221bd4cfd1dd0eaa5805a28fcc57955b` at 05:47:13 UTC. Its 35 successful windows continuously cover 419,086 characters and end at the complete, non-truncated source boundary. Root's read-only audit rehashed the saved proof, source review, every frozen context and WorkOrder, and every successful formal Scheduler envelope; all window refs, source hashes, offsets, company/document bindings and the final boundary matched.

The proof hash is `40ecd9c7a72ff03301abda00eda4aed9d17dd7b11b8bd167e0b0d8aefb5bc467`; the source content hash is `b7a7cb7a2d7169f740d32f2b712158f731401b1a33c2ac15cd57af3f04690f0e`. Private receipt `foundation-followup-r9a-document-read-proof-audit.json` has SHA-256 `b87bb7656f2cec76f8a1d680c49a4db2779c732108e5595deb20634ba88c45b0`. This is actual broad-reading execution evidence, not an independent rating of the extracted prose and not proof that the other companies' historical dismissed reviews have been reopened.

### Model-spec prompt scope correction

The live observation also showed IBM and CTSH model-spec drafts refused because `horizon.because` exceeded 400 characters. The prompt already included the complete schema, but a recent instruction demanded base case, alternative trigger, impact, falsifier and next observation for each model decision. Root narrowed that instruction to the actual consumer: specific reasoning for each model line/horizon, and the key structural uncertainty in the overall assessment. Field-length instructions derive from the existing output schema; investment scenarios and tracking belong to their downstream consumers. No text is silently truncated and no validator is weakened. Prompt contract 0.4 changes TASK_HASH so prior refusals do not suppress a corrected attempt. Forty-nine model-spec/lane tests passed; actual regenerated outputs remain to be assessed after deployment.


## Telephone-call inventory and historical extraction display — 06:15 UTC follow-up

Owner reported that the Cockpit appeared to have lost every earnings call. A fresh read-only Core backup audit found current valid-quarter inventory ACN 0, CTSH 1, EPAM 2, IBM 0, DXC 4. These acquisition counts match the earlier 03:50 audit. Whole-document `read` is now zero for these historical calls because the new completion contract requires formal window proofs; legacy `extraction_staged` records are still present and were not deleted. The UI had not explained that distinction.

The checklist now exposes `legacy_extracted` separately and labels it “历史已提取（全文待核验）”; it keeps whole-document `read` unchanged. Issuer attribution, consecutive-quarter coverage, current rejection state and per-period deduplication still apply. A newly verified copy of a quarter supersedes that quarter's legacy display without double counting. This does not create proofs, reopen decisions or issue paid calls.

Current live-copy comparison preserves every existing checklist field while showing CTSH 1, EPAM 2 and DXC 4 historical extracted quarters. Private audit receipts: `stage-checklist-audit-20260911Tcurrent.json` SHA-256 `6e5debba33e894108c486098e118385d600c7a830ff849929a781d00bd6ccfe6`; candidate `stage-checklist-audit-20260911Tcandidate.json` SHA-256 `eabc8bf9ff477ffed2913d36a5160f2092be94c6b004aeb681b12d930b57cd15`. Five test modules pass 79 tests / 1 skip; embedded Cockpit JavaScript syntax passes. An earlier relative-PYTHONPATH run failed a subprocess fixture; the same test and full focused run pass with the release-standard absolute PYTHONPATH.

ACN's discovered/acquired rows contain no Accenture quarterly call; IBM's correctly named rows are investor conferences rather than quarterly earnings calls. Historical broad searches also returned many unrelated companies. A separate Sol task is closing missing-quarter selection, search parameters and pagination. Owner explicitly requires relevance assessment before full-text acquisition: use title, snippet, dates, type, tags and the actual research gap; do not make company tags an absolute filter, and retain industry/peer research for its appropriate purpose. LLM selection must consume existing routing, budget and formal work contracts. No new selector or source-plan activation is claimed yet.
## Dossier request-decoration candidate

The follow-up candidate binds an explicitly configured Dossier producer or verifier request to a closed, versioned identity containing the semantic request, the exact transport/capacity retry configuration, the exact producer route references and the immediate recovery parent. Recovery parents carry the authoritative prior WorkOrder and result-envelope hash; arbitrary suffixes are not accepted as proof. Historical successful work remains replayable only under its exact old identity, while historical failures without the new binding do not acquire fabricated recovery authority.

The regression exercises `draft_unit` through a capacity failure and cooldown recovery, then `verify`, Cockpit, Scheduler, Router and the publication-time `validate_formal_unit_provenance` against persisted authorities. It also checks producer-route binding, exact retry configuration, replay and changed mission-hash isolation. This remains a reviewed integration candidate until the full affected suite and release acceptance complete; it has not been deployed and is not live Dossier-product evidence.

Agenda and Human Intent transport work also remains conditional on the shared adapter proof repair. Their real Unix-broker regression covered queued success, while capacity tests supplied a synthetic not-sent proof; the installed adapter does not yet author that proof for a real local BUSY refusal. Live BUSY therefore remains conservatively terminal until the adapter emits a validated local-not-sent proof and a real worker BUSY regression passes. Those entries must not be described as fully closed before that acceptance.

The Dossier review hardening follows every recovery parent to its root and verifies the immutable WorkOrder, result receipt, ResultEnvelope, semantic request, prompt, mission, retry configuration and producer routes at each edge. Route admission resolves its rejected Router decision and changed-policy proof; operator recovery resolves its controlled-redrive record; capacity recovery requires adapter-authored definitely-not-sent proof, the configured cooldown, the capacity-policy hash and a contiguous epoch sequence across intervening route/operator recoveries. A pre-binding root is allowed only for the exact default configuration and exact historical semantic/producer identity. The default live configuration path now has a regression covering all three default Scheduler attempts, cooldown recovery, drafting, independent verification, replay and publication-time formal provenance validation. Historical successes are replayed only after the persisted WorkOrder, formal record, ResultEnvelope and result receipt validate exactly; pre-binding configured records that lack enough metadata for final provenance are not borrowed. The affected 236-test suite passes in the candidate worktree. This remains unmerged and undeployed at this checkpoint.

### R9b acceptance failure and R9c preparation — 06:45 UTC

Frozen R9b `e68171f985baab8c6f684d2d667cddf82b6a8184` was not staged or deployed. Its full suite ran 6,960 tests in 576.223 seconds and failed with two assertion failures and five errors (one skip). The immutable failed log SHA-256 is `a5f182c6c1091360c02efdc3997a625e14fcc9ad0aa24ca86e4d1f893fdf2128`. Wheel byte verification (561 files and three JavaScript checks) and copied-state rehearsal (68 schemas, 40 entries, zero escaped calls) passed for that source; they do not override failed full-suite acceptance.

Six failures came from the old hand-built Dossier provenance fixture: it put producer-route metadata on verifier work but omitted the producer-route suffix that real Cockpit calls persist. The fixture now retains a semantic provenance request ID and generates the exact persisted verifier request ID, as production does. A tampered producer list is rejected at that stricter request binding. This does not broaden production acceptance or invent legacy authority. The remaining assertion still demanded generic investment scenarios in the model-specification prompt; it now checks the concrete model-structure judgement and observable structure-change condition, consistent with that output schema and its downstream consumers. All 16 tests in both affected modules passed.

Next: freeze a new R9c commit, repeat full-suite/wheel/rehearsal against that immutable source, and use the reviewed core-only deployment preserving the already installed 16/64/600,000-ms broker queue and all twelve model JSON configurations. Keep both R9a failed sustained-health evidence and R9b failed full-suite evidence explicitly bound in the next review. Actual Dossier 0.3 publication and continuous healthy runtime remain unaccepted until measured after deployment. AlphaEngine asynchronous search selection and directed annual-report four-node execution continue in separate worktrees.

### Budget counter and failed-host activation preparation

A further hidden listing limit was found outside the R9c freeze: source-call reservations counted only the first 100 open discovery dispatches and the first 100 open document acquisitions. Increasing configured budgets/concurrency could therefore undercount pending calls. Integration now uses one source-filtered SQL count over both complete pending sets, including children launched under superseded missions, instead of treating a paginated display API as accounting. A regression creates 105 real governed dispatch records, verifies all 105 reservations reduce the configured budget, checks source isolation and settlement release.

The private failed-host preactivation audit at 06:45:46 UTC found one qualifying host: `www.investing.com`, three distinct terminal-failure URLs, cooldown until 12:08:56 UTC under the proposed six-hour rule. Two retryable failures and three unknown failures are excluded from this terminal-failure rule. Candidate selection preserves the prior plan's search specs, source scope, static host lists and 1,000-call rolling budget. The immutable candidate/selector are prepared but **not activated**; do not report the currently installed coordinator as already enforcing cooldown. Audit SHA-256: `00850bc755c91cfea0c2e0686788b65b7b2f12aa7512db89ad67382fbaaa4965`. Next: finish R9c sustained runtime acceptance, then apply the bounded plan selection and verify actual coordinator exclusion/recovery.

### R9c installed, sustained health pending — 07:01 UTC

Freeze `c29b3e22808cb900b458eeb00d9e5447b72f5db6` passed all 6,960 tests in 568.156 seconds (one skip); full log SHA-256 `103c849b4d852b8b79a823a1eaaecad2729783a164e618a336c3c811eb762481`. Wheel SHA-256 `3321885f6800e7c065a1fdc37b888e9eb3bc8204c46ffd44742d8f611ad94412`; copied-state rehearsal binding SHA-256 `830cf0b0da316779f57bcfe367c9b3cbc2a8ca4cf70eea60a3f394b44f58cc27` (68 schemas, 40 entries, zero escaped calls). R9b remains a failed release candidate.

The reviewed core-only deployment ran 06:54:35–06:59:07 UTC, drained in 81.612 seconds and exited zero. Retained backup: `deploy-backup-20260911T065436Z`; exact source archive: `.release-source.JIUVf9`. All 1,887 archived tracked files and 561 runtime files matched the accepted commit/wheel. The twelve complete model configurations, current 16/64/600,000-ms broker queue, broker plugin bytes and historical unresolved invocation were preserved. Installed byte receipt SHA-256 `de2d21f7cb7cc79d126cf7116d6115ceca6c1b9ab0b47266b1c25a04d9b4b5a4`; configuration receipt SHA-256 `5616865c15f4118ca09feb8a27f6c9e5a9f87e8f73a9d2c8ea9fb54f7243c09a`. New controller PID 18253 was born at 06:56:41.033839 UTC. The 45-sample observation began 06:59:58.970062 UTC; runtime and actual Dossier product acceptance are still pending.

Outside this freeze, integration now contains asynchronous AlphaEngine candidate selection through real child/Unix broker/Scheduler acquisition tests. Root added the missing process-independent Cockpit purpose/configuration registrations, so the new selector can appear and bind its actual configuration even before any child module is imported. All 156 selection/model configuration/Cockpit routing affected tests passed. The earlier hidden source budget counter repair passed 22 discovery plus 68 web/stage/fetch tests. Neither change is part of installed R9c. Next: complete selected-page/query-variant handling and review the annual qualitative/provider-retry integration before the next freeze; activate the separately reviewed failed-host policy after R9c health acceptance.


### R9c sustained health and first actual Dossier accepted — 07:12 UTC

The 45/45 samples from 06:59:58 to 07:11:15 UTC were healthy on the same postdeployment controller PID 18253, spanning 676.570677 seconds. Finalization rechecked all 561 installed runtime files, twelve complete model configurations, broker tree and preserved historical state, then marked R9c `deployed_verified`. Health summary SHA-256: `3a6514314a78bb7857da839bb8696640b2519130b8c1cd4ab76f6a22c7b58bce`; finalized deployment receipt SHA-256: `38d91e1626269d312a049fad7d3384b5009b62315998df32c6707697ad29ecc2`. Failed R9a health and failed R9b tests remain historical failures.

Independent read-only acceptance found and verified ACN `company-dossier-version:company-sec-cik-0001467373:2`, content hash `b57cece992565fb09d6f7864dae555326cb30c25be72888e1ff0a3a0dc8928a9`. New business-model, supply/cost and variant-view units have valid producer and independent-verifier formal provenance. Industry-classification, segment/mix and demand-driver units retain their explicitly reported legacy proof status. Receipt `dossier-v03-acceptance/r9c-post-deployment.json` SHA-256: `31a5afca6555ad49d840b97dbfa06503043fb69ac009bb6d84498d49b5d5757f`. This verifies one actual publication, not completion of all five companies or an independent investment-quality rating.

The postdeployment stage audit retained acquired quarterly calls ACN 0 / CTSH 1 / EPAM 2 / IBM 0 / DXC 4 and displayed historical extraction separately from full-document read proof. Audit SHA-256: `eabc8bf9ff477ffed2913d36a5160f2092be94c6b004aeb681b12d930b57cd15`.

The next candidate incorporates selected AlphaEngine page/query progression, an explicit 130-call plan matching current mission authority, and registered annual-document retrieval/draft/independent-verifier/staging contracts. The annual/provider integration passed 270 affected tests in 83.547 seconds. Top-level resident execution and selection activation are still being closed; none of these next-release features is described as already installed. Next: complete these concrete consumers, validate the full immutable next release, and activate the reviewed failed-host plan with scoped rollback and new-controller health proof.


### Web cooldown activated; nonblocking quarantine follow-up — 07:35 UTC

Root executed the reviewed configuration activation at 07:21:13 UTC. Writer PID 28749, controller PID 28760 (started 07:19:33.785924 UTC), and control PID 28764 all restarted; exact new-controller health passed. The old v3 plan remains unchanged; only the selected v5 plan, selector and writer plan argument changed. The actual coordinator reported `www.investing.com` under the configured 3-distinct-URLs / 24-hour window / 6-hour cooldown. Receipt `web-host-cooldown-review/activation-receipt.json` SHA-256 `d47d75bbdd9f8f87d8a29089f0d61a3720ce34980e609d3b276b311327ef5c8f`, content hash `ff5301b078c6b6d839498d7da0c1b447fca01042edaa610c3432a3eafeb2b1d9`. All protected configurations and mission authority were preserved.

The owner clarified that an unreachable domain must not create a queue blockage. Candidate `792e3d0` therefore adds an explicit optional recovery policy: quarantine is reconstructed from the immutable fetch ledger and survives the initial rolling window. Normal work excludes quarantined hosts before selection; healthy-source work wins even when an unreachable host was configured as preferred. An expired timer admits one low-priority probe only when normal work is absent. The source's existing in-flight guard prevents a second probe. Any unsuccessful probe increases the configured backoff, capped by the configured maximum; an acquired response resets quarantine. Static skip hosts never probe. Candidate idle output separately counts held domain links. This behavior is not activated yet; the installed policy is still the initial six-hour cooldown.

Six new fold/real-ledger coordinator tests cover window expiry, typed versus retryable failures, increasing/capped delay, successful reset, healthy-host priority, one in-flight probe, explicit terminal-row probing, static skips and configuration compatibility. Broader testing exposed an unrelated candidate AlphaEngine rewrite that accessed `document_type` before dispatching Web specs. Root fixed source dispatch in `58ce924`; the installed R9c never contained that regression. The combined Alpha/Web/domain/model-selection run passed 188 tests in 15.718 seconds. Root also removed the newly introduced 480/500-character truncation of mission research questions: complete questions now reach selection, governed by the actual configurable model input budget (9 focused tests).

Actual follow-up Dossier verification passed all three ACN 0.3 versions, through version 4. Newly verified units now include competitive position, guidance style, management/capital allocation, historical price drivers and the KPI dictionary; inherited legacy-proof status remains explicit. Receipt `dossier-v03-acceptance/r9c-post-deployment-followup.json` SHA-256 `b513bd3654bc73eb193c1216586e2899681b29ceafb33a7353b5339709176098`.

A disk check found only 1.9 GiB available. Seven completed temporary test copies were archived after verifying every payload, path, mode, owner, timestamp, symlink and absence of open handles; only their redundant expanded copies were removed. Original payload 2,025,739,872 bytes is preserved in 314,981,469 archive bytes with manifests and restore commands. Research databases and deployment backups were not removed. Available space recovered to approximately 3.5 GiB before the bounded activation.

Next: finish independent annual-report top-level execution review and create the real two model configurations; bind them and discovery selection to current routing and day budgets; prepare the provider-failure bridge against the final immutable release; run full acceptance and deploy. Existing historical unknown invocations remain untouched. Follow-on post-send uncertainty needs a separately budgeted fresh WorkOrder rather than replay/refund of the old invocation.


### Integrated annual execution and independent recovery review — 07:55 UTC

Integration `a80ae5b` includes the registered annual-report production path and installation of two independently selectable model roles. The combined annual/provider/host/AlphaEngine/selection suite passed 243 tests in 87.451 seconds; log `/tmp/dalton-r10-integrated-annual-host-alpha-tests.log`. Discovery call plans can exceed the former arbitrary 1,000-call ceiling; actual governed budgets remain authoritative, with only SQLite integer representation limiting storage.

Host recovery review found no production defect and added real SQLite close/reopen coverage: a healthy host runs before a due quarantined-host probe, the second simultaneous probe is refused, failed probes remain isolated, and an explicitly configured multiplier of one preserves a fixed interval. Integration `36eb547` retains this permitted operator choice instead of inventing a minimum exponential multiplier.

Independent annual review confirmed that a single child now waits for exact Scheduler retry time without spending a transition and uses a new invocation on the next attempt. It also identified the remaining crash boundary: no autonomous consumer relaunched a child killed during that wait. That gap is being fixed with the exact approved plan and existing Work identities, rather than requiring a second human run. First-install annual configurations are also being hardened against concurrent owner writes and given explicit configurable retry/queue defaults; the real existing seed files have no such overrides. Annual call/run defaults are being registered so first installation does not silently retain a 120-second, single-attempt path.

Unknown post-send completion remains fully charged and historically intact. The next recovery must create a new bounded WorkOrder and persist a versioned recovery link that downstream verification can resolve; marking the original failed Work successful or replaying its invocation is not acceptable. This work is still under development. None of these candidate changes is reported as deployed.

Next: close actual child-death resume and bounded unknown-result recovery, complete exact configuration activation rehearsals, freeze the full release, and repeat full tests, wheel verification, copied-state execution and postdeployment health.


### Actual failed-host projection and annual budget blocker — 08:01 UTC

A single strictly read-only transaction over live Core projected the candidate host policy: `www.investing.com` has three distinct terminal-failure URLs, remains quarantined until `2026-09-11T12:08:56.858093+00:00`, and has ten failed document rows. No fetch or model call was made. Private receipt `web-host-recovery-review/live-ledger-projection-20260911T0757.json` SHA-256 `9fc37e86b88d2c94325a5bf3b23361f631b129c41dc33a25f0f0ee4e86993ccf`. The installed v5 initial cooldown and candidate v6 persistent quarantine remain distinct.

Further independent review found a release blocker in the new annual model worker: it inherited empty budget hooks and recorded Observability costs but had no mission/day budget admission or settlement. A per-call WorkOrder cap is insufficient. This candidate has not been deployed. The repair must attach real budget authority before dispatch, settle each attempt exactly once, retain full reservations on unknown completion and release only proven not-sent capacity refusals. Any historical attempt lacking this proof remains unproven; it cannot acquire fabricated settlement history to authorize recovery. Only after this foundation is fixed may a separately bounded new Work recover an unknown result.

Annual call/run defaults passed 21 focused tests. Provider host installer passed 19 private tests after stopped-process/socket proof, exact scope and dynamic configuration preservation fixes. AlphaEngine activation helper now rejects a retired mission even when the old version remains in history, binds the reviewed source commit and checks installed selection capability before activation (eight private tests). All these helpers remain inert pending final immutable release binding and acceptance.


### Configurable transport bounds and budget-wiring review — 08:09 UTC

Root removed the document reader's arbitrary three-not-sent-retries, 3,600-second queue and 60-second backoff ceilings. Overrides remain closed non-negative integers, with waits required to fit the runtime clock. Installed defaults and actual mission/day budgets are unchanged. A real governed extraction fixture now tolerates five proven pre-send refusals before success and creates exactly one paid admission and settlement; a long queue/backoff configuration is reflected in the immutable Scheduler lease.

The 63-test run exposed two old synthetic capacity tests that changed only an error code after constructing a completed model invocation. The shared provider-proof repair correctly rejects those unproved capacity claims. Tests now require terminal failure and retention of actual paid usage; production proof checks were not weakened. The final affected assertion and adapter/budget/provider suite passed 32 tests in 1.607 seconds. Initial failed test logs remain in `/tmp/dalton-r10-configurable-transport-tests.log` and `...-v2.log`.

Annual budget and transport configuration bindings are now in the immutable plan and WorkOrder metadata (40 focused tests). The first actual budget-hook implementation is merged, but independent review is still checking active mission/outer budget/pool authority, cross-day replay, overrun accounting and an actual broker subprocess path. Direct hook tests and the CLI fixture adapter do not establish those production conditions. The isolated full-suite preflight at `ee39177` is running; it is explicitly not deployment acceptance.


### Isolated full preflight failed and targeted repairs passed — 08:14 UTC

The immutable preflight `ee39177bad8edf5ae962e095ba2055788ca2d138` ran 7,040 tests in 601.117 seconds and failed with four failures, three errors and one skip. It was not staged or deployed. Full log SHA-256 `e188281d2cd9a2dd161c361f2b18c84b90cde3c152267d3d22c03e977c20db57`; receipt `/tmp/dalton-foundation-followup-r10-preflight-full-suite-receipt.json`.

Two failures were the unproved-capacity fixtures already corrected above. Two annual raw model-output schema files lacked titles; they now declare their identity while preserving the exact runtime prompt schema. The deterministic registered annual retrieval proof is now tested as a content-addressed derivation, not incorrectly required to carry an event timestamp that would change on replay. Model configuration registration again preserves the original first three names and appends discovery selection after them. The 121-test affected contract/registry/model-selection/annual suite passed in 5.476 seconds; a separate shape check confirmed both published model schemas match the actual prompt schemas excluding annotations.

Next: finish actual budget authority and crash/unknown recovery review, then freeze a new complete candidate and repeat the full suite. This failed preflight remains explicit evidence, regardless of the targeted checks.


### Annual child restart path merged; edge-case review remains open — 08:22 UTC

Integration `26b94e8` adds automatic annual child recovery and exclusive first-install configuration publication. A real subprocess fixture was terminated during provider backoff; after reopening the launcher, it resumed the same ticket, plan and Scheduler Work, used attempts one and two, and reached independent verification/staging. A superseding mission pointer prevented recovery. The combined annual/setup/launcher/transport/contracts run passed 33 tests in 16.116 seconds.

Root review found additional recovery edges that those tests did not cover: a child that fails before admitting any Work could be relaunched without bounded process backoff; one nonresumable historical ticket could prevent scanning later eligible tickets; and an inherited still-live child was monitored without reserving the launcher's normal single-flight slot. These are being repaired before release. Successful process restart in a fixture is not the final production acceptance.

The final Alpha activation helper now also publishes through an exclusive hard link, so failed fsync cannot expose a partial target and a concurrent owner write cannot be replaced. Ten private helper tests passed; helper SHA-256 `179e629211a04eefe6e33e36ae8d1b077b3b3baf35c86834b9248bf02b5747cb`. Its provisional source binding still needs the final release commit. The separate v6 host activation helper passed nine private tests and remains explicitly inert until rebind.

### Domain policy validation and production model review — 08:32 UTC

Root unified plan/runtime cooldown validation and removed the arbitrary 100-URL and 30-day configuration ceilings. Positive integer counts and representable clock durations remain required; the selected 3-URL / 24-hour / 6-hour live policy is unchanged. The legacy cooldown query now normalizes `as_of` to UTC before comparing stored timestamps. Regression checks prove that UTC, UTC−7 and UTC+8 representations of the same instant produce identical quarantine results for both policies. The combined host/Web/discovery suite passed 54 tests in 13.417 seconds (`/tmp/dalton-r10-host-configurable-policy-tests-v2.log`). The initial run's obsolete test expecting a 101-URL override to be rejected was updated; its failure log remains preserved.

Independent production annual testing found another fixture-hidden blocker: the verifier route supplies producer-family authority but the Work lacked its required provider contract/schema hash. That contract is being wired and tested over an actual Unix broker transport. Root also confirmed that the separate Cockpit model executor still lacks configurable returned-provider retries and its legacy single-route accounting trusts capacity error codes without dispatch proof. Those paths must use actual proof, distinct paid attempts and immutable policy identity before this foundation is described as complete. The fresh-Work annual unknown recovery graph has a reviewable candidate; its temporary budget stub is not production acceptance.

Next: finish the annual budget/verifier and crash-resume repairs, integrate the fresh-Work graph with actual budget authority, close the common document/Cockpit provider retry paths, then freeze and repeat complete release acceptance. Current live runtime remains R9c; candidate features are not reported as deployed.

### Actual source decoding failures and installation rehearsal — 08:44 UTC

The bounded child supervisor merged in `dee4bf1`; 26 integrated launcher/annual/setup/budget tests passed in 17.034 seconds. The first root command named two nonexistent test modules; that invocation failed and was corrected rather than counted as passing. The annual installation rehearsal on configuration copies preserved all 12 existing files byte for byte, added exactly the two dedicated annual roles and preserved all 14 files on a second install. No live mutation, database creation or model call occurred. Receipt `annual-config-review/dee4bf1-rehearsal/review.json` SHA-256 `e86dbc1bba1ac322b4045be4082ad0cc0366c00b6f563919da1bde440d14a719`; final release rebind remains required.

A read-only inspection of the live reader's six failed document views identified failures before model dispatch. Cognizant's public 16-page AI-services PDF accepts an empty password, but the old reader rejected every `is_encrypted` file. Accenture's Angie Park page was stored as a valid gzip body and then incorrectly decoded as UTF-8. Two CSI Market pages have contradictory UTF-8 BOM/Windows-1250 metadata and remain unresolved; an empty rendered Zacks page and a missing Morningstar acquisition ticket remain separate holds.

The candidate supports only an empty PDF password (real passwords are still required), and decodes gzip with integrity checks and an explicit configurable `reading_limits.max_decompressed_bytes`. Original spool bytes and manifest hashes remain unchanged; newly supported paths have distinct renderer identities. Existing ordinary UTF-8/PDF renderer identities remain stable. Forty-six source/reader/configuration tests passed in 14.016 seconds, including real encrypted PDF fixtures, corrupt/truncated gzip rejection, configured size bounds and a complete compressed-body receipt/spool round trip.

Actual candidate rendering of the original spool bodies yielded 28,924 Cognizant characters and 4,513 Accenture characters, neither truncated, with the target company/person present. The PDF's second page was independently rendered and visually inspected. Read-only rendering receipt `/tmp/dalton-reader-proof-20260911/actual-rendering.json` SHA-256 `aa1acfaefd14fc03d159ba4a374d3fc833b6fa16f55a88ac063be96f40f5f27e`. This proves source readability, not completed model extraction or research deliverables. Independent review is checking source bounds and whether historical unreadable holds can consume the new renderer safely.

Next: merge the independently tested production annual budget/provider contract, finish recovery graph and common model retry integration, close reader review, then run a new immutable full release acceptance. Live health was healthy on controller PID 28760 at the latest check; no new deployment has occurred.


### Production annual authority and retained uncertainty — 09:02 UTC

Annual budget/verifier repair `bed7a36` passed 72 integrated tests in 14.153 seconds, including the real `sec_lane_cli` → adapter → Unix broker path. Each paid attempt requires the current mission and exact company/model permission, current constitution/mandate/playbook and governed budget authority. Verification has its own admission and provider contract. Cross-day replay retains the original admission day; unknown metering retains the reservation, and an overrun records a durable alert and budget freeze. Fixture-only success is no longer the sole proof for this path.

Fresh-Work recovery graph commits `0abc118` and `9f96b34` retain the original failed Work/invocation/budget history, bind a separately budgeted replacement and resolve downstream verification/staging against the effective graph. The graph currently proves recovery from returned provider failures with unknown metering. Actual socket loss after dispatch still needs a typed durable receipt and a real transport recovery test; it is not described as closed. The integrated 76-test run found a duplicate mission-pointer insert in the merged fixture. Root made the setup idempotent while asserting exact pointer equality; the affected 21 tests then passed in 16.342 seconds. The initial failed log is retained.

Reader review confirmed original compressed bytes remain provenance authority, ordinary rendering identities remain stable, and historical unreadable summaries are re-evaluated without reopening human decisions. A real extraction service test proves both initial reading and later re-read honor the configured decompression bound. AES-256 empty-password PDFs now have an explicit `pypdf[crypto]` installation dependency. The 32 affected tests passed in 11.090 seconds; development Python 3.13 and installed Python 3.14 produced identical text hashes for the two actual source bodies. No live extraction success is inferred from rendering alone.

Next: close queue-aware annual leases and safe-retry consumption, actual post-send uncertainty receipts/recovery, and common model-path retry/accounting review. Then freeze a new full candidate and refresh the exact configuration delta review (new annual roles plus explicitly reviewed provider policies), wheel, copied-state rehearsal and deployment health. The existing 12-file preservation rehearsal cannot be reused as proof for newly changed policy bytes.


### Common paid retries, real socket recovery and second preflight — 09:15 UTC

Common document/Cockpit provider retry commits `e5e5879`/`07b35b1` bind the policy to immutable Work identity, use separate Scheduler attempts and budget admissions, retry the same selected profile before fallback, and retain full reservations when actual cost telemetry is absent. Root review caught and corrected an initial estimated-cost refund and recursive retry implementation; iteration now closes the previous attempt's contexts before waiting. The 144-test integrated run passed in 27.759 seconds. The first root command included a nonexistent test module and failed; its log is retained. End-to-end queue validation `abc0447` removes the stale one-hour cap; all 32 broker JavaScript tests passed.

Annual installation fix `87954cd` keeps the new annual unknown-recovery defaults even after generic role setup has populated a provider policy. Inherited owner retry values are retained, existing dedicated annual files remain byte-identical, and zero fresh WorkOrders explicitly disables recovery. Sixteen annual/setup/provider tests passed in 12.590 seconds. A real setup rehearsal over a read-only backup of the live Router preserved every existing config field except 11 explicitly added provider policies, added exactly two annual configurations, and changed no bytes on the second setup. No live database/configuration writes or socket/model calls were allowed. Receipt `model-config-delta-review/87954cd-rehearsal-v2/review.json` SHA-256 `0e9cf7bc12fb0b2c3437a08dc590525f7f66488bc9f41811897ce8360ff6b51f`; planner policy still awaited its consumer in this rehearsal, and final source rebind is required. The first rehearsal was rejected by the helper's literal temporary-path comparison across macOS's symlinked temp directory, before any setup call escaped; resolving the scratch path fixed the guard.

Real post-send unknown evidence is merged in `1adda53`: timeout/drop/malformed response after dispatch retains typed invocation and unavailable metering, without inventing a broker response. A separately admitted fresh Work can proceed to a new actual response, independent verification and staging. Historical mission hash, pool and outer-budget binding are checked semantically. The first 78-test integration run found a supervisor race: a second supervisor treated a summary published by a still-live child as terminal. `8393cc2` retains the slot until that exact process exits and gives atomic ticket writes unique temporary names. The test child's summary now matches the real CLI's explicit `ok` field. Thirty-two launcher/recovery/annual tests passed in 33.393 seconds.

Annual queue/lease commits `36329a4`/`db33822` consume safe retry configuration on the actual adapter path. Only proven pre-send failures retry the same route; queue and call time plus completion grace fit within the immutable Work elapsed budget. The default claim is 2,432 seconds; a configured 7,200-second queue with sufficient run budget passed a real Unix transport boundary test. The combined 104-test annual/transport/budget/recovery/launcher/adapter suite passed in 52.702 seconds.

The second immutable full preflight, `f1b0a8969e7d8f1d9e848cb5e4ce00aadf9af146`, ran 7,073 tests in 621.710 seconds and failed three tests (one skip). All failures concern the new annual verifier provider schema: missing packaged-asset declaration, nonportable schema keywords, and rejection by the installed Google validator. Log SHA-256 `89bd3bc69feed35fb4c271bfcf6db97f5a25074c4bd79217340643727b66dd4c`. It was not deployed. The portable provider contract repair and the custom research-planner retry/accounting consumer are the remaining integration work before the next complete freeze. Live health at 09:09:30 UTC remained healthy on controller PID 28760 with a 0.446-second heartbeat age.


### Portable verifier fixed; deployment space preserved — 09:22 UTC

`2e599ea` declares the annual verifier schema in wheel package data and supplies a Google-portable provider projection. The Work binds the actual packaged provider schema hash; the internal strict verifier still enforces its nonempty values and pass/refusal constraints. The 34-test root integration run, including packaging, installed Google validator, production Unix broker and fresh-Work recovery, passed in 28.048 seconds. This closes the three known preflight2 failures through affected tests; full release acceptance still requires a new freeze.

The planner custom path is not yet merged: review found broad adapter exceptions still returned a zero settlement without proving no send, plus a fixed 120-second transport timeout. These must consume the configured effective call budget and preserve uncertain spend before release. Additional model-consumer inventory remains explicit; inactive or retired paths will not be confused with installed production consumers.

Initial deployment disk preflight required 4,046,451,661 bytes and had only 3,727,314,944. The older `deploy-backup-20260911T052940Z` was archived with every payload, path, type, mode, owner, group and timestamp verified, then rechecked unchanged with no open file handles before removing its redundant expanded copy. All 89 files and 8 directories are retained in the 158,225,885-byte archive (source payload 1,021,574,568 bytes), SHA-256 `e1536a0bcd814f7246d3705305113b9e4aba0893abbd23a24c864e7d974d238c`; manifest SHA-256 `249db169a60d0a8cf092be113fa1b9b8a4b6174bab2d65f0da2478c49ff40dbb`, with a restore command in the verification receipt. The latest R9c backup `deploy-backup-20260911T065436Z` remains expanded. A subsequent unchanged-reserve preflight passed with 4,597,002,240 bytes available. Main's unrelated four-file DeepSeek diff remains SHA-256 `43832e8e6d32cec9bf48b59044d200df0d57a99d30598859c23d4c1360207932`.


### Planner retry consumer merged; new release acceptance pending — 09:27 UTC

`c621b2e`/`0be62c2`/`e6f013c` close the custom Writer planner path: returned-provider retry and safe transport policies bind immutable Work identity; each paid attempt gets a separate route/invocation/admission/settlement; only exact not-sent proof releases uncertain reservation. Post-send unknown evidence is persisted without automatic same-Work retry. Installed plan-purpose call budgets and narrower per-request limits determine the Work, adapter and lease timeout, replacing the fixed 120-second adapter setting. The 152-test root planner/driver/setup/Cockpit/budget integration run passed in 10.956 seconds. These are candidate changes until complete release acceptance.

Actual setup inspection corrected an earlier review assumption: the shared role installer seeds transport retry on all eleven role configurations, not only the planner. This matches their existing transport consumers. The final copied-Router rehearsal at `e6f013c` proved exactly twelve provider-policy additions, eleven transport-policy additions (extraction's existing transport was unchanged), and two new annual configurations. Every other existing field and owner override remained unchanged; a second setup preserved all fourteen files byte for byte. Receipt `model-config-delta-review/e6f013c-rehearsal/review.json` SHA-256 `6a64b9a555861284480c446bb17964ff8eb721c16c1dfa4ce369c388fe2ee23e`. A final freeze rebind is still required.

Next: freeze this complete release scope and run full Python tests, broker checks, wheel/source verification and copied-state execution; only then stage and deploy the exact reviewed configuration delta. Persistent host isolation, AlphaEngine selection and the provider-failure bridge retain separate exact activation gates. Follow-on model-consumer inventory is still open for human-intent, legacy agenda, ThesisImpact and offline calibrations, with runtime/retirement status to be established rather than assuming all are live. Planner selection/budget changes during a long-lived Writer also need explicit reload/immutable-work review. These follow-ons remain scheduled after this bounded deployment and are not reported as finished.


## 2026-09-11 09:37 UTC — R10 copied-state failure retained

Frozen `697dcba84664d94f9ebab4dfabfc42d26721bdf9` packaged 572 runtime files exactly (410 Python, 68 SQL, 91 JSON, 3 HTML), with three inline JavaScript syntax checks and 32 broker tests passing. The independent full Python suite is still running. Copied-state rehearsal failed at 09:29:51 UTC: `mission_source_discovery` escaped with `unavailable:RemoteError`; retained Writer log identifies `CoverageMissionConflict: document is not awaiting acquisition`. No R10 deployment or live state mutation occurred. Failure binding `/tmp/dalton-foundation-followup-r10-rehearsal-release-binding.json` SHA-256 `7a841e7629dda5f418982d16ac7051047af51dee63a58a03d8d6ad062011a5d0`, report SHA-256 `7a983792580606729735d22790393763c58c582e77b500d16b290b1eb55ac44f`; scratch state retained.

Next steps: independently fix acquisition state replay; strengthen copied-state rehearsal to run the reviewed actual role setup (12 existing configs plus two annual configs); then freeze a new revision and repeat release acceptance. Configuration transition private verifier passed 17 tests and exact 14/15 read-only authority checks; its 697 source binding is not acceptance of this failed release. Persistent host quarantine, provider bridge and AlphaEngine selectors remain unactivated candidates behind the final release gate.


## 2026-09-11 09:43 UTC — full R10 failure and behavioral lease regression

The immutable 697 full suite completed 7,100 tests in 701.980 seconds: one failure, zero errors, one skip. Log SHA-256 `bc58d900be5efa2661c53f1495337ed1ec5a13f7e03d1848c7348937486fdd20`; failed receipt retained at `/tmp/dalton-foundation-followup-r10-full-suite-receipt.json`. The failing test inspected the public Cockpit call function for lease keywords moved into `_call_once` by iterative retry; it did not execute a model request. Replaced that brittle assertion with a real pinned-router/Scheduler test advancing its clock by configured 600- and 7,200-second queue waits plus the full permitted call duration. Both independently named work orders complete successfully under versioned leases. All 53 Cockpit fallback and planner CLI tests passed (2.109 seconds), log `/tmp/dalton-r10-real-cockpit-lease-regression.log`. This does not relabel the frozen full-suite or copied-state failures.

Two inert release helper candidates now verify exact stage 14 configuration through the separately bound transition verifier, preserving the original 12-file historical baseline independently. Twelve private positive/negative composition tests passed, including artifact/source drift, unsafe paths, failed child verification, and exclusive receipt preservation. They have not been staged or executed on live state.


## 2026-09-11 09:47 UTC — actual discovery repair and planner reload integrated

`1286793` permits a failed discovery row to settle as acquired only through the existing caller-proven source-authority path. The live-state copy contained 30 of 33 failed AlphaEngine rows whose exact document was already held; these rows must enter review without another fetch. The atomic transition clears failure fields and deliberately adds no fabricated transport success, so it cannot reset host quarantine. Seventy-five root-integrated host/discovery/fetch tests passed in 24.853 seconds.

`79f6d63` fixes resident Writer planning after Cockpit selection, budget or queue changes. New planning reads current service routing/broker/call budget plus registered sidecar retry/ledger controls; a disabled service cannot be resurrected by stale LaunchAgent arguments. New Work binds the effective budget, retry policies, route policy version/hash and credential slots. Changed requirements create a distinct planner Scheduler policy; admitted old Work and its policy are not expanded or rewritten. The worker checks exact execution authority and reconstructs timeout/queue/lease from its Work. Root integrated planner/driver/Writer/model configuration/router/Scheduler/doctrine/contracts tests: 276 passed in 18.190 seconds (`/tmp/dalton-r10-planner-reload-integrated-tests-v2.log`). The earlier root command named nonexistent `tests.test_writer_server`; its 253-test/one-import-error log is preserved separately.

Remaining actual custom model consumers are explicitly classified: natural-language IntentInterpreter is reachable through the configured control intent composer and is the next bounded retry/accounting task; ThesisImpactModelWorker is reachable through its enabled production service and remains pending. Legacy Agenda coordinator is retired by default under ADR-0009 and only runs with explicit `legacy_agenda_plane:true` plus agenda enablement. No claim is made that every model consumer is closed.

At 09:44 UTC the existing live controller PID 28760 (started 07:19:33) passed the read-only health check; heartbeat age 0.273 seconds. Main's unrelated four-file DeepSeek diff remains SHA-256 `43832e8e6d32cec9bf48b59044d200df0d57a99d30598859c23d4c1360207932`. R10a freeze awaits the bounded copied-state actual setup entrypoint test, then full acceptance/deployment and separate host recovery activation.


## 2026-09-11 09:50 UTC — R10a acceptance preparation

`7ff37b9` adds explicit `--rehearse-reviewed-model-setup` to the existing deployment rehearsal. After catalog sync and before rendering, actual extraction/eleven existing-role/annual setup entrypoints run twice against the confined copied Router, with sockets refused. Exact 12→14 inventory, 23 permitted missing-field additions, existing-field value AND key-presence preservation, exact annual seed derivation, unchanged service/input metadata, duplicate route policies, and second-install byte identity are checked. The normalized final snapshot hash is reported for comparison with the separate private transition evidence. Root ran 102 existing rehearsal tests and three new transition tests, all passed. No live setup was run.

The complete bounded R10a source now comprises the failed-document authority repair, real Cockpit lease test, resident planner configuration reload and actual installation rehearsal. Freeze this source once, then run full Python suite, wheel/runtime inventory, broker tests and fresh copied-state rehearsal together. Preserve all failed R10/preflight artifacts. Natural-language Intent retry work proceeds independently outside this freeze.


## 2026-09-11 09:57 UTC — frozen R10a wheel/configuration/rehearsal evidence

R10a freeze is `/Users/everflow/Projects/dalton-foundation-followup-r10a-acceptance-worktree` at `7e3fb7bbd9095a3fd78af1b0e0d10bd7c6fb48e4`. Wheel SHA-256 `56afe2a3c104c6274cfd2caa60b33e86637ac9751c80f20f8ea06de2a3b70bfd`; 572 runtime files match the exact tracked set and source bytes, three inline JavaScript checks passed. Thirty-two broker tests passed. Full Python suite remains running; no release publication/deployment yet.

Actual copied-state setup and controller tick passed on the first R10a run, but the outer root harness incorrectly compared normalized JSON hash against the pretty-printed artifact file hash. Its failed binding `4b1543b743eec35c20122a96b25a278e3d80337964f12768d8732ad642570b0d` is retained without relabeling. Corrected harness reran from a fresh copy and passed exact normalized snapshot `6e99aee56dc561fd0dc4305f160cfcb99ab1586ebf988cd8af5c691b04fac172`, 12→14/23 additions, 68 schemas and 40 tick entries/zero escaped lanes. New binding `/tmp/dalton-foundation-followup-r10a-v2-rehearsal-release-binding.json` SHA-256 `127de39de1ba77a9a4f7ab6abeaaf14d42a04716889a3249814d7eba2bd1a4a2`; report SHA-256 `42e6118d4f92d5ab94ba1081f60d9f0bae2b071a3327bbccc37a625c6799bd37`. Successful scratch state/home were removed only after no-open-handle/process checks; logs/configuration/report remain.

Separate actual setup and read-only 14/15 authority verification were rerun against the final freeze in new private directories, preserving e6/697 evidence. Setup receipt SHA-256 `79d498526c4f78dca11aedcc759a4bf8c354a8e2c050af5678a104dc07cd4d38`; new transition verifier `ba09db179254cf959ec416a89117272c10f3fb0cc86a950652069b91a1ec54fe`, final binding `5b41956dade855eb61545f37b3e8df23266906750163f9a0ad2843faeffa7d7a`. Expected 12/14/15 and discovery values remain unchanged. Seventeen private verifier tests and 22 rebound release-helper composition tests passed.

Both failed rehearsal state trees were archived with every payload/metadata hash checked and no open handles, retaining all external logs/reports/failed receipts and verified restorable archives in the owner packet. True 697 failure archive SHA-256 `106780413a6c503ae44cb7a0ac77e07542fde4cd827bb0cea7249f38a797f8ea` (173,133,245 bytes); first R10a harness failure archive `49c987dd3bcbd217da211b1b25f0d1a49bf00d71300e195dbbec5c0209e65925` (173,248,275 bytes). Latest real deployment backup 065436 remains expanded. Available disk returned to roughly 5.6 GiB without reducing installer reserve.


## 2026-09-11 10:06 UTC — R10a deployed; sustained health pending

The frozen R10a suite passed 7,110 tests, zero failures/errors and one skip (614.170 seconds wall time). Full log SHA-256 `ed73173071e7ad1a3b3fa023c24b9576a062c1d6fcb75d1670516acf4e25f745`; receipt `1839ff797016e66c220d77972d956834d0dcc7ff63a904edd67e26dcccd81472`. Final reviewed stager SHA-256 `1eb04c7675761b5d7d4f3dcde8986ab5787c93c4c95fdf9a36a1f710b5967abf` published the accepted packet. A misspelled root preflight environment flag was rejected before service operations; corrected closed-environment preflight passed.

Authorized deployment ran 10:01:52.858685–10:03:36.359074 UTC, exit 0, backup `deploy-backup-20260911T100153Z`; execution receipt SHA-256 `e265686c5fe91b2e01b0c30501144aa6205e6274370f319fd57a45276f919397`. Retained source `.release-source.q8vrvm` has all 1,921 tracked archive files byte-identical; all 572 installed runtime files match the accepted wheel. Installed-bytes receipt SHA-256 `66ca6ad06d6eb9c3699099d313d59fd2c1c77fdcc4d652c478f31ddd7ce8b2ee`.

Exact stage-14 configuration verification passed, including original 12-file baseline linkage, two annual derivatives, every route/credential and canonical day-budget authority; configuration receipt SHA-256 `eac382ce5ca0e3943e2b861def099b64c224426172afec1a0f7770a1c9eb2940`, stage14 receipt `78a5d50368d512363254c3c7254c10cd62dccbb6ad13251bc9f7acda72853d0f`. Service JSON and all three LaunchAgent plist hashes remain unchanged (annual configs are found by their runtime paths, without additional plist arguments). v5/Alpha v1, broker 16/64/600000 and historical unresolved broker invocation remain exact.

Manifest status is `installed_bytes_verified_runtime_pending`, SHA-256 `4486d61222ec623531136e2968395a187a78eee53186394c6d94be1a98e0838c`. New controller PID 90488 started 10:02:31.623143 UTC; first health sample at 10:05:19 passed. The required 45 samples spanning at least 660 seconds are still running. This is not yet sustained runtime acceptance or all-company product completion. Separate host recovery/provider/Alpha packages must wait for the deployed_verified gate.

## 2026-09-11 10:16 UTC — next-release Intent retry and expected feed holds

Integration `80dd3b0` adds the reviewed natural-language Intent retry consumer outside the installed R10a freeze. Root ran 271 integrated tests covering Intent/control/workspace/model selection, real adapter/provider proofs, Scheduler, fallback and planner: all passed (`/tmp/dalton-intent-retry-integrated-tests-r10a-next.log`, SHA-256 `8f8afc72d9a6160ac17913f84a9bd76458798a1715139f6c440ba3394606fc1d`). Terminal retry paths retain the actual provider/capacity proof; persisted retry evidence is reconstructed and revalidated. This is pushed code, not live activation.

Live log review found an expected Sales Notes governance refusal repeatedly surfaced as an unmapped Writer exception. Feed dispatch now preflights both list and get authority before building any runner, returns a structured blocked result with the exact reason/operation, and reloads the actual governance files on the next tick. No authority is created or changed. A real-file regression verifies both operation holds, zero runner construction before approval, and recovery after a genuine approved file replacement. All 56 human-feed tests passed (`/tmp/dalton-feed-governance-hold-tests.log`, SHA-256 `8636d6f834961f5e3b1b236e49173a63381a3c0006d056aa972613bf10c1bce1`). This change also awaits the next release. ThesisImpact parity remains under independent implementation; final visual/reference-deliverable/multi-workspace work remains ordered after foundations.


## 2026-09-11 10:18 UTC — sustained R10a acceptance passed

All 45 samples passed over 670.8493875 seconds, same postdeployment controller PID 90488 and three completed planner cycles. Summary SHA-256 `11e917781f9c8aad7438e558726feab7da8597e229e512676bf413b00cc62f8f`. Finalizer rechecked raw sample bindings, 572 runtime files, exact 14 model configs, route/credential/budget authority, mission and preserved broker/history. Runtime receipt SHA-256 `3335705b2b12aad350e25659216982c8f6744489c1098fe0b16823339749744f`; manifest is now `deployed_verified`, SHA-256 `ee1b7d91555bc93f6ffc3d23fa596439b4e500cedc257f92f37b91039e48f595`. Historical failed health/suites/rehearsals remain separately bound.

Root independently ran all 13 web activation helper/rebinder tests and 24 provider bridge installer tests. Web final package SHA-256 `353e6b52e2d50c2ff9daf548460a22ca356d015a539ea3da689d8635296be873` passed read-only preflight against this accepted release. Authorized controlled activation started; drain reported zero remaining children. Its new-process health receipt is still pending. Separate audit identified an unexpected restart between durable child launch and DB acquisition binding that could admit another probe; a minimal durable-ticket gate and restart regression are in development. This controlled-window activation does not claim that unpatched crash seam has already been closed.


## 2026-09-11 10:24 UTC — host recovery live; obsolete backup deletion authorized

Web v6 activation passed at 10:19:56.612290 UTC: receipt `web-host-recovery-r10a-activation/activation-receipt.json` SHA-256 `3d130e2175cef5aee38e3038b7e23a89adc7469ea30a2f89c6ede34bb19d10f1`. Writer/controller/control PIDs changed to 94844/94850/94854; controller began 10:18:33 and passed health after its first tick. Selected v6 plan `1f73355f3828ee0f62f70981502fb6c797c0efb434e2bfaa16d876730eb88b6a`, Writer plist SHA-256 `000ebefda0b9b9cfcf430ecec18f2da9cc6a21a2ac340e4a993f729c618695cc`; exact service/configuration/history protections passed. The actual coordinator consumes 3 distinct terminal URLs / 86,400 seconds / 21,600 seconds initial hold with configured recovery. Independent actual-ledger projection retains Investing.com isolation and ten held failures while a healthy URL remains eligible. Restart-seam repair is still a next-release item; root requested additional PID-reuse protection before merge.

Disk availability dropped to approximately 300 MB during new-controller startup (no new runtime ENOSPC was observed; the log also retains earlier ENOSPC history). Root initially verified and archived older 065436 backup, freeing about 900 MB. Owner then explicitly instructed: delete obsolete backups, do not archive them. Under that superseding instruction, 18 old deployment tar archives were directly deleted, freeing 2,548,750,153 bytes. `obsolete-backup-deletion-20260911.json` records the exact inventory; latest `deploy-backup-20260911T100153Z` and active web/bridge rollback files remain. Older small hash manifests are historical records only: their deleted archive payloads are no longer available. Future cleanup must delete obsolete backup copies rather than create replacement archives.


## 2026-09-11 10:31 UTC — actual product proof, runtime backup cleanup and next release

Owner identified 26 runtime snapshots under `state/dalton-core/backups` (~21 GiB) and authorized keeping the newest three after verification. Root checked each of the nine retained database files against manifest size/SHA-256 and full read-only SQLite `integrity_check`; no backup files had open handles. Deleted 23 old snapshot directories directly, without new archives. Retained IDs: `20260911T071935.194784Z`, `20260911T100231.684487Z`, `20260911T102012.646598Z`. Released payload 19,811,529,618 bytes (18.45 GiB); directory now 2.9 GiB and system available 27 GiB. Receipt `state-backup-prune-20260911.json` SHA-256 `7f9c8d354d2371e4792bbea9d576c2da39812942dade6f1e01f10466b3a4ecc4`. A separate attempted cleanup preflight for old /tmp test copies stopped on its age assertion before any deletion; it made no changes. Configurable retention implementation is assigned separately.

Root independently verified seven existing Dossier 0.3 versions across four companies using complete formal producer/verifier WorkOrder, result, route, request identity, mission, unit block and predecessor/carry-forward checks. EPAM v4 (10:08:47 UTC) newly verifies catalyst_calendar, history_of_price_drivers and kpi_dictionary. Existing legacy/null-proof units are reported explicitly; CTSH still has no Dossier. Read-only audit receipt SHA-256 `98dae6dfc97f0ec8cbbcba01888573cd3c31ea36dc5e8ffb24b746acceee91d2`, file `dossier-provenance-r10a-readonly-audit.json`. The first root audit harness lacked sqlite Row mode and failed before writes; corrected read-only rerun passed.

Durable public-web launch admission merged as `29d8dce`: exact matching live process and unknown identity retain the slot; only proved PID mismatch/death releases it. This closes the pre-acquisition-bind writer restart gap while handling reused PIDs and ambiguous macOS paths honestly. Root's 59 fetch/host/discovery integrated tests passed (22.425 seconds); code is not yet deployed. Bridge root review found and fixed an overstrict running-state pending-record check: historical unknown invocation remained exact, while a legitimate new request was active. Updated inner 25 and outer 11 tests pass independently; stopped apply still requires only the exact historical pending row. Separate final package/preflight is prepared, not yet installed.


## 2026-09-11 10:34 UTC — provider failure bridge activated

Root approved the exact second bridge review (SHA-256 `705785f3e9d05d2c9bfb26f9b5f235f6dfddf1398cdd2806b5bbd27ea2300282`) after independently running 25 inner and 11 outer tests. Inner helper `2fa6ecd0e82aad210698161a18049195570f6c873bd0904aa56a8aaf493408cc` distinguishes running-state admission from stopped-window quiescence; old failed review and static-only rehearsal remain as superseded evidence. Outer helper `9ff0ab7e107aeb0c17c5a1945bfb20dee18cb83c06c0af7807f06688cc56b16b` is bound to actually executed stateful fake-host success and rollback tests, not static source inspection. Final inputs `54b7ad23e1e38108d5ae2a39ff3fbcb7b08c41cdb7fc3986c3953bdec339e707`, final package `56ae092fed4d1326dc4fc7e3ac6b928c015518dfe44e6fe9992951f6d8d9744e`; read-only preflight passed.

Authorized maintenance began 10:31:55 UTC, children drained with zero remaining. Reviewed host bridge patch/runner registration/managed broker plugin and the exact plugin-path-only OpenClaw config delta installed successfully. Gateway changed 74026→99261 (10:32:08 UTC); Writer/controller/control changed to 99490/99520/99524. New controller and static-dashboard publication passed health at the end of the first full cycle. Inner receipt `provider-failure-bridge-r10a-stopped-window/install-receipt/receipt.json` SHA-256 `ac41e1e2c0617ea2066432419951abd83de25cb720eea753aeef8781669da874`; outer activation receipt SHA-256 `b1159447deaea10ef078479d94d62c578d82ac1c00f633c8f3fb3c114ab7fc06`. Exact 14 model configs, all 23 profiles, queue 16/64/600000 and immutable historical unknown invocation were preserved; service/plists/web-v6/Alpha-v1 remained unchanged. OpenClaw config now `249124e3d372aaab5479a272f4901046fb04aec3339d3276711c2068edcb402e`. No paid failure canary was introduced; runtime failure classification is installed, while content completeness remains separately evaluated.


### 2026-09-11 10:53 UTC — R11 freeze and Alpha stopped-window failure

R11 freezes `62f35597b2ca3c57d02bc06e688a2484829c1309`. Root integration checks passed: ThesisImpact retry 344 tests / 60.125 s; backup/service/rehearsal 161 tests / 2.003 s. Wheel SHA-256 `cc7c9d2033745d41f9491493ef9c91401ea18d9f27cb1bd5f2647de98cfefea6`, 410 Python + 68 SQL + 91 JSON + 3 HTML files match frozen source; three embedded JavaScript checks pass. Full-suite acceptance remains pending. Automatic backup retention is implemented but must only be configured after the new runtime is installed.

Alpha metadata-selection preflight passed while Writer was running, but actual stopped-window stage-15 verification failed: strict read-only Router correctly refuses cold WAL databases without existing WAL/SHM. The helper restored its three owned targets and original Writer plist; its baseline stage-14 check hit the same cold-WAL condition before restart. Root independently proved all restored config/plist bytes and target absence, then initiated baseline restart and exact running stage-14 verification. Failed execution log and activation backup are preserved, no activation success receipt exists. Next: finish baseline health, repair and test real cold-WAL verification without weakening production read-only behavior, then repeat reviewed activation in a new packet.


### 2026-09-11 11:01 UTC — baseline recovery verified; R11 acceptance rejected

Alpha baseline recovery verified at 10:58:54.562988 UTC: Writer 4476, Controller 4487, Control 4491; exact stage-14 verification SHA `78a5d50368d512363254c3c7254c10cd62dccbb6ad13251bc9f7acda72853d0f`. The initial 300-second health wait failed while synchronous startup backup ran SQLite integrity checks. The later observation passed without restarting again; the initial timeout remains recorded. Alpha selection is not enabled.

R11 first full-suite observation: 7,131 tests / 33 errors / 1 skip / 706.362 s. Thirty errors depended on the mutable live OpenClaw repair fixture, whose legitimate bridge installation changed its hash. Three multiprocessing tests recursively reran the suite because the private test runner lacked a `__main__` guard. Root terminated only the proven runner process tree and retained the native result/log/process inventory. Failure receipt SHA `9d8d3d386a6b90bd3eced0e7a9dac3f6afa4441fbc10fb2747b32fdd2a529948`. Frozen-source Agenda edits were independently detected, attributed to the owning agent, preserved in an isolated worktree and precisely restored; the run remains invalid.

Guarded runner regression passed all 65 capacity/workspace multiprocessing tests in 3.115 s. Immutable reviewed host fixtures are now committed without changing production host proof requirements. Root Dossier integration passed 274 tests in 16.512 s; tooling/fixture integration passed 62 in 1.424 s. Dossier dates require exact cited period support; all-unknown answers expose evidence gaps and never become published units. Next: finish cold-WAL activation correction and async single-flight backup/retention, then freeze the combined successor and rerun acceptance from clean immutable source.


### 2026-09-11 11:13 UTC — R11a acceptance running; Alpha v2 restored

R11a freezes `424c22529d5317c4c5c9dc8ba88f37299cea3f49`. Wheel SHA `418bf77f7b5285f9e53e041bdcf9d36463e57a7fa0bd4ce368ded2ec1e50d046`; 572 source/runtime file comparisons and three JavaScript checks pass. The guarded full-suite runner now includes whole-source discovery, native unittest counts and its own artifact hash. Root async-backup integration passed 187 tests / 8.015 s (the first command named two nonexistent test modules and failed; its log is retained). Agenda/adapter/fallback passed 73 / 2.328 s. Agenda's supported/manual path preserves typed post-send unknown results; the live legacy Agenda plane remains retired. Backup failure waits the configured interval before another attempt, and active backup/retention is single-flight off the heartbeat thread.

Alpha v2 failed before target creation because the stopped budget database retained WAL/SHM; automatic baseline restart and health succeeded. Root postfailure health receipt SHA `e8424483f73a15e1e4c485878e1910bb19e1772920024daac41a016926c74e9f`, Controller 10589, 14 model configs. V3 now covers actual retained nonempty WAL from a crashed SQLite process, proving committed rows survive local snapshot replay while all source file bytes and metadata stay unchanged. It remains unactivated pending independent review and actual preflight.

New read-only product findings: current model-spec responses for IBM/CTSH/EPAM exceed the 1,200-character assessment boundary by 33/1/31 characters respectively; prior semantic/filed-concept checks passed. A bounded configurable formatting repair must create a new paid request/result identity and fully revalidate the output, rather than replay the same successful model response or truncate it. Dossier repair targets also do not yet wake or enter the planner state; this feedback propagation is being developed beyond R11a.


### 2026-09-11 11:30 UTC — R11a acceptance passed; Alpha selection live; backup cleanup complete

Clean immutable R11a `424c22529d5317c4c5c9dc8ba88f37299cea3f49` passed the guarded native full suite: 7,164 / 0 failures / 0 errors / 1 skip, 612.02619575 seconds. Full-suite receipt SHA `6e69a4951a66c68a127e9a38c5389928aea538e3b67a54a826e1aa6663362bfa`; native result `13baa84baf6289871a1421ad880ae2e051fb968cbf93aac60d036539b7cb6af2`; log `990e2545d5da0806908f95a1feaebc069560dc1ff2f6d6df25c167be882df269`. Wheel SHA `418bf77f7b5285f9e53e041bdcf9d36463e57a7fa0bd4ce368ded2ec1e50d046`; verification receipt `0fe138681701c7d48642cce082d9912a70477469b4e3d3fa99c1f75fafbc1c41` covers 572 exact runtime files and three JavaScript checks.

Copied-state rehearsal v2 passed 68 schemas / 40 entries / 0 escaped across 13 steps, with current 15-model snapshot and only `backup.keep_latest=3` applied to the scratch service configuration. Binding SHA `e239a8f1d90f4651d81c06d9e43fb731d934fdc7c400b3fa38a67fafc786a4ee`; report `c6175f858b24c36749a67642328e85a3228e470b708b3e386e4079803570e292`. First run failed while writing its report because strict exclusive output rejects the `/tmp` symlink parent; no passing binding was produced. A complete rerun with resolved `/private/tmp` outputs passed. Frozen source remained clean.

Alpha v3 activation succeeded with typed cold and retained nonempty WAL-copy verification: `alphaengine-selection-r10a-activation-v3/activation-receipt.json` SHA `ed0d5b6d0c90f67e96d09f2ba3b3ae5cf54fb6814e8899b22c417c78191badb5`. Stage-15 receipt `4ed6401f954658ec1abbd99f763b59a3ef23da2e0d619939b648cddb410b69c6`; healthy new Writer/Controller/Control 12653/12660/12664. Three owned plan/selection/config files are live; Writer plist SHA `6d7f673616611f7e71cf799d3243d8ce32f881b7221609ddd8c22a1df32717c2`. Existing web-v6, provider bridge, mission v14 and owner budget are preserved. No paid canary or quota reset was introduced; earliest observed normal Alpha expiry is approximately 16:26:44 UTC. An initial v3 running-state preflight failed before writes, its direct diagnostic passed and retry preflight passed; root does not assign an unproven cause. V1/v2 failures remain separately recorded.

After maintenance-generated snapshots, accepted R11a pruning logic independently verified the latest three snapshots and removed four superseded snapshots, freeing 4,137,289,976 bytes. Receipt `state-backup-prune-r11a-predeploy-20260911.json` SHA `5a88952e0e34a6afdc19177149c7def70240e280bf608b048a36c011274fbbaf`. Retained IDs: `20260911T111631.495486Z`, `20260911T110754.373412Z`, `20260911T105130.936886Z`. This supplements the previously recorded deletion of 23 old snapshots (18.45 GiB). Full retained SHA/SQLite integrity checks passed. Two inactive rehearsal state/home trees were deleted after no-open-handle checks, freeing 2,335,615,668 bytes; receipt `r11a-rehearsal-scratch-cleanup.json` SHA `e5f067e63ccb071c35453b13dee7df47ee499b11b7f88dac0df703e6cc72229c`. Evidence/config/log artifacts remain; no replacement archives were created. At root recheck backup directory is 3.0 GiB and filesystem availability 25 GiB.

Next: complete concrete R11a deployment/recovery command review, install the accepted wheel, enable retention and require sustained runtime health before marking deployed_verified. Beyond R11a, `e18054f` / `fa0b59a` securely project terminal Dossier repair feedback into research state and planner signature (88 root tests / 0.566 seconds); they are pushed code, not installed. Exact-target admission currently remains fail-closed while a distinct mission-bound registered-annual research path is implemented; legacy human approvals are never impersonated. CompanyModelSpec repair must use a configurable bounded fresh paid request and full semantic revalidation. No all-company product-completion claim is made.

### 2026-09-11 11:43 UTC — targeted feedback and event constraints; zero-base budget diagnosis

Integration `d9b48f5` / `2994f53` carries exact Dossier target ref/hash through planner inquiry and idempotency. Unsupported directed repairs are visibly held before broad paid-template admission; forged lower-level admissible entries are also rejected. Root ran 120 feedback/planner/task tests (2.282 seconds). The separate mission-bound annual admission and execution path is under review, including actual readable acquired source proof without requiring completion of broad annual reading. This is not yet a live autonomous targeted-repair capability.

Root found event analysis repeatedly failing its 1,200-character `because` contract even though the judge prompt did not disclose that bound. `6a89756` adds validator-derived text limits and concise decision instructions, preserving all semantic/citation checks. A shared prompt-contract revision participates in both the lane's failure/ticket signature and the actual model Work request identity. Existing successful judgements remain excluded; an active child continues to hold the slot. Root 159 Event/lane/budget tests passed in 6.698 seconds; a further six routing tests passed with the added unchanged-context Work identity regression. Independent Sol review found no blocking issue and passed 154 Event/lane tests. The initial source inspection guessed a nonexistent event_judgement_draft module; corrected inspection located the actual event_judgement.py implementation. No runtime source was edited.

Read-only live health at approximately 11:36 UTC passed with Controller 12660 and heartbeat age 2.443 seconds; Dossier and ModelSpec content holds persist on installed R10a. Actual ZeroBase route `route-decision:ec0ec6d4c9de4ae1b3c017255446bc2c` rejected both otherwise eligible brain candidates solely because each estimated $0.191440 exceeds the unchanged $0.12 default. Its two live model configs have no cost override. An exact private candidate in `zero-base-budget-r11a-review` adds only `call_budget.max_cost_usd=1.0` to producer and verifier; configuration validation, setup preservation and actual Cockpit budget resolution pass. Routes and mission daily authority remain unchanged. This is prepared but unapplied, scheduled after R11a acceptance so its 15-file rehearsal snapshot remains exact. Latest-per-policy Router audit found this current ZeroBase policy to be the remaining cost-only rejection; older retired policy failures were kept as history.

R11a generic seven-command runner was rejected as insufficient for live execution: it lacked concrete commands, retained logs and recovery after stopped-window failures. Concrete deployment/recovery wrappers are being completed; services remain healthy on the current accepted release. ModelSpec structured-output repair candidate also received root review findings requiring exact cross-mission/purpose/text provenance and compatibility with normal capacity/route recovery before merge. No unreviewed repair is activated.

### 2026-09-11 11:58 UTC — current host rehearsal and reviewed foundation followups

A concurrent external operation changed OpenClaw web search to Antigravity: full config SHA `f45fa4698e55e6404f2558b2aa5f558bd906fd68f1d2e8c0914c475bd5b6a9a6`. Root verified the exact five changed JSON paths and unchanged model catalog/model-broker entries; no actor is inferred. Current bytes are preserved. R11a copied-state rehearsal v3 passed all 13 steps, 68 schemas, 40 entries and zero escaped calls using unchanged 15 model configs/keep3 candidate and the new OpenClaw snapshot. Binding SHA `7345c0a2cdc826e5ddbeb4ed08a7c9afd21b11c43576f5e7da91dccbba6fb955`, report `c4f73be8b65f0f46f5406891f7e6aee8b3d11e378db70750f797de2dd3f1d1fd`. Prior rehearsal bindings remain historical. After no-open-handle checks, v3 scratch state/home were deleted directly (1,173,459,248 bytes), retaining logs/configs/acceptance proof; cleanup receipt SHA `21e6592b9daaf03c6f7fc90dc0ec262084779d5b4d4f82eae16d0116eab6f402`.

This rehearsal does not establish live web-search compatibility. Source inspection found the current Dalton result normalizer still requires provider/trust-label `gemini`, whereas the new live websearch LaunchAgent explicitly sets provider ID `antigravity`. A versioned configured-provider compatibility change is assigned separately; legacy Gemini raw receipts must retain their original verification. Root issued no search call and does not present a synthetic/real-source contract mismatch as a paid runtime failure observation.

Reviewed mission annual-research authority merged as `4042d47` / `f72d0eb`, with precise negative assertions in `479b10a`. It binds signed mission, company, exact Dossier target, planner inquiry, actual acquired readable SEC source, explicit retrieval limits, model configuration/profiles and budget-policy ceilings. A broad-reading completion proof is optional; no human approval is manufactured. Root's corrected six-module authority/research-plan/migration suite passed 44 tests / 23.511 seconds. The first root command named nonexistent registered-annual and bootstrap test modules (35 loaded / 2 import errors); that log is retained separately. Actual autonomous dispatch and before-send balance admission remain the next implementation slice.

ModelSpec structured-output repair merged as `492b106` / `98201d6`. Root's seven-module integration passed 154 tests / 9.009 seconds, including the mission annual authority. The repair rechecks exact parent Work/result/formal receipt, mission/purpose, state/task/config/text identity, contiguous ancestry and reconstructed prompt; normal capacity/route recovery remains usable. Scalar type changes are rejected. A private next-release configuration candidate adds only `structured_output_repair.max_attempts=1` to `initial-screen-model-config.json`, before SHA `504967b6a35b0ffd73c39514db18dffb125183cfff7ae4ec074bf3d63eb9b805`, after `594be4afc33bc4cde9dab389e5daaf447f8267f37bd7d7168d14ed2bf62ebf21`. It passes new-runtime validation/setup preservation/request rekey checks and remains unapplied; R11a does not yet accept this option. Source and policy activation must be accepted together in the successor release.

Concrete R11a deployment wrapper is in final root review. An initial root tooling test run intersected the agent's followup edits and failed one rollback fixture (`service_config_before` absent); no deployment occurred. The wrapper must pass final exact-version tests and preserve concurrent owner config/state/DB/plist changes before root executes it. Original main's four unrelated DeepSeek edits still have identical diff SHA `43832e8e6d32cec9bf48b59044d200df0d57a99d30598859c23d4c1360207932`.

### 2026-09-11 12:22 UTC — all-document directed research requirement and R11a deployment

Owner clarified that directed research must cover every connected document source, including sell-side research, other company filings, sales notes, wiki and web pages. Claims are derived knowledge, not a replacement for original material. The required architecture retains original bytes where available, normalized readable text, exact source/version/hash and locatable citations; it supports new question-driven retrieval and expanded/full reading after initial ingestion. A metadata-only or summary-only item must explicitly report missing full text and acquisition/import requirements. Existing source access and retention authority remains applicable. The current annual-report implementation is the first source adapter, not the boundary of the product capability.

Parallel assignment: annual_plan_contract audits each actual source-to-storage-to-read path and designs the shared document registry/search/read interface with non-annual tests. annual_unknown_recovery completes the annual mission execution/canonical promotion path and isolates SEC-specific authority from reusable Scheduler/model/verification/staging components. agenda_transport independently reviews that execution path. No assertion is made that all current source adapters already retain complete searchable text. Next acceptance requires a new question to retrieve an original passage omitted from existing Claims, with exact source/version citations, for non-annual materials as well as annual reports.

Root rejected the final ops packet's renamed `wheel.whl`: pip requires the distribution/version/tag filename. Tooling commit `fc52e7d` preserves the accepted wheel filename and adds a staging regression. Root's three tooling modules pass 24 tests; a real pip dry-run accepts the stable packet wheel. Stable packet is `foundation-followup-r11a-release` under the owner activation directory, manifest SHA `0c124766f04abf5c6821041ac02e429d644a092af2b2df608e61ff359a8e3a3f`. Exact current live preflight passed. Authorized deployment is now running with private full logs and verified fresh rollback; continuous health acceptance and publication remain pending.

Configured web-provider support merged as `64a539ed` from `6f04e11`, preserving legacy Gemini identities/raw replay and binding new provider contracts through service/installer/launcher/Core. Exact broker provider mismatch becomes terminal without producing a SourceEnvelope. Agent reports 176 Python and 16 broker tests; root integration tests are running. This successor feature is not part of the frozen R11a deployment.

### 2026-09-11 12:33 UTC — installed R11a and all-document source audit

R11a installation succeeded 12:20:41–12:21:52 UTC. Deployment log SHA `605cfb2f265d3311d322d41981ff3d811249e4a048aa7ad23c6f6ea5461d4ef8`; installed verification SHA `bfb12cafece99ccf5c14540949f6ffceb0aa0c232724bc4aa5ecaa1e2150aeb3`. All 572 runtime files and 15 model configs match; current OpenClaw/provider tree, mission v14, model routes/budget policies, web-v6/Alpha v3 and disabled ThesisImpact are preserved. Backup config now has keep_latest=3 with the existing 86,400-second interval. Its first deployment-start snapshot automatically superseded one old snapshot; actual directory still has three snapshots.

The first health observer attempt failed in tooling: resolving the venv Python symlink launched base Python, which did not import Dalton; it did not produce health JSON. Root stopped that observer, retained failed samples, fixed interpreter identity and preserved raw stdout/stderr on parse failures. Root tooling checks pass 25 tests. Original accepted packet helpers remain unchanged; the separately hashed `observe_r11a_health_v2.py` (`f684458d09a14762a007616b50383df0cd958dc8e4a8512d30e67951641b7d9f`) starts a complete new 45-sample/660-second observation. Direct runtime health and all v2 samples so far pass on Controller 30266. Final publication is still pending; no partial observation is called sustained acceptance.

Root Web provider integration passes 118 tests / 8.425 seconds with an absolute source PYTHONPATH. The first run used relative src and failed one child-process test (118 / 1 failure); preserved log, corrected command and full six-module rerun pass. No source change was required for that failure.

Source audit identifies separate existing full-text authorities: Alpha paginated acquisitions; public-web/SEC fetched and rendered bodies; sales-note/wiki/prior-research normalized feed text. Metadata-only indices/searches are not full documents. Guidepoint's licensed Q&A excerpt is explicitly not a full interview. Prior-research original binary artifacts are recorded in extraction output but not fully bound into the feed manifest; that is a remaining provenance gap. Existing DocumentIndex and Cockpit projections do not expose a complete original-text research path. Shared read/search registration starts with sales notes and wiki, with source version/hash/offset proofs and exact access/completeness distinctions.

Independent annual-executor review reproduced three blockers before merge: forged paid prompt/source metadata accepted under a valid admission; stage-4 formal replay could claim resolved/canonical output with no staged candidates; and staging before lease claim plus wall-clock-dependent candidate identity broke crash/retry convergence. These candidate commits remain unmerged and uninstalled while the author closes each issue with regressions. Production CLI/launcher/normal-planner admission wiring is assigned separately; a tested callable library alone will not be described as runtime capability.

### 2026-09-11 12:49 UTC — R11a accepted; non-annual reading and planner strategy

R11a `424c225` now passed 45 healthy samples over 671.149 seconds on Controller 30266; health summary SHA `741dad25e09ff59ba26a01219312471deebfbb386c274fa0ea23a304ebc6b702`. Fresh post-observation installed bytes/configuration/model routes/budget/mission/source/plugin checks also passed. `current-release.json` and release-specific publication receipt SHA `627be5581943b784b6217769fc9d91b1cde41adff2a57666c78cf66ece0a55d4` identify the deployed release. Legacy R10a manifest remains unchanged and historical; its old deploy-wave4.zsh now refuses before service changes when a current pointer exists (zsh syntax and actual refusal verified). Deployment acceptance is runtime_health_only, not all-company research completion or a human governance signature.

First publication attempt correctly refused a concurrent OpenClaw change before pointer writes. Read-only diff showed exactly `/skills/entries/company-deep-dive-mx0829v1`; all model, broker, web and non-skill configuration was unchanged. Current full host SHA `69b03ce222843674295d63411eaac69800afea7593d6f160557a8445d0d40c45` is preserved. A separate exact transition review SHA `9388699659166e6a322b9f282394bd90dbffe248323cc03e1798ea91bd1fa312` binds those bytes and unchanged plugin tree; the post-observation verification used this reviewed host state. Original deployment artifacts are unchanged.

After release acceptance, exact before-byte checks installed only call_budget.max_cost_usd=1.0 in the two ZeroBase producer/verifier configs. Activation receipt SHA `1571044f8a9fe58ce4f2446e1811d20fb6ba73dc625a2127a9f816d6688f7ac9`; current runtime config pointer SHA `5af250b1b46006440fdcfb55e4653a207c17008ca73382d3956d0074dfebcb4a`. Other 13 model configs, routes and mission daily budget are unchanged. Direct health still passes; fresh normal-lane model result remains pending. ModelSpec structured repair configuration remains unapplied until successor code acceptance.

Non-annual document reading merged as `cfb4fe08` / `a6b10c56`: sales-note/wiki exact normalized text registration, question-driven lexical search and range reading, version/offset/hash citations, source re-verification on each replay. Root 23 tests pass / 0.189 seconds. Root review caught latest-ticket lookup invalidating old registered versions and unbounded accumulation of every term match; fixes pin the exact original acquisition ticket and lazily merge bounded results. Old versions remain readable after a new acquisition. Alpha/public-web and Core-acquired SEC adapter work continues; no live universal-reading claim is made.

Planner strategy `f65ca469` exposes source-neutral directed_document choices from each company's readable_documents and configured query bounds, allowing a new inquiry without a Dossier failure. It binds document/version/query, allows translated keywords, refuses path/model/source overrides, and keeps unsupported directed work out of broad paid templates. A rationale-only edit does not purchase a duplicate query. Root 137 planner/task/CLI/launcher tests pass / 2.751 seconds. This is the parser/state contract; actual verified inventory loading, normal admission dispatch and canonical outcomes are still being connected.

### 2026-09-11 13:20 UTC — actual ZeroBase results and source-neutral inventory

After the $1 single-call activation, normal queue runs produced independently verified ACN, EPAM and DXC ZeroBase reviews. Root checked their exact Core hashes against Scheduler formal ResultEnvelope outputs and invocation refs, stored Works and ModelRouter decisions. Producer family was openai-gpt-6 and independent verifier google-gemini-3. The private normal-lane-verification.json SHA is `01308bf604fc5d92d481ffa4e18a0a74e8389471dd49b1f3fe9fb3c6c2657706`; current-runtime-config.json now has SHA `12044dfe7906e20896cde31d54f03c57833f94d0d43669f867910c3c0b0f8f0c`. These Cockpit invocations are embedded in Scheduler formal results, not standalone Core model_invocations rows. IBM's new review was refused because its cited ClaimVersion was absent from the entire stored Work/question; that source check remains in force. This is execution/provenance verification, not a claim of full investment-content quality acceptance.

Source adapters now cover complete acquired Alpha pages, deterministic fetched web/SEC text, sales notes and wiki. Registry 0.2 separates logical document identity from content version and binds exact acquisition tickets, raw/normalized content and access policy. Pure read-only manifest readers and RawSpoolReader/ReadOnlyConnectorReceiptReader avoid mkdir/chmod/schema migrations during inventory work (29 related root tests passed). The generic Core-acquired adapter binds mission/company from actual Core rows for all supported sources. Exact completed SEC 8-K coverage proves the API is not limited to annual reports.

Root inventory/planner integration `2319b824` passes 96 related tests. All acquired rows in the active mission are examined without a hidden document count cap. Only verified registrations enter readable_documents; unavailable originals have explicit reasons, and full registrations stay in an internal map. Configured source/read limits and exact document tickets change the planner wake signature even when row counts are unchanged. The registry capability is separately exposed to the admission host by `4c65491e`. This code is not deployed yet.

The first live read audit used the wrong spool directory and found no raw objects; the actual installed Writer points to state/transcript-spool. That audit is retained as a failed setup attempt, not source loss. A corrected read-only audit is in progress. Independently, 65 Alpha and one web historical acquired row have no ticket_ref, and the new strict Core adapter currently rejects them. The compatibility fix must locate a verified completed acquisition at initial registration and pin that exact ticket for future replay, without rewriting Core rows or using latest-ticket lookup on an old registered version.

Annual executor review blockers are closed in `1784b2fa` and broker-safe IDs in `b8a731ba`; canonical candidate promotion merged as `45e05b07` plus `85a9ba48`. Independent review caught self-consistent staged proof tampering that did not match Scheduler formal outputs; the fix requires both draft/verifier exact formal output equality and invocation binding. Root registry/promotion regression passes 33 tests / 15.732 seconds. Candidate staging/promotion still does not mean the Dossier gap is resolved.

Next: close generic mission document admission identity (rationale-only/new-plan replay must not buy a duplicate execution), exact company ownership, production admission/child/Writer routing, and automatic known-not-sent capacity/day-budget recovery. Unknown post-send failures must retain their accounting barrier. Complete original-container binding for prior-research imports. Then freeze successor code, run full tests, exact wheel/current-state rehearsal and deploy with current host SHA 69b03ce2 and the two activated ZeroBase configurations. Preserve the accepted R11a rollback until the successor is accepted.

### 2026-09-11 13:39 UTC — 100 originals verified; concurrent spool loss fixed

Corrected real-state inventory now verifies 100 complete originals: 89 AlphaEngine, 5 SEC and 6 web-search acquisitions. By current Core association: IBM 25, CTSH 25, EPAM 3, ACN 24, DXC 23. These counts mean readable source versions, not relevant-company attribution, valid transcript quarters or satisfied stage requirements. Each planner item now has a bounded, verified opening excerpt plus existing review state/rationale; the prompt explicitly separates source readability from relevance and treats excerpts as quoted material. Preview length is configurable. Root 97 planner/inventory tests pass; 78 plan storage/Cockpit/task tests pass after updating old fabricated-hash fixtures and adding an altered-plan hash rejection test.

Private read audit `document-research-readonly-inventory-20260911-v2.json` content hash is `c2c4967717ce6409cc0c901d3011cc1398efcf4b664f586b6da1878a6db2359f`. Config hash `a492eec50e38059028f8787e91b9f61c325fa8641a9eee2ea4f178d8980dae22` is an audit-only policy in the owner packet, not activated live. Registry fixes include ticketless legacy rows pinned to an exact completed download, and source:web-search→source:public-web translation backed by the exact discovery/envelope. The first alias patch still failed live because mission carry-forward preserves an ancestor discovery_ref; `ee713ca8` validates the same mission's immutable ancestor chain instead of requiring discovery in v14. Four acquired web rows remain explicitly unreadable: two csimarket bodies start with a UTF-8 BOM but declare windows-1250 and contain invalid UTF-8 bytes; one lacks completed ticket files and one lacks a completed acquisition ticket. No decoder guessing or invented original text was introduced.

A separate concurrency defect was reproduced in RawSpool: one instance's gc_orphans deleted another instance's active partial, and the active sink then failed finalize with FileNotFoundError. `daeaaf73` adds a cross-process directory coordination lock plus an activity lock on each sink, keeps that lock through object publication, and collects only unlocked abandoned partials. Real subprocess tests prove active download preservation and cleanup after process exit; interleaved collection at publication proves there is no close-before-link gap. Root 22 spool/transport/read-port tests pass / 0.486 seconds. Completed objects remain outside orphan collection. This fixes source download loss; it does not claim that every concurrency/capacity mechanism is complete.

Generic model authority is separately configurable and represented in Cockpit selection (`3445499c`); two future config names are mission-document-draft-model-config.json and mission-document-verifier-model-config.json. They are not yet installed. Source-neutral four-stage execution is integrated as `4e26bb40`: source replay, directed draft, independent verifier and lease-owned candidate staging. Planner formal authority review caught both injected plans and a mistaken dependency on nonexistent standalone Core ModelInvocation rows. Live Cockpit planner formal results reside in scheduler.sqlite with embedded invocation refs and Router decisions; `b79abd84` follows that actual topology. No fake Core invocation rows are added. Candidate staging still is not canonical promotion or Dossier resolution.

Next: typed query-miss/insufficient-evidence outcomes, planner feedback/wake, independently verified generic canonical promotion, safe automatic recovery after known-not-sent capacity/budget blocks, complete production CLI/Writer routing, and prior-research raw-container binding. Then freeze and validate the complete successor release before deployment. Research planning must learn from attempted questions even when no new Claim was written.


### 2026-09-11 14:08 UTC — original read-back and production integration findings

Typed query-miss/insufficient-evidence/candidate-stage observations now reach company research state and planner wake. A real executor insufficient-evidence regression performs one draft, no verifier and no candidate staging, then reads the actual observation into planner input; the 99-test root integration passes / 18.315 seconds. Missing evidence is not equated with evidence of absence, and candidate staging is not a resolved Dossier requirement. Canonical promotion and automatic bounded recovery remain separate pending integration work.

Prior-research manifest 0.2 (`e4dade2f`) retains original container bytes and independently binds the deterministic normalized text. Rereading verifies both original and rendered content; old normalized-only manifests and incomplete/truncated renderings cannot represent complete originals. `a6d42021` enables this verified prior-research adapter in configured planner inventory. Historical notes/wiki retain their received full text and honestly label the lack of a separate source container.

A real source audit found that registration did not apply the schema/text check used by search. One HTML body rendered to zero characters yet was advertised as readable. `94227ca9` requires nonempty original text, exact byte/character/hash equality and a valid registration before either source registration or Core acquired registration can enter inventory; it also rechecks those properties at read replay. Root 33 document/strategy/inventory tests pass / 1.687 seconds. The previous 100-original count is superseded by **99**: 89 AlphaEngine, 5 SEC, 5 web; IBM 24, CTSH 25, EPAM 3, ACN 24, DXC 23 by current Core association. These remain readability counts, not relevant-company or stage-completion counts. Five web records have explicit failures: one empty rendered original, two conflicting/invalid UTF-8 bodies, one missing completed acquisition files and one no completed ticket. Audit v3 content hash `ab70e87b93f065e0f22f60dc141c0c03448c590044ef447d1830f203da6492ad`; old v2 is preserved.

Real read-back of AlphaEngine document `130000110511092` (34,225 characters) and SEC filing `0000051143-26-000010` (212,068 characters) searches a newly supplied revenue-recognition/client-demand question, rereads the matching range and replays both proofs exactly. A SQLite authorizer rejects any Claim-table access; no Claim read was attempted and no model was called. The same query returned no match in the five readable web documents, which is recorded as query_miss rather than inability to reread them. Private original-readback receipt content hash `5f8c4effb50220f7ef31f194c8c9dafbdfa93dc7d566b9135c9ac539061fd9c0`. This proves original access independently of extracted Claims, not correctness of an investment conclusion.

Production preflight/UDS checks surfaced additional real integration seams. Workflow capability refs were being treated as Router semantic capability tokens; actual profiles advertise research/verify. The fix is being integrated with exact Work declarations, preserving stored Work hashes and Cockpit-selected models. Generic verifier purpose must explicitly participate in the existing output protocol, and public-web directed-document staging must use its own artifact contract. Independent review also requires formal route/invocation/output replay and exact executor/staging/observation authority; those fixes remain pending. No synthetic test success is substituted for these production checks.

`dce1e537` derives an otherwise absent execution elapsed default from configured queue, call, transport retry and provider retry windows. Explicit general/purpose limits and packaged annual 7,200-second policy retain precedence; too-short explicit limits remain visible errors. Root 21 budget/authority/admission/inventory tests pass / 7.673 seconds. Private prospective generic configs set single-call cost to $1 and timeout to 600 seconds, with existing route choices and retry configuration; they remain inactive until complete successor acceptance.

Next: finish and independently verify production dispatch, exact canonical promotion, known-not-sent recovery after actual resource refresh and observation replay; then freeze the successor, run the full suite, exact wheel/current-state rehearsal and deploy under existing owner authorization. Preserve the currently accepted R11a rollback and unrelated DeepSeek/host edits. No human research signature is implied by development/deployment authorization.


### 2026-09-11 14:50 UTC — production recovery and transactional promotion review

Actual Router preflight now accepts all five selected draft/verifier profiles using semantic research/verify capability tokens. New annual requests freeze the semantic token in request identity; historical requests retain exact Work bytes. Generic production uses immutable admission references and server-owned original/source/model configuration. Prospective activation contains five exact config changes (two generic models, document inventory, enabled processing lane, initial-screen structured repair); these are still not installed.

Recovery integration `26aee9a4`, `7d7d873d`, `88774f3b`, `521ce55b` and `5fd39977` requires exact Scheduler owner/formal, Router endpoint/family/capability, invocation, source and accounting proof. Actual cost requires matching settlement; estimated cost retains its full reservation and exact estimated usage/cost receipt. Known-not-sent capacity/day-budget blocks use configured fresh Work identities and due-time holds; unknown post-send remains blocked. Waiting admissions no longer prevent independent admissions from progressing. Agent production regression uses a real UDS broker: three BUSY attempts, typed wait, fresh Work and independent verifier, with five physical calls and settlements [0,0,0,10000,10000]. Agent focused production/lane/recovery set: 58 PASS / 33.815 seconds; public accounting proof set: 27 PASS / 27.013 seconds. These are focused checks, not final release acceptance.

Intermediate frozen source `dcab723fcc8e2d19ce4bc225b9023ef6cc0d244f` ran 7,330 tests / 695.872 seconds with 3 failures, 1 error and 1 skip. Log SHA `fddee70f9de1b9d2acba4c4781a40417edfbf649800e7172b67f99d04afae9b7`. It was not deployed or accepted. Failures exposed a real-UDS fixture missing its semantic capability, two missing Cockpit lane labels, processing-lane ordering expectations and four unregistered migrations. Corrections are integrated; installer bootstrap mapping has also been checked separately. Broker precheck passed 32 model-broker and 16 web-broker tests. The new complete frozen run remains required.

Root generic canonical promotion now uses an exact in-process executor capability, full original search replay, both independent model proofs and exact staging bundle under the existing document auto-commit rule. A candidate supplied through JSON cannot assert this capability. Initial wiki-original promotion/idempotency checks passed, then independent review required the complete accounting chain and promotion proof to be written in the same transaction as Evidence/Claim/decision. A fault test exposed mission authority constructors implicitly committing through executescript during resolution; that read-only boundary is being repaired before claiming atomicity. No human governance rule or mission permission is expanded.

Model configuration clarification: OpenClaw remains the upstream catalog source. Broker startup reads plugin configuration; Dalton's hourly mission_model_catalog lane reconciles provider definitions, prices and broker profiles into versioned Router authority, which Cockpit selects. This is automatic catalog synchronization, not a reread of the entire OpenClaw config for every call. Provider catalog changes should flow through this chain without model-specific Dalton business changes. Routine workspace collaboration bookkeeping is not a user-facing model requirement.

Next: finish read-only in-transaction authority replay and atomic promotion fault tests, register its migration, integrate executor canonical outcome, independently review and freeze the full successor. Run the complete suite, build/verify the exact wheel, rehearse against current-state copies and deploy with reviewed config changes under existing owner authorization. Keep the accepted R11a rollback until successor sustained health and actual installed state pass.


### 2026-09-11 15:10 UTC — canonical original research and restart closure ready for freeze

`c397edac` introduces generic canonical promotion through the existing document qualitative policy. Exact source registration/search proof, four effective Works/formals, independently selected model endpoint/invocation and actual-or-estimated budget/usage/cost proofs bind the staged bundle. Evidence, Claim, authorization and the complete promotion receipt enter one Core transaction. `ace2376f` removes implicit schema commits from mission resolution; rollback fault injection proves no partial canonical Claim survives. `0f5abfcb` / `0129626f` close the internal persist hook against fabricated formal rows and mismatched transaction receipts. The signed mission and policy are rechecked, with no permission expansion.

`ac66e6eb` connects both fresh completion and stored stage replay to policy promotion; `d9e764c0` makes the normal lane revisit staged outcomes lacking promotion under the active document rule. Its real UDS regression crashes after immutable outcome creation, then resumes the actual production child with identical model and budget counts, resulting in one canonical Claim and promotion. Disabled policy still leaves candidate_staged terminal. Agent focused lane/production/promotion: 23 PASS / 22.761 seconds; expanded recovery/router/budget: 240 PASS / 63.230 seconds. Root nine promotion tests PASS / 18.247 seconds cover estimated accounting, replacement Work, JSON authority refusal, atomic rollback, lost commit response and executor restart. One prior 71-test combined run had a single obsolete test expectation of zero promotions after the newly automatic promotion fixture; `3c1d5f1d` now compares the complete pre/post existing receipt instead. That failed run remains recorded; a final full run is still required.

Installer and rehearsal now both name all directed-document schemas (`6b10984d`). Actual read-only policy inspection confirms policy-11 already enables `research-auto-commit:mission-document-qualitative:v1`, so no new human signature is required for this path. The inert private directed-originals-r12a-release packet contains current 15-model baseline, five prospective config changes, source policy/audit inputs, signed mission receipt and current host/plugin snapshots. Actual route/budget/mission/plugin verification passed without live writes or provider calls. Current accepted live release remains R11a; R12a is not yet accepted or deployed.

Next: freeze clean source, run whole-source unittest discovery with native receipt, build and compare the entire wheel file set, rehearse the five config changes against a confined current-state copy, then perform the authorized stopped-window deployment and sustained health/installed-state verification. Publish a new current-release pointer only after that evidence passes; real company deliverable quality remains a separate acceptance task.


## 2026-09-11 15:32 UTC — R12a installed, sustained health pending

Frozen runtime `f9f93e3dbce40f6d6e7b85c7be71b2e3457ba0d7` passed 7,374 tests, zero failures/errors, one skip (714.288 seconds runner wall time). Native log SHA-256 `1de2ca49d359393e98744976a8e19ecde6b9bf0b408ffc81da88aa4ac7b391c5`. Its exact wheel SHA-256 is `38dffea59535250b17591668026373a67c1139619441e629eba840c875404477`; 598 runtime files and three embedded JS checks pass. Confined live-state rehearsal v4 passed 73 schemas, 13 steps and 42 tick entries with no escaped paths. Its exact binding file SHA-256 is `33af6c427384602288a7325a5a80560d2c4bd334a5358a29a810b3b5545c44ce`.

Earlier rehearsal attempts correctly refused the wrong live-root argument, then scratch-path comparisons against the live baseline. Supplemental ops-only `078697a15102c6ecd8735976e9e78fd94024607c` derives and binds a confined transition, compares normalized expected paths without writing live paths into the scratch copy, and fixes successor pointer-chain/restart publication handling. The accepted proof separately identifies runtime and ops commits; no runtime source was changed after full-suite acceptance.

The accepted manifest SHA-256 `b5318d46d4b27e9eec54706ef755cab75a06c11bdb3e4a91294e45c5c76b3eda` deployed at 15:24:41–15:25:55 UTC, exit 0. Physical 598-file installation, 17 model configurations, mission v14, directed-original lane enabled, ThesisImpact disabled and backup retention three all verified. Packet: `dalton-owner-activation-20260910/directed-originals-r12a-release`. Fresh rollback `deploy-rollback-20260911T152443.097427Z` and predecessor accepted rollback remain available. Sustained observation started at 15:29:32 UTC using the actual installed runtime Python; the first observer launch failed before sampling because the development interpreter lacked the package import path, and was corrected without changing installed state. Acceptance requires all 45 samples, one postdeployment controller identity and at least 660 seconds. No current-release pointer has yet been changed.

The current runtime catalog projection reports synchronized public model data. Broker/catalog hot-reload semantics are being independently audited against the owner's expectation that OpenClaw supplies the model directory and Cockpit selects research roles. Forecast audit found two concrete candidate fixes: unselected estimated drivers blocked filed-history readiness, and IBM's valid negative historical tax observation was extrapolated into an invalid flat tax scenario. Independent review will establish that the fixes preserve the actual model exit contract. Numerical consensus remains absent and its source-governance rejection is being traced. None of these candidate findings is described as deployed or as five-company product completion.


## 2026-09-11 15:41 UTC — failed health and actual planner findings; owner UI correction

R12a observation completed: 45 samples / 670.737735 seconds, same controller PID 84003, one failed sample (`0025.json`, 15:35:53 UTC). Heartbeat age was 31.280454 seconds against the unchanged 30-second limit; last tick 15:35:22, next observed tick 15:36:00. Subsequent health recovered, but this is a failed acceptance, not a passed window with a sample removed. Finalization/publication is not run. Exact installation remains verified and both rollback authorities retained. Independent service-stall diagnosis is active.

The normal planner child ran twice after activation and made zero model sends: both returned `CockpitModelError: the question and its context exceed the model input bound`. Generic document admissions/outcomes/promotions remain zero. The new inventory therefore has not yet produced live directed reads. The child loads its sidecar directly while Cockpit writes the `plan` budget to `service.json#bounded_planner.config.planner_call_budget`; the bounded-planner route already has the shared effective-config resolver. This concrete consumer omission is being fixed rather than silently trimming documents or claiming the model executed.

Root `22c190ff` adds a checked, non-provisioning financial-model state alongside document figure counts. Actual read-only replay finds all five companies have current-mission models with 12 history quarters; historical-driver counts ACN 9, CTSH 10, EPAM 11, IBM 12, DXC 12. Zero document figures therefore cannot truthfully mean no numerical model base. Model summaries preserve exact version/hash, each driver definition and history count, current-vs-historical mission, unknown read failures, and explicitly do not certify stage completion. Five new tests and 88 related tests pass.

Integration `759850d6` + `0666739a` incorporates the independently reviewed model gate/history fixes and requires the current sensitivity selection ref/hash plus band-rule ref before stage closure. Old projections remain readable but cannot close the current-rule gate. `aed177a5` fixes catalog propagation within an hour: public OpenClaw catalog changes trigger synchronization on the next controller tick, appending immutable profile versions under stable Cockpit selections. Historical bytes and credential-only changes do not manufacture catalog changes. These are successor candidates, not installed R12a changes.

Latest owner instruction: after the current development pipeline is completed, re-evaluate all earlier expectations and shelved work and implement remaining items. Cockpit is the final control surface for a nontechnical user: keep the useful subtask board and run details, use ordinary language, foreground research progress/judgement/blockers/owner decisions, and move low-level Claim/Evidence details into optional inspection. **The owner then explicitly confirmed liking the current UI style: cancel the earlier full visual redesign and preserve that style.** Content/interaction cleanup proceeds within the existing visual system.


## 2026-09-11 15:54 UTC — owner-approved consensus restored; planner budget fix merged

The owner approved the exact public Yahoo analyst-estimates source at 15:49:48 UTC. The installed checked governance reader returns `approved`, `approved_by=human:lumos`, content hash `535ac526d380459a46aa3947acc6fb83ad107f91c3ad506f0998ff53bc7363d3`. A temporary replay had already proved that the source permission alone restores admission with mission v14 unchanged; no replacement mission or code change is needed. At the normal 15:51 tick, ACN launched and successfully published `consensus-estimate-version:2744c531230be1f69470be26ab8e49f8`, hash `51cebe7e250090072374f521322241899bee51b83998009230eba1bcbc107bda`, from real invocation `connector-invocation:yfinance:58b6e09868b10de7c804cb745bb534cd`. Four mapped periods cover FY2026/FY2027 and FY2026Q4/FY2027Q1. Private receipt: `consensus-owner-approval-review-20260911/live-approval-and-first-result.json`. No manual dispatch, service restart or automation signature was used. Other companies continue in the normal serial refresh queue; one successful company does not establish five-company completeness.

Planner fix `a5076619` now reads the explicitly configured Cockpit planning budget in the actual research child, preserving historical defaults when no override exists. The input projection preserves all document identities and source/version/review facts, discloses any complete preview/proof triplets omitted to fit a smaller configured budget, and refuses before routing if even that complete identity inventory cannot fit. Root financial-state assembly regression `2c118bed` additionally proves that existing model history reaches the real planner state and changes its hash.

Read-only measurement with the financial model projection: current full prompt 223,750 bytes; old 120,000 bound cannot fit even the 134,280-byte identity-only prompt. Current brain routes allow at least 744,000 input units. The reviewed successor config sets 250,000 input / 4,000 output / $3 per planning call / 300 seconds; the mission's $100 daily budget remains unchanged. This fits all 99 previews without projection and covers the configured $10/$50 per-million conservative byte-count reservation (maximum $2.70 at the configured limits). Candidate artifact `planner-input-budget-review/planner-call-budget.activation.json` SHA-256 `6c841ebdeb0876f96419f6935ad7f8a426b28ab6d0ecb2ede2ae69d7f0175f78`, expected service before `24a739b024dc196b68ebf296b7909808e9a51ffd0a560abdd4296a2209ace44e`, after `7dd76ea9c75fc9de6783ae9b29fffb16c9ecb49c3c2ab01aa2c281edb2ecd6ef`. Nothing in this paragraph claims that budget delta is already live.

Controller sampling localized the stale-heartbeat window to synchronous Scheduler expiry scans on the controller thread while projection competes for disk pages. Independent implementation is moving maintenance to one owning worker thread and adding a leased-event index; failed maintenance must still degrade health, and no synthetic heartbeat or relaxed freshness limit is permitted. Successor ops will reuse the existing toolchain with an explicit preserve-existing configuration mode, checking all current 17 models and original-reading files while applying only the reviewed service-budget delta.


## 2026-09-11 16:30 UTC — R13a rejected, period-join repair and rolling quota evidence

R13a `1afbeb76846fad570e35eb15c788f18a43f52227` full suite finished at 16:19:38 UTC: 7,407 tests, one failure, zero errors, one skip, 715.169 seconds runner time. Log SHA-256 `1f8f8fe8ea1d9887e7240f9cbee65e12f54387138aa228f3173b6167361612f7`. The sole failure was an outdated Cockpit mock in the pool-refusal test: missing `budget_for` produced a synthetic one-byte limit and stopped before the intended pool refusal. Test-only `a19a2975` supplies the real budget shape and asserts that the mocked call was reached; 59 related tests pass. This corrects the fixture, not the failed R13a receipt. The initial root focused command also named nonexistent `tests.test_cockpit_model`; the corrected seven-module run with `test_cockpit_model_fallback` passed 190 tests / 32.595 seconds.

The live consensus queue completed all five companies without restart/manual dispatch: ACN 15:51, CTSH 15:56, EPAM 16:02, DXC 16:06, IBM 16:11. The original vendor records and source authority replay pass. New data exposed the old `(metric, period_end)` gap join: stored old sensitivity projections can compare quarterly model revenue against annual Street revenue sharing the same end date. R13a is explicitly not deployable. Candidate exact-quarter/annual mapping, current-algorithm fingerprint invalidation and honest unavailable periods are in independent review. Review additionally requires reusing held historical actual revenue quarters for current-year A/E aggregation and disallows invented EPS result types or label-based metric matching. The repair must follow the formal model's exact result/driver identity and typed calendar.

At 16:28–16:29 UTC, live AlphaEngine count remains 130, but this is a replenished rolling window: latest invocation is 16:26:53.946634 UTC today and oldest still-active invocation is 16:31:40.960088 UTC yesterday. The earlier expiring call allowance was consumed by a new normal queue call. A full count is not proof that the rolling limit failed to refresh. No cap was changed.

Successor ops v0.2 preserve-existing mode passed independent review after exact per-file snapshot, receipt failure recovery, actual setup replay and service replacement race fixes. Root integrated 44 tests pass. A follow-up explicitly binds the newly approved Yahoo file as a generic preserved state authority, with no authority mutation. Final R13b runtime/ops freeze and actual copied-state rehearsal still await closure of the financial period-join review.


### 2026-09-11 16:47 UTC — R13b frozen with filed-quarter consensus comparison

Independent review approved both consensus commits together; integrated as `6a1e52d6` and `6a2631ff`. The final bridge binds exact `result:revenue`, a unique filed revenue anchor, typed fiscal windows and compatible units. It combines available historical actual quarters with formal model estimates; it refuses incomplete years and conflicting overlaps. Label inference is removed. EPS is explicitly unavailable until a formal per-share model exists. The current sensitivity rule invalidates the old annual/Q4 comparison without treating old stored results as current. Root and reviewer inspected five-company read-only preview evidence; all current fiscal-year revenue comparisons are populated without new source calls or live writes. Private v2 receipt SHA `b8e291e6b1f64c68b9bdaf1c79b683389464dd4125a4e711d077e5dd136059eb`; these are unpublished previews, not stage completion.

Frozen R13b source is `6a2631ff489ac888b363cf26e24949171ce61fad` at the dedicated foundation-r13b acceptance worktree, including runtime and ops. Root full-suite runner, wheel verification and full copied-state rehearsal are running in parallel. The preserve-existing transition now also pins exact approved Yahoo authority bytes in rehearsal and deployment; 46 integrated ops tests passed before freeze. The inert packet contains only reviewed preserved inputs so far, not deployment acceptance.

Next: accept this exact source only after the full suite, wheel and actual copied-state rehearsal pass; deploy under existing owner authorization, verify exact installed configuration and observe sustained health. Preserve R12a's failed health evidence and R13a's failed test receipt. After the deployment pipeline is closed, resume the requested owner-instruction gap audit and implementation, keeping the current Cockpit style.


### 2026-09-11 16:51 UTC — rehearsal copy gap isolated from frozen runtime

The first real R13b copied-state rehearsal failed before transition because the inherited baseline copier omitted the two document/lane JSON files introduced in R12a. Preserve-existing correctly refused missing scratch inputs; no live mutation occurred. The failed log/report and scratch remain. A minimal ops-only correction is in progress with independent review, using the existing separate `ops_code_commit` binding rather than changing frozen runtime `6a2631ff` or replacing its running suite evidence. The next rehearsal must use fresh output paths and independently verify the actual live bytes before scratch path rewriting.

The R13b wheel passed exact source/archive/file-set/byte verification for 599 files and three embedded scripts. Wheel SHA `eb05cf3ff0dacfae88fc37e0e96ba7ac204172010d7e0fd124c437a7662bb6f0`; verification receipt SHA `cf153c9f55638318a06e2443dbc1204ff9877afd33a2890f8745e18b0fd950c4`. A fresh read-only planner measurement now holds 110 readable originals and a 242,074-byte complete prompt, fitting the reviewed 250,000-byte budget without preview omission. Measurement SHA `d40e8a641eac42ee50288157edd101a392b84a3aad804de69d09c7edc998635b`. These counts are readability only, not company relevance, telephone-call coverage or stage completion.


### 2026-09-11 16:59 UTC — R13b tests pass; real install preservation still blocks release

Frozen R13b completed 7,437 tests / 1 skip, zero failures/errors, 722.02 seconds; source remained clean and exact. Full-suite log SHA `a5ebdd3c8448c357a11f478ee5901c2bb3a0f66297d0f92769711e48ea5f9748`, native result SHA `9b6aee5e9918a442d031bc1cb8f1d63b9e68eaeefaad3004959e06d36517b607`, runner SHA `2dfd6c8b8e412303eebe7272af86267b64755a3f889ad9c179bedaf99e1ff804`. These successful tests do not supersede the failed actual rehearsal. After the separately reviewed ops copy correction, the second rehearsal reached migrations and scratch transition, then discovered cockpit_setup drops existing `openclaw_config_path`; the installer restores the field while unnecessarily reserializing the entire service file. Exact reviewed service bytes would therefore fail after real installation. Minimal runtime preservation and no-op shell behavior are being fixed and independently reviewed before R13c freeze. No live deployment occurred.

Fresh product baseline at 16:56:16 UTC verifies four actual Dossier v0.3 records with formal provenance (ACN/EPAM/DXC/IBM); CTSH has none. All 12 planner cycles since R12a installation refused at the old input boundary before sending, with no formal writes. Generic original research has zero admissions/starts/outcomes/promotions, so live functionality remains explicitly unproven. Telephone-call gaps persist for ACN/CTSH/EPAM/IBM, with broker-research gaps for CTSH/DXC. Private baseline SHA `cc8f4b0292a08d2ea228174d17a6efe3919daa0c64d8bf6704cf71c7900e3307`, receipt SHA `146062314dcf88342269424f76d88f5df6498b9803f413ebac4f7c58529c2ee2`.

Owner asked whether one brain becomes a bottleneck. Current implementation runs coordination asynchronously and sends work to independently executing lanes; one coordinating plan at a time does not imply one model request for the whole system. The established current blockage is pre-send input configuration, not measured compute saturation. Keep one coordinator initially and examine actual planner elapsed time, task waiting age and new-information feedback latency after successful deployment. If coordination becomes the measured constraint as coverage grows, prefer a portfolio coordinator delegating to scoped industry/company planners with shared canonical records and distinct task ownership. Do not introduce several competing global planners or claim unmeasured throughput. This assessment does not displace the active foundation/deployment work.


### 2026-09-11 17:09 UTC — R13c runtime frozen; finish exact copy coverage

Runtime `4d459e544ad0c580c37be77b702f276e75d8c32b` includes the independently approved Cockpit catalog-path preservation and installer no-op rewrite correction. Its clean full suite is running. The new wheel matches all 599 frozen/archive/package runtime files, compiles and passes three embedded JS checks. Wheel SHA `181a5ed6f2f33677338fb3494e5f28ef69a691f2032c6bd7172a0b3f4609efa1`, wheel receipt SHA `5e520767ef3eb53c9f4f69ea679c725655c1f358247a862c4b12517133d2c7f6`. A real failed-scratch replay with corrected code now preserves all 17 model configs and service/document/lane bytes across two actual setup calls. This is targeted confirmation, not the still-required fresh full rehearsal.

The full installer/copy audit identified two further rehearsal-only omissions: existing model-catalog-sync.json and the owner-modified phase8 ad-hoc template. The actual installer preserves both existing files, so the rehearsal must carry their reviewed content rather than omit one or substitute the repository seed. A bounded successor-wrapper correction is underway with independent review; frozen runtime and its suite stay unchanged under the already supported separate ops binding. The valid external market-digest-output symlink must not cause the rehearsal to copy an external corpus or falsely label it confined. No new runtime defect was established by this audit. R13c has not been deployed.


### 2026-09-11 17:22 UTC — R13c installed; sustained and functional acceptance running

R13c full suite completed 7,441 tests / 1 skip, zero failures/errors, 713.835 seconds. Receipt SHA `6ad4247e63ab7add10a1205a10d5cf9d9cd72693811c26d7ee6022dd8d2ec709`. Final copied-state rehearsal uses immutable runtime `4d459e54` and separately immutable ops `9c038e1b`, with 14 steps, 42 tick entries, zero escaped calls and all exact configuration checks passed. Binding SHA `b2055f1fb01060884b9c5a89cb4497c0b0d6820c5b86c1faa9ea355e8e731899`; candidate manifest SHA `aa996a813f54e92f3adb396523a67324436f0cc50fcc6ce3151f28678fcc71e3`. Independent review rehashed all 16 artifacts and performed a no-mutation live preflight.

Authorized deployment ran 17:19:08.649829–17:20:36.374477 UTC, exit 0. All 599 runtime files matched, all 17 model configs remained exact, the sole service mutation produced reviewed SHA `7dd76ea9c75fc9de6783ae9b29fffb16c9ecb49c3c2ab01aa2c281edb2ecd6ef`, and approved Yahoo authority bytes remained `cbdafb0a...`. Installed verification SHA `01b26731c7367bfe800735e1cf83042759b89cb336a521d1563cfb49cee22f88`. Fresh rollback is foundation-r13c-release/deploy-rollback-20260911T171914.809571Z, database snapshot `20260911T171921.442378Z`; retain until successor acceptance.

Controller PID 6859 started 17:20:02.924201 UTC. Root began manifest-bound sustained health observation using installed runtime modules, without source/src shadowing. First normal planner ticket `research-plan:0113d5277ecbb725155faf1a` launched. Independent read-only checks now follow actual planner budget/formal/plan/admission outcomes and five-company stored sensitivity refresh. Neither a launched ticket nor the initial healthy sample proves completed original research or sustained health. Accepted pointers remain unchanged until finalization.


### 2026-09-11 17:35 UTC — sustained health accepted; real planner transport blocker isolated

R13c passed all 45 manifest-bound health samples over 669.917 seconds, same PID 6859, maximum heartbeat age 5.109 seconds. Post-observation installed verification also passed. Health summary SHA `9adb69d5c30aaf4c1b4c6c68e0ed073800b983f160acad6e9f73597700da2667`, finalization SHA `2f51f3eeaedfd87c2346248c97755da96fc32784713ae4e3c7cab1b20bf4640c`. Current release/runtime pointers were published through the verified successor publisher using fresh expected hashes; publication receipt SHA `b89fe5d0d5b513e3cd9b66d9a84e27e8d45d50880cd5497ab1ed96e89d6b2e8d`. The release is stable, not a claim that every research product works.

The first normal planner Work `work:cockpit-plan-f47603e73c3ed5cc4049c894328d731d` admitted the configured prompt (248,419 bytes / 114 document identities), then failed because its encoded broker envelope exceeded the client's 262,144-byte frame limit. Exact failure has invocation:not-started and no usage refs; no provider call is established. The ledger settled the 2,684,190-micro reservation conservatively, so the earlier diagnostic calling it paid was wrong. Corrected immutable audit v2 SHA `a748caecb5447b4adc26ca41b51ba477e57954b4f077cfafd74794fae0874897`, receipt `86b18f9ad1909de50bd28752e1384629f5a242e4209b206bb9b6b0883e9ac76d`; v1 retained as superseded diagnostic.

Root fix `37b52e0d` / integrated `d00d3346` introduces a request-only oversize exception that also carries definitely-not-sent classification. Real Cockpit + real adapter regression proves no socket creation and zero budget settlement; response oversize remains post-send-unknown with conservative reservation. 77 focused tests / 2.675 seconds and independent boundary review passed. Sol's companion change makes frame bytes configurable throughout producer/verifier consumers with a shared 1 MiB default and effective execution identity. Matching host-broker configuration needs a separately reviewed stopped-window transition; existing role configs can remain exact. These fixes are not yet deployed.

Normal sensitivity runs have published current R13c rule results for IBM and CTSH; both match exact current-fiscal-year actual/estimate aggregation. Three companies remain pending normal turns. Pure read-only join replay passes all five, and EPS explicitly remains unavailable without a formal per-share model. Interim receipt SHA `dbbd608f9e6087383218501bf291dbc7b8658c84584b964e434fb1234968145c`. Continue normal verification alongside the transport fix; do not manually dispatch paid canaries or rewrite old projections.
