# Foundation execution and reading closure — 2026-09-11

This register preserves the ongoing foundation/Dossier/deployment work and the owner’s new requirements. A source commit or passing unit test does not by itself close a row; actual runtime acceptance is recorded separately.

| Work item | Current evidence and state | Completion criterion / next action |
| --- | --- | --- |
| Historical stage vs current source/readiness | API/UI separation merged; acquired/read counts displayed separately | Deploy and check actual five-company page |
| Missing sources classified unplanned by writer | Wrong coordinator.load_plan call repaired; actual socket test passed | Actual stage tick must show configured source gaps |
| Shortfall retry regressed during pagination | Restored configured short cadence, cursor and open-child rules preserved | Same-freeze tests plus real scheduling evidence |
| Planner STOP ignored by acquisition | STOP now constrains fresh/retry selection, exact mission-scoped, rows retained | Confirm surplus web queues stop while other work remains eligible |
| Repeated terminal URL failure | Typed retryability persisted; unknown historical failures remain unknown | Explicit false must not launch again; no broad prose heuristics |
| Repeated failing hosts | Configurable 0.5 plan; guarded append-only attempts; exact source/host cooldown with success reset | Review/activate private 3-URL/24-hour/6-hour candidate, verify cooldown/status and recovery |
| Annual read falsely completed after model failure | ACN 33/33 failed; CTSH 27/32 failed; DXC 38/39 failed, historical Sep10 22:22–22:46 UTC | Newly generated AND cached failures keep review incomplete; 11 focused tests passed, integrated acceptance pending |
| Proof of successful whole-document reading | Formal per-window proof, transaction CAS, CLI publication and full schema install merged; truncated source refused | Deploy and confirm legacy unknown stays unread and a fresh full successful pass becomes read |
| Historical incorrect dismissals | Review ledger lacks resolver identity; cannot infer it from registered_by | Prepare exact review/hash/failure evidence and bounded supplemental repair approval; preserve history and human decisions |
| Capacity and queue | Historical BUSY was broker concurrency limit; current cap8, owner now authorizes higher | Configurable bounded FIFO, duplicate joining, queue timeout before invocation, reviewed cap16 candidate |
| Retry then fallback | Existing capacity retry and chain logic require audit against owner sequence | Transient failure retries same provider before fallback; unknown completion cannot be blindly repeated; exhausted work stays pending/blocked |
| Token ceiling/timeout | Some requests estimated16038 vs work limit16000; no model invoked; extraction config has no explicit call overrides | Exact prompt-budget accounting/window fit, configurable larger reviewed time/token limits without overriding daily authority |
| Directed vs broad reading | Existing annual pass enumerates windows; research-task probes exist | Verify general read coverage AND question-driven local annual/web retrieval, including revenue recognition question; implement missing executor route |
| Hardcoded limits | Source inventory799 matching lines,15 modules use common call/run resolvers | Review all budget/concurrency/queue/retry/timeout/window/cadence defaults and actual runtime overrides; fix unreasonable/disconnected constraints |
| Configurable tracking execution | Installed cadence, write cap, all five candidate scan caps and lookback now consumed; 70 tests passed | Deploy; existing policy hash/defaults preserved; live configuration unchanged |
| Configurable document reading | Source length/PDF pages/window/quote bounds now reach source verification, context and Cockpit; 56 tests passed | Explicit reviewed release config; full-text reread must retain same bound limits and hash |
| Every model prompt and analyst expectations | New owner requirement: inspect producer, context, verifier and semantic identity together | Root handles extraction/ask/debate/events; Sol agents handle foundations/deliverables, planner/quality, and forecasts/earnings/conviction; complete matrix and fix stale assumptions |
| Decisive, testable views and tracking | Owner clarified: understand market thinking and anticipate its next move, rather than force disagreement | State main judgement, conditional A/B triggers and consequences, what changes the judgement, and actual tracking observations; no uncommitted “both possible” prose or fictitious scheduling |
| Probability and payoff | Owner clarified evidence confidence, scenario probability and price-implied payoff are distinct; low-confidence upside may still be attractive when downside is priced | Audit actual decision structures and tracking consumers; no mandatory disagreement/high-confidence gate, no blanket prompt template; foundational work remains first |
| SEC annual index setup | Existing v1 quota50 conflicts with current template200 under same immutable key | Reuse only exact validated stricter existing authority, preserve spend; refuse other drift; no silent budget increase |
| Dossier variant/0.3 product | Variant prompt fix merged; live R8a remains no_v03_records | Same-freeze release, actual new formal producer/verification/provenance and HTML checks |
| Deploy and sustained health | R8a health45/45 passed; next freeze not yet accepted | Full Python/JS tests, wheel bytes, copied-state rehearsal, config preservation, deployed bytes and sustained runtime/product evidence |

The owner explicitly asked to finish these tasks along with prior ongoing work, audit unreasonable hardcoded limits, retain progress/next steps, commit/push, and deploy autonomously. Model and budget choices must remain cockpit/config driven. Human research signatures remain human. Advanced investment-methodology work, final visual redesign, reference-file import/export polish, and multi-workspace activation remain sequenced after foundations; these are not removed from the broader backlog.

## Integration checkpoint — 05:12 UTC

The first R9 freeze is `050a044c18570431bdd7bbf782cd44ff5ef94596`. Its full suite is diagnostic and still running; failures mean it is not accepted. Wheel/source equality passed for 561 runtime files (400 Python, 68 SQL, 90 JSON, 3 HTML) and three JavaScript syntax checks. Root subsequently fixed preservation of explicit reading limits across installation (`10340e8`); this requires a new freeze and fresh acceptance, not reuse of the old suite result.

Transport scope remains explicit: extraction and Cockpit support the reviewed queue/retry contract; other direct production adapter entry points need their own configuration, identity and lease review. Agenda/Human Intent/Thesis Impact/Planner gaps predate R9; different immutable Scheduler policies can coexist in one database. A Sol agent is implementing the Agenda slice first.

The annual directed probe currently proves retrieval into a governed backlog question only. A new independent Sol task is implementing the versioned qualitative ResearchPlan contract and registered-filing retrieval node; model drafting, independent verification and candidate staging must follow through real formal authority.

Conviction v2 candidate code remains under review and is not activated or accepted. Initial review found incorrect omission of earnings-expectation revisions from return sources, incomplete prompt/schema and verifier identities, and insufficient formal governance/provenance/ongoing-tracking admission. These are engineering work to complete before presenting any signature request. The owner explicitly requested methodology to strengthen decisions rather than a universal prompt checklist.

R9 diagnostic suite completed: 6,944 tests in 582.034 seconds, 6 failures, 1 error, 1 skip. Closure: update obsolete prompt/default-budget assertions without weakening bounds; build the oversized preflight input from the actual WorkOrder limit; bind the Guidepoint test fixture to its real excerpt hash; add the missing document-read-proof MigrationSpec and its connection constructor. The 170-test affected suite passed; six migration tests additionally prove the real proof table is created on the shared connection. No failed R9 acceptance has been promoted.

## R9a deployment checkpoint — 05:40 UTC

The unchanged R9a source `b6cbddc1867fd1853f4870a04a777d2f79e1b869` passed 6,946 Python tests in 601.083 seconds (one skip), 29 actual Node broker tests, wheel/source comparison for 561 runtime files, and a copied-live-state rehearsal with 68 schemas, 40 tick entries and zero escaped calls. The accepted wheel SHA-256 is `6f3e21b0a27642a8330b4c18a019e7fcf6afed31c0e5cf9273acd6ad16116319`. The earlier failed R9 result remains separate.

Core deployment finished successfully at 05:32:23 UTC. All 561 installed runtime files matched the accepted wheel and retained source archive. All twelve complete model configuration files matched their predeployment contents. Backup `deploy-backup-20260911T052940Z` and retained source `.release-source.MsUxXg` are preserved in the private owner activation packet. The original main workspace's unrelated DeepSeek diff remains untouched.

A second bounded maintenance window drained in-flight tasks, applied the reviewed document transport settings and broker queue, and restored writer/control/controller. It finished successfully at 05:40:00 UTC. Broker settings are now concurrent 16, queued 64, maximum queue wait 600,000 ms. Document transport adds one definitely-not-sent retry, 600 seconds queue wait and two seconds backoff. This is not evidence that all provider-failure retry paths are complete. New controller PID 96478 started at 05:38:21 UTC; sustained 45-sample health observation began at 05:40:20 UTC and remains pending at this checkpoint.

Private evidence is under `/Users/everflow/Projects/dalton-owner-activation-20260910`: `foundation-followup-r9a-deploy-execution.json`, `foundation-followup-r9a-installed-bytes-verification.json`, `foundation-followup-r9a-config-preservation-verification.json`, `foundation-followup-r9a-activation-execution.json`, and `foundation-followup-r9a-runtime-activation.json`. Live health and actual new Dossier 0.3 product provenance are separate acceptance rows; neither is inferred from installer exit zero.

Next foundation work remains assigned in parallel: (1) returned provider failure proof, independently budgeted same-profile retry and fallback through real workers; (2) Agenda/Human Intent transport configuration and lease consumers, with generic failure envelopes kept conservative until the shared proof exists; (3) registered annual filing retrieval followed by real qualitative drafting, independent verification and candidate staging. Research signatures and historical human/unknown dismissals are not rewritten under operational deployment authorization.

The owner's probability/payoff clarification remains a decision-system requirement, not a universal prompt template. Scenario probability, confidence in evidence, conditional returns and price-implied assumptions must remain distinct. A low-probability but materially underpriced upside can justify a positive recommendation; neither consensus agreement nor modest confidence is an automatic rejection. The eventual decision must explain downside, valuation/earnings/revision assumptions, what would change the judgement and actual observations that update it. Foundation execution is still first; the unaccepted Conviction v2 candidate is not activated.

### Actual Dossier acceptance finding

The R9a post-activation read-only check still found zero Dossier 0.3 records (`dossier-v03-acceptance/r9a-post-activation.json`, SHA-256 `2e074ed9f302c6eb1ff159b1a0d4fcb85796e493862811d3fde95d602f6b4f4c`). The first ACN run after activation (`e82d8c3ac2547bc071d4b1b8`, 05:38:52 UTC) produced three units and received an independent pass, but correctly refused publication with `unit_provenance.business_model.producer authority binding drifted`.

Root traced this to a real caller/validator mismatch: Cockpit bound the mission content hash only for memo purposes, while Dossier 0.3 requires it for both producer and verifier. A new integration fix adds those purpose bindings and therefore gives corrected work a new identity instead of rewriting or borrowing old paid results. A second mismatch appeared only when exercising the actual Router/Scheduler: the verifier request includes a deterministic producer-route suffix, while the Dossier proof stores its semantic request ID. Validation now accepts the exact suffix derived from the formally bound producer routes, preserving legacy unsuffixed records and all other provenance checks.

The regression calls the actual dossier draft and verifier functions through Cockpit, Scheduler, Router and budget store with a fake provider, then invokes the same formal provenance validator used for publication. It checks replay and changed mission-hash isolation. The affected 203-test suite passed. This repair is integration code, not part of installed R9a; deployment and actual new product acceptance remain next steps. Explicit transport/capacity recovery request decorations also need coverage before enabling those options on the Dossier-specific configuration.

### Runtime observation and selected web plan

R9a's first sustained observation completed 45 samples over 671.055 seconds. One sample at 05:49:29 UTC was degraded with `OperationalError: database is locked`; the controller stayed alive with a fresh heartbeat and recovered by the next sample. The last successful sweep timestamp preceded the failed tick. The observation is **not accepted** and its raw evidence remains intact. Root is investigating unnecessary Scheduler sweep write-lock acquisition; no failed sample is removed or relabeled.

The failure-host cooldown candidate also exposed an installation boundary: LaunchAgent rendering still selected the fixed web v3 filename. Integration now supports a hash-bound `web-search-plan-selection-v1.json` alongside the existing SEC selector, so a separately retained web plan can be selected without rewriting historical plan files or resetting to v3 after regeneration. Missing selection preserves the previous default; proposed, source-mismatched, hash-drifted or escaped selections fail before plist publication. Seventeen web/SEC selection tests passed. The private cooldown candidate is not activated yet; activation must bind the exact new plan/selector and leave source permissions and budgets unchanged.

The completed health summary hash is `3457f8764737d88b6841a61ebe6350bca5248fc4b278164c8d302ab64dc5f395`; it is retained as failed acceptance. Source review found that every sweep first acquired SQLite's write lock even when no lease was due. Integration now performs an advisory read first, and keeps the original transaction plus a fresh lease-state check whenever expiration may be necessary. A real WAL lock-contention test proves an idle sweep can finish while another writer holds the lock; a due sweep still refuses contention and later expires exactly once. A competing-sweeper test proves stale advisory reads do not duplicate expiry. Seventy-one Scheduler/service tests passed. This removes a concrete contention source; it does not claim to prove every possible lock holder or that live acceptance has passed.

### First actual complete annual-read proof

EPAM `sec:filing:0001352010-26-000015` produced `document-read-proof:221bd4cfd1dd0eaa5805a28fcc57955b` at 05:47:13 UTC. Its 35 successful windows continuously cover 419,086 characters and end at the complete, non-truncated source boundary. Root's read-only audit rehashed the saved proof, source review, every frozen context and WorkOrder, and every successful formal Scheduler envelope; all window refs, source hashes, offsets, company/document bindings and the final boundary matched.

The proof hash is `40ecd9c7a72ff03301abda00eda4aed9d17dd7b11b8bd167e0b0d8aefb5bc467`; the source content hash is `b7a7cb7a2d7169f740d32f2b712158f731401b1a33c2ac15cd57af3f04690f0e`. Private receipt `foundation-followup-r9a-document-read-proof-audit.json` has SHA-256 `b87bb7656f2cec76f8a1d680c49a4db2779c732108e5595deb20634ba88c45b0`. This is actual broad-reading execution evidence, not an independent rating of the extracted prose and not proof that the other companies' historical dismissed reviews have been reopened.

### Model-spec prompt scope correction

The live observation also showed IBM and CTSH model-spec drafts refused because `horizon.because` exceeded 400 characters. The prompt already included the complete schema, but a recent instruction demanded base case, alternative trigger, impact, falsifier and next observation for each model decision. Root narrowed that instruction to the actual consumer: specific reasoning for each model line/horizon, and the key structural uncertainty in the overall assessment. Field-length instructions derive from the existing output schema; investment scenarios and tracking belong to their downstream consumers. No text is silently truncated and no validator is weakened. Prompt contract 0.4 changes TASK_HASH so prior refusals do not suppress a corrected attempt. Forty-nine model-spec/lane tests passed; actual regenerated outputs remain to be assessed after deployment.


## Telephone-call inventory and historical extraction display — 06:15 UTC follow-up

Owner reported that the Cockpit appeared to have lost every earnings call. A fresh read-only Core backup audit found current valid-quarter inventory ACN 0, CTSH 1, EPAM 2, IBM 0, DXC 4. These acquisition counts match the earlier 03:50 audit. Whole-document `read` is now zero for these historical calls because the new completion contract requires formal window proofs; legacy `extraction_staged` records are still present and were not deleted. The UI had not explained that distinction.

The checklist now exposes `legacy_extracted` separately and labels it “历史已提取（全文待核验）”; it keeps whole-document `read` unchanged. Issuer attribution, consecutive-quarter coverage, current rejection state and per-period deduplication still apply. A newly verified copy of a quarter supersedes that quarter's legacy display without double counting. This does not create proofs, reopen decisions or issue paid calls.

Current live-copy comparison preserves every existing checklist field while showing CTSH 1, EPAM 2 and DXC 4 historical extracted quarters. Private audit receipts: `stage-checklist-audit-20260911Tcurrent.json` SHA-256 `6e5debba33e894108c486098e118385d600c7a830ff849929a781d00bd6ccfe6`; candidate `stage-checklist-audit-20260911Tcandidate.json` SHA-256 `eabc8bf9ff477ffed2913d36a5160f2092be94c6b004aeb681b12d930b57cd15`. Five test modules pass 79 tests / 1 skip; embedded Cockpit JavaScript syntax passes. An earlier relative-PYTHONPATH run failed a subprocess fixture; the same test and full focused run pass with the release-standard absolute PYTHONPATH.

ACN's discovered/acquired rows contain no Accenture quarterly call; IBM's correctly named rows are investor conferences rather than quarterly earnings calls. Historical broad searches also returned many unrelated companies. A separate Sol task is closing missing-quarter selection, search parameters and pagination. Owner explicitly requires relevance assessment before full-text acquisition: use title, snippet, dates, type, tags and the actual research gap; do not make company tags an absolute filter, and retain industry/peer research for its appropriate purpose. LLM selection must consume existing routing, budget and formal work contracts. No new selector or source-plan activation is claimed yet.
## Dossier request-decoration candidate

The follow-up candidate binds an explicitly configured Dossier producer or verifier request to a closed, versioned identity containing the semantic request, the exact transport/capacity retry configuration, the exact producer route references and the immediate recovery parent. Recovery parents carry the authoritative prior WorkOrder and result-envelope hash; arbitrary suffixes are not accepted as proof. Historical successful work remains replayable only under its exact old identity, while historical failures without the new binding do not acquire fabricated recovery authority.

The regression exercises `draft_unit` through a capacity failure and cooldown recovery, then `verify`, Cockpit, Scheduler, Router and the publication-time `validate_formal_unit_provenance` against persisted authorities. It also checks producer-route binding, exact retry configuration, replay and changed mission-hash isolation. This remains a reviewed integration candidate until the full affected suite and release acceptance complete; it has not been deployed and is not live Dossier-product evidence.

Agenda and Human Intent transport work also remains conditional on the shared adapter proof repair. Their real Unix-broker regression covered queued success, while capacity tests supplied a synthetic not-sent proof; the installed adapter does not yet author that proof for a real local BUSY refusal. Live BUSY therefore remains conservatively terminal until the adapter emits a validated local-not-sent proof and a real worker BUSY regression passes. Those entries must not be described as fully closed before that acceptance.

The Dossier review hardening follows every recovery parent to its root and verifies the immutable WorkOrder, result receipt, ResultEnvelope, semantic request, prompt, mission, retry configuration and producer routes at each edge. Route admission resolves its rejected Router decision and changed-policy proof; operator recovery resolves its controlled-redrive record; capacity recovery requires adapter-authored definitely-not-sent proof, the configured cooldown, the capacity-policy hash and a contiguous epoch sequence across intervening route/operator recoveries. A pre-binding root is allowed only for the exact default configuration and exact historical semantic/producer identity. The default live configuration path now has a regression covering all three default Scheduler attempts, cooldown recovery, drafting, independent verification, replay and publication-time formal provenance validation. Historical successes are replayed only after the persisted WorkOrder, formal record, ResultEnvelope and result receipt validate exactly; pre-binding configured records that lack enough metadata for final provenance are not borrowed. The affected 236-test suite passes in the candidate worktree. This remains unmerged and undeployed at this checkpoint.

### R9b acceptance failure and R9c preparation — 06:45 UTC

Frozen R9b `e68171f985baab8c6f684d2d667cddf82b6a8184` was not staged or deployed. Its full suite ran 6,960 tests in 576.223 seconds and failed with two assertion failures and five errors (one skip). The immutable failed log SHA-256 is `a5f182c6c1091360c02efdc3997a625e14fcc9ad0aa24ca86e4d1f893fdf2128`. Wheel byte verification (561 files and three JavaScript checks) and copied-state rehearsal (68 schemas, 40 entries, zero escaped calls) passed for that source; they do not override failed full-suite acceptance.

Six failures came from the old hand-built Dossier provenance fixture: it put producer-route metadata on verifier work but omitted the producer-route suffix that real Cockpit calls persist. The fixture now retains a semantic provenance request ID and generates the exact persisted verifier request ID, as production does. A tampered producer list is rejected at that stricter request binding. This does not broaden production acceptance or invent legacy authority. The remaining assertion still demanded generic investment scenarios in the model-specification prompt; it now checks the concrete model-structure judgement and observable structure-change condition, consistent with that output schema and its downstream consumers. All 16 tests in both affected modules passed.

Next: freeze a new R9c commit, repeat full-suite/wheel/rehearsal against that immutable source, and use the reviewed core-only deployment preserving the already installed 16/64/600,000-ms broker queue and all twelve model JSON configurations. Keep both R9a failed sustained-health evidence and R9b failed full-suite evidence explicitly bound in the next review. Actual Dossier 0.3 publication and continuous healthy runtime remain unaccepted until measured after deployment. AlphaEngine asynchronous search selection and directed annual-report four-node execution continue in separate worktrees.

### Budget counter and failed-host activation preparation

A further hidden listing limit was found outside the R9c freeze: source-call reservations counted only the first 100 open discovery dispatches and the first 100 open document acquisitions. Increasing configured budgets/concurrency could therefore undercount pending calls. Integration now uses one source-filtered SQL count over both complete pending sets, including children launched under superseded missions, instead of treating a paginated display API as accounting. A regression creates 105 real governed dispatch records, verifies all 105 reservations reduce the configured budget, checks source isolation and settlement release.

The private failed-host preactivation audit at 06:45:46 UTC found one qualifying host: `www.investing.com`, three distinct terminal-failure URLs, cooldown until 12:08:56 UTC under the proposed six-hour rule. Two retryable failures and three unknown failures are excluded from this terminal-failure rule. Candidate selection preserves the prior plan's search specs, source scope, static host lists and 1,000-call rolling budget. The immutable candidate/selector are prepared but **not activated**; do not report the currently installed coordinator as already enforcing cooldown. Audit SHA-256: `00850bc755c91cfea0c2e0686788b65b7b2f12aa7512db89ad67382fbaaa4965`. Next: finish R9c sustained runtime acceptance, then apply the bounded plan selection and verify actual coordinator exclusion/recovery.

### R9c installed, sustained health pending — 07:01 UTC

Freeze `c29b3e22808cb900b458eeb00d9e5447b72f5db6` passed all 6,960 tests in 568.156 seconds (one skip); full log SHA-256 `103c849b4d852b8b79a823a1eaaecad2729783a164e618a336c3c811eb762481`. Wheel SHA-256 `3321885f6800e7c065a1fdc37b888e9eb3bc8204c46ffd44742d8f611ad94412`; copied-state rehearsal binding SHA-256 `830cf0b0da316779f57bcfe367c9b3cbc2a8ca4cf70eea60a3f394b44f58cc27` (68 schemas, 40 entries, zero escaped calls). R9b remains a failed release candidate.

The reviewed core-only deployment ran 06:54:35–06:59:07 UTC, drained in 81.612 seconds and exited zero. Retained backup: `deploy-backup-20260911T065436Z`; exact source archive: `.release-source.JIUVf9`. All 1,887 archived tracked files and 561 runtime files matched the accepted commit/wheel. The twelve complete model configurations, current 16/64/600,000-ms broker queue, broker plugin bytes and historical unresolved invocation were preserved. Installed byte receipt SHA-256 `de2d21f7cb7cc79d126cf7116d6115ceca6c1b9ab0b47266b1c25a04d9b4b5a4`; configuration receipt SHA-256 `5616865c15f4118ca09feb8a27f6c9e5a9f87e8f73a9d2c8ea9fb54f7243c09a`. New controller PID 18253 was born at 06:56:41.033839 UTC. The 45-sample observation began 06:59:58.970062 UTC; runtime and actual Dossier product acceptance are still pending.

Outside this freeze, integration now contains asynchronous AlphaEngine candidate selection through real child/Unix broker/Scheduler acquisition tests. Root added the missing process-independent Cockpit purpose/configuration registrations, so the new selector can appear and bind its actual configuration even before any child module is imported. All 156 selection/model configuration/Cockpit routing affected tests passed. The earlier hidden source budget counter repair passed 22 discovery plus 68 web/stage/fetch tests. Neither change is part of installed R9c. Next: complete selected-page/query-variant handling and review the annual qualitative/provider-retry integration before the next freeze; activate the separately reviewed failed-host policy after R9c health acceptance.


### R9c sustained health and first actual Dossier accepted — 07:12 UTC

The 45/45 samples from 06:59:58 to 07:11:15 UTC were healthy on the same postdeployment controller PID 18253, spanning 676.570677 seconds. Finalization rechecked all 561 installed runtime files, twelve complete model configurations, broker tree and preserved historical state, then marked R9c `deployed_verified`. Health summary SHA-256: `3a6514314a78bb7857da839bb8696640b2519130b8c1cd4ab76f6a22c7b58bce`; finalized deployment receipt SHA-256: `38d91e1626269d312a049fad7d3384b5009b62315998df32c6707697ad29ecc2`. Failed R9a health and failed R9b tests remain historical failures.

Independent read-only acceptance found and verified ACN `company-dossier-version:company-sec-cik-0001467373:2`, content hash `b57cece992565fb09d6f7864dae555326cb30c25be72888e1ff0a3a0dc8928a9`. New business-model, supply/cost and variant-view units have valid producer and independent-verifier formal provenance. Industry-classification, segment/mix and demand-driver units retain their explicitly reported legacy proof status. Receipt `dossier-v03-acceptance/r9c-post-deployment.json` SHA-256: `31a5afca6555ad49d840b97dbfa06503043fb69ac009bb6d84498d49b5d5757f`. This verifies one actual publication, not completion of all five companies or an independent investment-quality rating.

The postdeployment stage audit retained acquired quarterly calls ACN 0 / CTSH 1 / EPAM 2 / IBM 0 / DXC 4 and displayed historical extraction separately from full-document read proof. Audit SHA-256: `eabc8bf9ff477ffed2913d36a5160f2092be94c6b004aeb681b12d930b57cd15`.

The next candidate incorporates selected AlphaEngine page/query progression, an explicit 130-call plan matching current mission authority, and registered annual-document retrieval/draft/independent-verifier/staging contracts. The annual/provider integration passed 270 affected tests in 83.547 seconds. Top-level resident execution and selection activation are still being closed; none of these next-release features is described as already installed. Next: complete these concrete consumers, validate the full immutable next release, and activate the reviewed failed-host plan with scoped rollback and new-controller health proof.


### Web cooldown activated; nonblocking quarantine follow-up — 07:35 UTC

Root executed the reviewed configuration activation at 07:21:13 UTC. Writer PID 28749, controller PID 28760 (started 07:19:33.785924 UTC), and control PID 28764 all restarted; exact new-controller health passed. The old v3 plan remains unchanged; only the selected v5 plan, selector and writer plan argument changed. The actual coordinator reported `www.investing.com` under the configured 3-distinct-URLs / 24-hour window / 6-hour cooldown. Receipt `web-host-cooldown-review/activation-receipt.json` SHA-256 `d47d75bbdd9f8f87d8a29089f0d61a3720ce34980e609d3b276b311327ef5c8f`, content hash `ff5301b078c6b6d839498d7da0c1b447fca01042edaa610c3432a3eafeb2b1d9`. All protected configurations and mission authority were preserved.

The owner clarified that an unreachable domain must not create a queue blockage. Candidate `792e3d0` therefore adds an explicit optional recovery policy: quarantine is reconstructed from the immutable fetch ledger and survives the initial rolling window. Normal work excludes quarantined hosts before selection; healthy-source work wins even when an unreachable host was configured as preferred. An expired timer admits one low-priority probe only when normal work is absent. The source's existing in-flight guard prevents a second probe. Any unsuccessful probe increases the configured backoff, capped by the configured maximum; an acquired response resets quarantine. Static skip hosts never probe. Candidate idle output separately counts held domain links. This behavior is not activated yet; the installed policy is still the initial six-hour cooldown.

Six new fold/real-ledger coordinator tests cover window expiry, typed versus retryable failures, increasing/capped delay, successful reset, healthy-host priority, one in-flight probe, explicit terminal-row probing, static skips and configuration compatibility. Broader testing exposed an unrelated candidate AlphaEngine rewrite that accessed `document_type` before dispatching Web specs. Root fixed source dispatch in `58ce924`; the installed R9c never contained that regression. The combined Alpha/Web/domain/model-selection run passed 188 tests in 15.718 seconds. Root also removed the newly introduced 480/500-character truncation of mission research questions: complete questions now reach selection, governed by the actual configurable model input budget (9 focused tests).

Actual follow-up Dossier verification passed all three ACN 0.3 versions, through version 4. Newly verified units now include competitive position, guidance style, management/capital allocation, historical price drivers and the KPI dictionary; inherited legacy-proof status remains explicit. Receipt `dossier-v03-acceptance/r9c-post-deployment-followup.json` SHA-256 `b513bd3654bc73eb193c1216586e2899681b29ceafb33a7353b5339709176098`.

A disk check found only 1.9 GiB available. Seven completed temporary test copies were archived after verifying every payload, path, mode, owner, timestamp, symlink and absence of open handles; only their redundant expanded copies were removed. Original payload 2,025,739,872 bytes is preserved in 314,981,469 archive bytes with manifests and restore commands. Research databases and deployment backups were not removed. Available space recovered to approximately 3.5 GiB before the bounded activation.

Next: finish independent annual-report top-level execution review and create the real two model configurations; bind them and discovery selection to current routing and day budgets; prepare the provider-failure bridge against the final immutable release; run full acceptance and deploy. Existing historical unknown invocations remain untouched. Follow-on post-send uncertainty needs a separately budgeted fresh WorkOrder rather than replay/refund of the old invocation.


### Integrated annual execution and independent recovery review — 07:55 UTC

Integration `a80ae5b` includes the registered annual-report production path and installation of two independently selectable model roles. The combined annual/provider/host/AlphaEngine/selection suite passed 243 tests in 87.451 seconds; log `/tmp/dalton-r10-integrated-annual-host-alpha-tests.log`. Discovery call plans can exceed the former arbitrary 1,000-call ceiling; actual governed budgets remain authoritative, with only SQLite integer representation limiting storage.

Host recovery review found no production defect and added real SQLite close/reopen coverage: a healthy host runs before a due quarantined-host probe, the second simultaneous probe is refused, failed probes remain isolated, and an explicitly configured multiplier of one preserves a fixed interval. Integration `36eb547` retains this permitted operator choice instead of inventing a minimum exponential multiplier.

Independent annual review confirmed that a single child now waits for exact Scheduler retry time without spending a transition and uses a new invocation on the next attempt. It also identified the remaining crash boundary: no autonomous consumer relaunched a child killed during that wait. That gap is being fixed with the exact approved plan and existing Work identities, rather than requiring a second human run. First-install annual configurations are also being hardened against concurrent owner writes and given explicit configurable retry/queue defaults; the real existing seed files have no such overrides. Annual call/run defaults are being registered so first installation does not silently retain a 120-second, single-attempt path.

Unknown post-send completion remains fully charged and historically intact. The next recovery must create a new bounded WorkOrder and persist a versioned recovery link that downstream verification can resolve; marking the original failed Work successful or replaying its invocation is not acceptable. This work is still under development. None of these candidate changes is reported as deployed.

Next: close actual child-death resume and bounded unknown-result recovery, complete exact configuration activation rehearsals, freeze the full release, and repeat full tests, wheel verification, copied-state execution and postdeployment health.


### Actual failed-host projection and annual budget blocker — 08:01 UTC

A single strictly read-only transaction over live Core projected the candidate host policy: `www.investing.com` has three distinct terminal-failure URLs, remains quarantined until `2026-09-11T12:08:56.858093+00:00`, and has ten failed document rows. No fetch or model call was made. Private receipt `web-host-recovery-review/live-ledger-projection-20260911T0757.json` SHA-256 `9fc37e86b88d2c94325a5bf3b23361f631b129c41dc33a25f0f0ee4e86993ccf`. The installed v5 initial cooldown and candidate v6 persistent quarantine remain distinct.

Further independent review found a release blocker in the new annual model worker: it inherited empty budget hooks and recorded Observability costs but had no mission/day budget admission or settlement. A per-call WorkOrder cap is insufficient. This candidate has not been deployed. The repair must attach real budget authority before dispatch, settle each attempt exactly once, retain full reservations on unknown completion and release only proven not-sent capacity refusals. Any historical attempt lacking this proof remains unproven; it cannot acquire fabricated settlement history to authorize recovery. Only after this foundation is fixed may a separately bounded new Work recover an unknown result.

Annual call/run defaults passed 21 focused tests. Provider host installer passed 19 private tests after stopped-process/socket proof, exact scope and dynamic configuration preservation fixes. AlphaEngine activation helper now rejects a retired mission even when the old version remains in history, binds the reviewed source commit and checks installed selection capability before activation (eight private tests). All these helpers remain inert pending final immutable release binding and acceptance.


### Configurable transport bounds and budget-wiring review — 08:09 UTC

Root removed the document reader's arbitrary three-not-sent-retries, 3,600-second queue and 60-second backoff ceilings. Overrides remain closed non-negative integers, with waits required to fit the runtime clock. Installed defaults and actual mission/day budgets are unchanged. A real governed extraction fixture now tolerates five proven pre-send refusals before success and creates exactly one paid admission and settlement; a long queue/backoff configuration is reflected in the immutable Scheduler lease.

The 63-test run exposed two old synthetic capacity tests that changed only an error code after constructing a completed model invocation. The shared provider-proof repair correctly rejects those unproved capacity claims. Tests now require terminal failure and retention of actual paid usage; production proof checks were not weakened. The final affected assertion and adapter/budget/provider suite passed 32 tests in 1.607 seconds. Initial failed test logs remain in `/tmp/dalton-r10-configurable-transport-tests.log` and `...-v2.log`.

Annual budget and transport configuration bindings are now in the immutable plan and WorkOrder metadata (40 focused tests). The first actual budget-hook implementation is merged, but independent review is still checking active mission/outer budget/pool authority, cross-day replay, overrun accounting and an actual broker subprocess path. Direct hook tests and the CLI fixture adapter do not establish those production conditions. The isolated full-suite preflight at `ee39177` is running; it is explicitly not deployment acceptance.


### Isolated full preflight failed and targeted repairs passed — 08:14 UTC

The immutable preflight `ee39177bad8edf5ae962e095ba2055788ca2d138` ran 7,040 tests in 601.117 seconds and failed with four failures, three errors and one skip. It was not staged or deployed. Full log SHA-256 `e188281d2cd9a2dd161c361f2b18c84b90cde3c152267d3d22c03e977c20db57`; receipt `/tmp/dalton-foundation-followup-r10-preflight-full-suite-receipt.json`.

Two failures were the unproved-capacity fixtures already corrected above. Two annual raw model-output schema files lacked titles; they now declare their identity while preserving the exact runtime prompt schema. The deterministic registered annual retrieval proof is now tested as a content-addressed derivation, not incorrectly required to carry an event timestamp that would change on replay. Model configuration registration again preserves the original first three names and appends discovery selection after them. The 121-test affected contract/registry/model-selection/annual suite passed in 5.476 seconds; a separate shape check confirmed both published model schemas match the actual prompt schemas excluding annotations.

Next: finish actual budget authority and crash/unknown recovery review, then freeze a new complete candidate and repeat the full suite. This failed preflight remains explicit evidence, regardless of the targeted checks.


### Annual child restart path merged; edge-case review remains open — 08:22 UTC

Integration `26b94e8` adds automatic annual child recovery and exclusive first-install configuration publication. A real subprocess fixture was terminated during provider backoff; after reopening the launcher, it resumed the same ticket, plan and Scheduler Work, used attempts one and two, and reached independent verification/staging. A superseding mission pointer prevented recovery. The combined annual/setup/launcher/transport/contracts run passed 33 tests in 16.116 seconds.

Root review found additional recovery edges that those tests did not cover: a child that fails before admitting any Work could be relaunched without bounded process backoff; one nonresumable historical ticket could prevent scanning later eligible tickets; and an inherited still-live child was monitored without reserving the launcher's normal single-flight slot. These are being repaired before release. Successful process restart in a fixture is not the final production acceptance.

The final Alpha activation helper now also publishes through an exclusive hard link, so failed fsync cannot expose a partial target and a concurrent owner write cannot be replaced. Ten private helper tests passed; helper SHA-256 `179e629211a04eefe6e33e36ae8d1b077b3b3baf35c86834b9248bf02b5747cb`. Its provisional source binding still needs the final release commit. The separate v6 host activation helper passed nine private tests and remains explicitly inert until rebind.

### Domain policy validation and production model review — 08:32 UTC

Root unified plan/runtime cooldown validation and removed the arbitrary 100-URL and 30-day configuration ceilings. Positive integer counts and representable clock durations remain required; the selected 3-URL / 24-hour / 6-hour live policy is unchanged. The legacy cooldown query now normalizes `as_of` to UTC before comparing stored timestamps. Regression checks prove that UTC, UTC−7 and UTC+8 representations of the same instant produce identical quarantine results for both policies. The combined host/Web/discovery suite passed 54 tests in 13.417 seconds (`/tmp/dalton-r10-host-configurable-policy-tests-v2.log`). The initial run's obsolete test expecting a 101-URL override to be rejected was updated; its failure log remains preserved.

Independent production annual testing found another fixture-hidden blocker: the verifier route supplies producer-family authority but the Work lacked its required provider contract/schema hash. That contract is being wired and tested over an actual Unix broker transport. Root also confirmed that the separate Cockpit model executor still lacks configurable returned-provider retries and its legacy single-route accounting trusts capacity error codes without dispatch proof. Those paths must use actual proof, distinct paid attempts and immutable policy identity before this foundation is described as complete. The fresh-Work annual unknown recovery graph has a reviewable candidate; its temporary budget stub is not production acceptance.

Next: finish the annual budget/verifier and crash-resume repairs, integrate the fresh-Work graph with actual budget authority, close the common document/Cockpit provider retry paths, then freeze and repeat complete release acceptance. Current live runtime remains R9c; candidate features are not reported as deployed.
