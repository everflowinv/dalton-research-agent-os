# R16a and financial-note development resumed — 2026-09-12

The original development conversation stopped during remote context compaction. Work resumes from integration branch `continuous-integration-wave4`; the unrelated four modified main-worktree model-deployment files are not part of this work.

## Accepted R16a

Frozen source `77cdfb486896fafb269e078dd00c07d7df0545ee` passed 7,675 tests, zero failures/errors and one skip in 775.443 seconds. The exact 606-file wheel and three embedded JavaScript checks pass; the guarded copied-state rehearsal passed 14 steps / 42 ticks / zero escapes. These existing receipts were revalidated rather than relabelled or rerun against different source.

Packet: `/Users/everflow/Projects/dalton-owner-activation-20260910/foundation-r16a-release`. Manifest SHA-256: `02af549e7258bf7ef8ac5fc449e059ec6e6086f285e7cfaceb4722689ec7babf`. The reviewed pure-preserve operations checkout is `52360f4c349f2b275d97c2465e39f2bd4b045569`.

Fresh read-only preflight preserved all 17 model configurations, existing mission v14, source authorities, service and OpenClaw configuration. Disk preflight required 4,817,426,110 bytes with 6,694,535,168 available. Deployment was started under standing owner authorization; receipts use the exclusive `deployment-resumed-v1` names. Installation, health and publication remain separately measured stages.

## Financial-note integration

Previously completed structure, evidence and production-plumbing changes were recovered from their existing isolated branches and cherry-picked through integration `633da1f1`. The planner target and inclusive period fixes were already present. No implementation was recovered by overwriting the integration tree from a stale branch.

Three new GPT-5.6 Sol agents independently review evidence replay, period/formula authority and actual production integration in separate worktrees based on author tip `400bc267`. Focused integrated tests are running. This work has not been deployed.

## Next

1. Observe the next authorized company-model attempts when their budget conditions permit; require validated spec persistence and real forecast consumption under task `adc99ada5bc838eabce5d75656fceb9c9e0430ffdb20ffb14b22099bd0533b36` before claiming live modeling completion. Do not expand budgets or force paid retries merely to obtain a successful result.
2. Advance exact-period financial-note requests through the governed retrieval/draft/independent-verification/promotion path. Four companies have selectable annual targets but no typed note promotions; quarterly evidence and DXC diluted-share/EPS authority remain explicit source gaps.
3. Continue company-specific full financial-statement, Category and Street coverage from source-backed authorities. The shipped annual note consumer is bounded and does not establish that broader product readiness by itself.

Retain strict AMZN workbook presentation and the current Cockpit visual style. Do not replace missing source evidence or human research decisions with deployment authority.

## Installation and review checkpoint

R16a installation finished `2026-09-12T03:00:57.739411+00:00`, exit 0; runtime606/config17 are exact, with zero configuration mutations. Controller PID 99648 started `2026-09-12T02:58:22.984013+00:00`. Deployment receipt SHA `ae6adc01add3d2b85bfaadf0a9231ce892e7930a1c1a36e1ec2ff8b1a6f0c5be`; installed receipt SHA `0aab0313899e994ffd9142df6922fa6c45b58041d3dd28c7cc7802697df9dbac`. Observation `health-resumed-v1` is running; no verified R16a publication yet.

After exact verification of 90 files in the newest three rollback snapshots, the obsolete R14c1 rollback was removed (13,947 files; 2,918,013,030 logical bytes). The plan and receipt are retained in the R16a packet. Free disk recovered to approximately 5 GiB; no new large copy is admitted until its own space guard passes.

Integrated evidence correction `917b1d5a` replays promotion columns, Ledger request identity, supports relations, ordered admission-bound execution and route identity; author checks passed 26 tests. Integrated structure correction `b9adfeae` separates annual/quarter applicability, makes validated annual note numerators available to the annual EPS consumer, and recomputes replay from inputs before annual export; author checks passed 102 tests. Initial recovered integration passed 179 tests before those corrections. Combined independent review and the actual model-spec entrypoint regression are still running.

The first postdeployment product audit failed inside the audit helper: `plan_json` is valid and self-hashed but intentionally does not contain the separately derived database `plan_id`. The corrected version will derive that ID from mission/state and compare all denormalized columns, schema and task identity. This is a verification-tool false negative, not evidence of damaged live planner authority. No product acceptance is recorded from the failed attempt.

## Verified R16a; frozen R17

R16a passed all 45 health samples over 673.488 seconds, followed by exact-source/config finalization and verified publication. Health SHA `fe375e3d28c6674d829fede10918aea73487b12f97d74f1a8853e17528b8b770`; finalization SHA `401087d0a6f23f7d7aaea7a8bc6519608f7555a053741ab85091c4393fa66c36`; publication SHA `10a7504489b60bc1f65bc191859986ed9d3fc538df5f5b750648fb19c0067817`. Owner release/runtime pointers now identify R16a. Completed scratch cleanup removed 21 DB/WAL/SHM files / 1,344,761,856 bytes after stable identity/hash/no-handle checks, preserving all other evidence.

Actual model-spec CLI regression `9a019cf0` covers same-mission acquired 10-K, typed target, four-stage promotion, model prompt/output, governed spec persistence and annual numerator 105, with quarterly absence remaining explicit. It exposed the SEC USDPerShare spelling mismatch. Independent review then found normalization affected historical input hashes; `c38eb438` preserves raw legacy 0.1/0.2 inputs and applies normalization only to the new structured path. Independent 55-test unit/state/plumbing validation passed, including fixed historical full-table hash. Root's prior combined corrected invocation passed 106 tests; an earlier invocation included a nonexistent module name and is not recorded as a source test failure.

R17 freezes `c38eb43800473174aab0673fa5e288c1d6b86545` on `foundation-r17-notes`. Its wheel matches 608 files (440 Python, 73 SQL, 92 JSON, 3 HTML) and three embedded JS blocks; SHA `57bb69efd681e78f1232aa6a8697ff1c463bf30ee03ef09f8fab85b776225338`. The full suite and fresh guarded pure-preserve rehearsal are running on that exact source. R17 is not yet accepted or deployed.

Corrected product audit `a78eecd5` passed six real persistence/adversarial tests and produced read-only `product-resumed-v2` at 03:09:40 UTC. Its source-bound receipt SHA is `4493ef5d585b8279bbfa02d50fe9a2fc22752baae08eb85e09adee3c67822afe`. Two stored plans have succeeded Scheduler formals; a third Work is running/queued. No fresh model or promotion is inferred. Further historical lifecycle audit shows DXC already reentered after midnight and completed with insufficient evidence, while EPAM exhausted four proved-sent/settled output-contract failures. A bounded reporting follow-up will distinguish these from genuinely unproved sends without changing retry decisions or budgets.

## Final R17a candidate

R17 full acceptance evidence passed: 7,725 tests / zero failures/errors / one skip in 783.356 seconds, wheel608/threeJS, and copied-state v2 14 steps / 42 ticks / zero escapes. Its v1 disk-reserve refusal occurred before copying and remains preserved. Completed v2 scratch cleanup removed 21 DB/WAL/SHM files / 1,370,841,088 logical bytes after passed binding/artifact checks and no-open-handle verification, preserving non-database evidence. R17 was superseded before any accepted manifest or deployment so that the final candidate could include the concrete live-output prompt correction. Packet `candidate-superseded-v1.json` records this boundary.

Four R16a company-model provider responses had been semantically refused before persistence because derived lines claimed a filed concept. The prompt/schema had said to tie derived results to filed subtotals without stating the required field placement. Integrated `bf7ed884` explicitly requires derived `line.concept=null` and places the exact filed historical tie only in `formula.tie_out_concept`. Semantic whole-refusal and the format/text-length-only repair contract remain unchanged. Author checks passed 68 tests, independent production review passed 79. Read-only actual-company fitting remains under 120,000 bytes for IBM 119,955; CTSH 115,133; EPAM 119,800; ACN 119,856; DXC 119,966. The fitter omits only complete numeric groups; this does not create missing source authority.

Integrated `b4bc0a77` is the patch-identical independently approved `3c96c87e` reporting correction. It proves the terminal failed envelope, completed canonical invocation, selected route, same-route budget admission, usage identity, actual cost and settlement before reporting a paid output-contract failure. Any diagnostic mismatch or exception retains the old stopped unknown-send barrier. Both diagnostic paths remain `eligible=False`, `retry_at=None` and return before recovery-link/enqueue. No paid retry, budget, outcome or promotion is introduced. Historical completed controlled reentries are classified before stale retry timestamps. Author checks passed 54 tests; independent evidence review passed 42.

R17a freezes exact source `b4bc0a772929900b00a32f79dde29417ab2c40e2` in `dalton-foundation-r17a-worktree` on pushed branch `foundation-r17a-notes`. Independent full-diff review verifies six linear commits / exactly 12 expected files since R17 / clean source / no cherry-pick drift. Full suite runs against this exact immutable source. Wheel608/threeJS byte match passes, SHA `e0f5e261e03a94c00bf75e2b27833fbf4ddd1358b4f722550b0aa6139b7124ee`; guarded copied-state rehearsal v1 passes. Config transition remains pure-preserve from installed R16a, retaining all 17 model configs and service/OpenClaw/mission/source authorities. Predeployment read-only baseline SHA `0ea590ce1bd1ef2d30f9db0a3e1a3c295c885493dd3d86678bda83040cc5f5ae`, receipt SHA `da50776d0cf582e08be5441f137ec86944fa3b0e13725fe984be98ce50115abf`. Acceptance, stopped-window deployment, health, finalization and publication remain pending.

## R17a installed; observation pending

R17a passed the exact-source full suite at `2026-09-12T03:46:03Z`: 7,730 tests, zero failures/errors, one skip, 773.108 seconds. Receipt SHA `960371151b3c185ddb4d0cb770f96b88670de759bb765299974a80a12da4b5ae`. Accepted manifest SHA `15b0593e49bca060ca8a88ee883f280eaee3756ed94745d3ae69110b6b64570a`; copied-state binding SHA `007dfbbdef8f20a1bf6985de72fc04b9804768b5924744257523a9d1489dd533`. The final fresh preflight passed before stop. Independent install-space preflight required 4,775,513,997 bytes with 8,991,481,856 available.

Deployment ran `2026-09-12T03:46:40.165187Z`–`03:47:43.116990Z`, exit 0. Runtime608 and model-config17 match exactly; configuration/service/external mutations are all zero. Deployment SHA `52366c581251e30c23558d5d46fbaead7058995222bc8bf75cfd4a94a5506bd9`; installed SHA `f24640629d1e724ae439c621a3d1f6f0e19b84fb32e68b2ae7dc90d68f520774`. Controller PID 8314 started `03:47:25.674042Z`. `health-v1` is running; R17a is not yet published as verified and the owner pointer remains R16a pending finalization.

Retained rollback snapshots are R15b `20260911T232723.049093Z`, R16a `20260912T025728.893320Z`, and R17a `20260912T034642.354544Z`. All 90 retained database/restore files match their three fresh manifests. The obsolete R15b `20260911T230942.117632Z` was removed after metadata/no-open-handle verification, deleting 13,947 files / 2,995,110,486 logical bytes. Plan, verification and removed-snapshot metadata remain in the R17a packet. Completed R17a rehearsal cleanup removed 21 DB/WAL/SHM files / 1,371,344,896 logical bytes while preserving all non-database evidence.

The initial read-only `product-v1` snapshot at `03:49:21Z` proves new company-model task hash `adc99ada5bc838eabce5d75656fceb9c9e0430ffdb20ffb14b22099bd0533b36` running for IBM under ticket `company-model-spec-run:4de0754bef459a6281e50571` / PID 8365. No terminal result existed yet, and no new models/promotions are inferred from launch. Audit SHA `596801cc483a1d9c22d3f1514ba676fd1e8d402576fe79a9ea65cdad740e9181`; receipt SHA `17fd22454a7c5f332dee9cf3c43999ae9ca74ae14e23fdba99efbadfa5d3d8f9`. Final post-observation product evidence is still pending.

## Verified R17a publication and product boundary

All 45 health samples passed on controller PID 8314 over 669.344 seconds, followed by exact runtime/config verification at `2026-09-12T04:00:17.104263Z` and successful verified publication. Health SHA `5c8810383b714a2fd7f4958c3998d35723eecd355e0365ac154dca8179ca9943`; finalization SHA `be9389d23c8fdac034c7f16c9fac8434cda9fac2956e7e5cd52c13a9deb50c0e`; publication SHA `93615f7a18555b95661452ac431d41b50de9b56c01911dc5047cd36ff718541a`. Owner `current-release.json` and `current-runtime-config.json` now record R17a as `deployed_verified`. R16a remains the retained predecessor; undeployed R17 remains a validated, superseded baseline.

Post-observation read-only `product-v2` at `2026-09-12T04:00:29.643746Z` binds the exact deployment, frozen source, controller and predeployment baseline. Audit SHA `bff627c20c2e6d5f152edbd1c555806dd30e0114cc432b8abd9b9ff5216676a6`; receipt SHA `0eae468af3731e08ce31af175328235dcc2dbf613304f4945a5f4cf03f27b19e`. It records zero postdeployment planner Works, zero fresh outcomes/promotions and zero new model specs/versions. The tracked old directed admissions still include one proven historical provider response/settled send and two no-send refusals; those are lifecycle facts, not fresh research output. The audit helper's retained R15 schema/title names do not replace its explicit R17a release/deployment binding.

The R17a model task was observed running and then encountered `BUDGET_REFUSED` with `spec_status=model_unavailable`, zero cost and zero formal authority writes. Provider success, model production and financial-note promotion are therefore not claimed. Existing budgets, semantic whole-refusal and human/source authority boundaries remain intact. This completes the R17a implementation/review/acceptance/deployment milestone; the next product proof requires permitted model execution and adequate source evidence.

Final budget evidence is preserved in the exclusive 0600 `product-budget-v1.json`, SHA `771308e24dd55793a74a6d2b5218a47e366fe8965e64c1c8b515ad401b55eab7`. IBM's failed formal at `03:51:19.393700Z` is `BUDGET_REFUSED`, binds `invocation:not-started`, has empty usage refs, and has no corresponding Core model invocation or observability usage entry. This proves no provider send. The coverage-budget admission reserved 1,499,670 micros and settled the same reservation with null usage; that reservation settlement is not proof of provider actual cost. No day/pool rejection row or explicit retry time was found, so a UTC rollover re-entry time is not established. The next budget investigation must identify the exact refusal/return-of-reservation boundary before changing any policy. At the final product cutoff, IBM's wrapper ticket remained running through repeated same-identity starts, no subsequent company task had started, and new model/spec authority remained zero.

## Continued development: corrected provider-send diagnosis

At the owner's request to keep developing on September 12, three GPT-5.6 Sol agents resumed parallel investigation of the budget disposition, lane restarts, and broker transport. This disproved the previous no-send conclusion: a synthetic Scheduler invocation identifier and missing Core accounting are not sufficient evidence that an external request never ran.

The exact broker journal record for IBM Work `work:cockpit-model_spec-2001f133f8dacddbba3e1d10aa0998e6` contains completed invocation `invocation:fcb4a2b63eb5d2eef7b23cdc9a527c20`, successful gpt-6-astra response, input 46,436 / output 6,843 / total 53,279 tokens, and broker-reported cost $0.80651. The Work/request output ceiling was 6,000. The adapter correctly produced `PROVIDER_BUDGET_EXCEEDED`; the fallback path incorrectly collapsed that downstream budget failure into local `BUDGET_REFUSED` with a synthetic `invocation:not-started` reference. Two filtered HTTP 200 transport events corroborate provider communication but do not by themselves prove two generation calls; the runtime may first count input tokens.

The exclusive 0600 correction receipt `foundation-r17a-release/product-budget-correction-v1.json` (SHA `e9a15ebe9c2a19165a70ce0606765138035cfd1aa762ac0de8ba82b387a7d9c0`) supersedes the no-send/provider_send_proven=false conclusion in `product-budget-v1.json`. The original receipt and earlier narrative are retained as historical evidence, not current truth. The budget reservation remains conservative; neither a refund nor an automatic paid retry follows from this correction. The broker's reported cost is not substituted for a missing canonical Core accounting entry.

Current work separates the local admission-refusal path from downstream/unknown dispatch failures, requires exact Work/attempt/route/phase rejection proof for audit no-send claims, and prevents time/restart-based repetition of a terminal company-model failure from monopolizing the lane. Provider output controls are being checked against the actual ChatGPT/Codex Responses transport, not assumed from a different OpenAI API adapter. R17a runtime acceptance remains valid; new model production remains unproved.


## R18 integrated fixes and actual transport capability

The actual endpoint is ChatGPT's `/backend-api/codex/responses`, through `openclaw-openai-chatgpt-responses-transport`. The installed native Codex request sanitizer explicitly deletes `max_output_tokens`; it is listed among unsupported parameters. Ordinary OpenAI Responses parameter forwarding does not prove this backend supports a hard output limit. Usage normalization separately records reasoning and does not double-add it to output tokens. The next broker revision must inspect the resolved transport capability, refuse unavailable mandatory controls before dispatch, and permit only proved local refusals to proceed through the already authorized fallback chain. Unknown dispatch retains conservative settlement and cannot authorize retry.

Integration `f76d3f44` preserves downstream `PROVIDER_BUDGET_EXCEEDED` in the chain failure detail and emits unavailable/unknown dispatch instead of a fabricated not-started reference. Actual local admission refusals retain their existing wire identity. Integration `f885470c` binds Scheduler failure replay, holds terminal model-budget/control failures across generic time/restart probes, and frees the lane for other eligible companies. A real implementation hash change can affect permission identity; replaying an immutable terminal Scheduler result does not itself authorize a new paid call.

The combined fallback, CLI, model lane, failure classification, permission and audit regression passed 169 tests. Audit compatibility `52fdbe18` additionally recognizes the historical directed-worker `MODEL_ADAPTER_REJECTED` label only when the exact local rejection proof exists; its 16 tests passed and independent review approved it. A generic label, synthetic invocation, missing usage, wrong attempt, wrong route or wrong phase remains insufficient by itself.

Read-only stopped-window review `af4a7781` documents why the existing runtime pure-preserve contract does not cover a changed external model broker. The current external CAS contract only validates the web-search broker. The R18 release must first add and review a closed model-broker artifact transition, hash-bound rollback, loaded identity/capability verification and preserved journal authority. Development continues in parallel; no R18 acceptance or installation is claimed.


## R18 control and financial-output corrections reviewed

Broker/host control commits `b4261b41`, `15809864`, and `0fb599e6` are integrated. The exact-version host guard runs after resolving the model API/endpoint and before transport dispatch, only for the plugin's explicit OpenAI provider-control mode. Ordinary Codex sessions are unaffected. Native HTTPS api.openai.com Responses is admitted; Codex, proxy, Azure, missing and unknown endpoint capabilities are refused. The existing Gemini mode follows its existing separate control path. Broker-local fixed typed errors carry exact no-send proof; only that proof permits an existing authorized fallback. All-local exhaustion settles zero, whereas missing/invalid proof or any subsequent unknown send retains conservative reservation. Independent combined integration review passed 58 Cockpit tests; broker 33 and host-patch checks passed separately. Earlier combined runtime/audit/patch regression passed 174.

Root's filtered read-only journal evidence found ten returned model specs containing 28 non-currency supplementary lines in currency-only income roles. Exclusive `product-role-unit-evidence-v1.json` retains identity hashes and structural role/unit/concept fields, excluding prose, prompts and numeric values; SHA `42ff303d62b415ae4cf5b83270638100bf7446dc0da9b4c3d36ffef16cf78d1b`. This evidence does not itself claim Scheduler validation, billing or new retry authority. Integrated `641b64d8` clarifies that company-presented components/subtotals are currency amounts, while basic EPS/shares/dividend-per-share remain source context rather than being forced into incompatible calculation roles. The regression accepts a valid spec with those supplementary source rows still present, rejects incompatible role/unit proposals as a whole, and proves the input is unchanged. Independent state/spec/structure/CLI checks passed 107 tests; whole-group prompt fitting remains bounded at 120,000 bytes. The schema description changes the task hash truthfully to `e7b893bf…`; no old terminal response is rewritten.

Read-only product recheck v2 at `2026-09-12T19:35:21.715962+00:00` confirms two historical exact local no-send refusals and one historical proved provider-response/settlement in the tracked directed scope, with zero fresh model specs/versions or outcomes/promotions. Audit SHA `eb94b36a19e26a5419644c722d0ea1a821e8b4c632045c80b8df95a29ad0b8ab`; receipt SHA `2a9b2c183b56f7bcd8d25fc82178dd5dd50c3adc1f01bd6b031cd054de0c1dc7`. This does not contradict the separately proved IBM provider send.

The first model-broker deployment contract candidate `9d0a70af` is not integrated or approved. Root and independent review found that it must preserve historical 0.3 manifest replay, handle the actual already-configured 1,048,576-byte frame limit without inventing a repeated frame change, bind the helper's exact host target and complete rollback receipt, and reverify installed host artifacts through finalization. Its 95 passing focused tests did not cover these gaps. Implementation and adversarial review continue before a single immutable R18 source/ops freeze and release acceptance.


## Producer budget semantics: correction to the control diagnosis

Further exact-path review distinguishes IBM's model-spec producer from an independent verifier. `OpenClawModelAdapter._required_provider_controls` returns null when the selected route has no producer family. IBM therefore sent `maxTokens=6000` as the ordinary completion request limit, without `requiredControls`; its installed Astra broker profile also has no provider-control contract. The existing producer policy uses the returned usage as an immutable acceptance ceiling, with conservative reservation on a rejected or uncertain result. It does not claim a host-proved hard provider cap. Consequently, the native Codex sanitizer explains why a requested 6,000-token producer response could exceed that value, but the new required-control endpoint guard only repairs the explicit independent-verifier path. It must not be described as preventing IBM's original producer request.

The rejected 6,843-token response was correct under the existing producer acceptance policy. The actual IBM defects were the downstream refusal being mislabeled as proved no-send and terminal wrapper restarts monopolizing the lane; both are fixed in R18. The role/unit prompt correction separately addresses observed producer output quality. This release preserves the existing producer policy, the 6,000-token acceptance ceiling, and owner-selected models. It does not silently introduce an output-only hard-control contract that would disable the current Astra/Claude CLI producer chain. Such a distinct opt-in contract would need its own supported-transport proof and policy scope. No extra owner decision is needed to preserve the existing behavior.


## R18 superseded before deployment; R18a rehearsal correction

The integrated broker/ops candidate `b0a5c35e4086c3f1b2153f1e03f8ffa9e5c9d3a0` received final independent review, including exact inner/outer receipt validation, gateway restart identity, before-target preflight, exact helper transformation, historical 0.3 compatibility and current frame preservation. It was frozen on `foundation-r18-controls`. Its wheel matches all 608 runtime files and three embedded JS blocks, SHA `070ff3c5fcce5ef371cada3993ee5fb464e4256c6e354cf2aed0d34d1a87d2e2`. Inert preparation succeeded with 17 model configurations preserved, a staged closed broker tree, and an exact host before/after scratch patch and no-call check. No accepted manifest or deployment was created.

Before executing the copied-state rehearsal, root found that its derivation still indexed the old single-plugin/frame-mutation structure and did not relocate the new host artifacts. R18 was explicitly superseded. Its owned full-suite child was interrupted after 113.5427 seconds to avoid completing acceptance for obsolete source; the runner preserves exit -2 and no native completed result. Exclusive supersession receipt SHA `cb1fba005a44272ba466f44861c530eb7c8e529b311259f92b7b55e6438b733c` explains this boundary. All candidate artifacts and the interrupted log remain intact.

Integrated `29be3d9e` derives all declared plugin mutations by kind, copies each plugin independently, relocates both exact host artifacts under the confined root, and preserves source/hash bindings. The proof explicitly says `artifacts_confined_not_executed`; it does not claim to run the actual OpenClaw host in the copied-state exercise. The separate inert scratch helper already validates the exact patch transformation and static check. A new current-frame/model-broker/host-artifact fixture reaches the final external validator and confirms the confined identities. Root's 154 copied-state/transition/broker/executor tests pass. R18a will use a new private packet and one corrected clean source/ops commit for full suite, wheel, copied-state, acceptance and deployment.


## R18a full-suite failure and R18b continuation (2026-09-12 20:21 UTC)

R18a exact source `85dd42c5df42ff8fd4a9854aa89e210c3f66d942` finished the full suite at 20:15:26 UTC: 7,794 tests, two failures, zero errors, one skip (770.16 seconds). Both `test_public_web_core_search` assertions expected recent invocation counts of one/two but received zero. The original full-suite artifacts are copied byte-for-byte into the inert R18a packet; `candidate-failed-v1.json` SHA-256 is `057af26b6db2f3a112251617c90188d96ce752c493ffb30500840a000ea91e94`. No accepted manifest or deployment exists. Passed wheel and copied-state checks cannot override this failure.

Focused reproduction confirms the production path dates a fresh connector execution from immutable request creation time. Once the fixed September 11 noon request ages beyond 24 hours, its September 12 execution is omitted from the recent count. R18b will separate request identity time from first execution time, with deterministic delayed-request and durable-replay tests; no historical records are rewritten. The deployed runtime remains R17a.

Independently approved compact model-spec JSON guidance (`acbed334`, integrated as `e6db406d`) joins this next candidate. Prompt contract 0.10 has task hash `af60c3495da0e329d17b79940bbffcfa5639c6683a1e6a26e5d9f1bbc4038564`; schema, parser, semantic rejection, repair boundary and 6000-token producer acceptance ceiling remain unchanged. Pretty and compact fixture inputs validate to the identical model spec/content hash. Historical IBM response formatting would remove 5,994 of 29,034 characters (20.645%); this is character-only evidence, not a token/billing measurement, a guarantee of fitting 6000 tokens, or permission to accept the old rejected response. No provider call or live retry was performed for this analysis.

The completed search fix (`083f8cbe`, integrated `de6402da`) independently passed review. A deterministic delayed request counts against first execution time; UTC-normalized accounting includes exactly 24 hours and excludes 24 hours plus one microsecond and future records. An expired durable replay stays free and preserves the invocation hash. Existing canonical invocation without a durable runner request now reports an explicit operator-recovery hold before another attempt. There is no automatic retry, migration or rewrite of historical invocation records. Root's 95-test integrated search/planner/model-spec/discovery run passed in 10.803 seconds. An earlier invocation using relative `PYTHONPATH=.:src` caused one AlphaEngine subprocess test failure because the launcher changes cwd to its state directory; rerunning the same set with absolute source paths passed. The frozen full-suite runner already uses absolute paths, so no suite failure is waived.


## R18b installed; continuous health pending (2026-09-12 20:43 UTC)

Frozen source/ops `3855c1e3c20e8d731f256e256d0fe833cca3ac65` passed 7,796 tests, zero failures/errors, one skip in 778.347546 seconds, finishing 20:38:59 UTC. Runner SHA `8b82325bf18433dc884a85d849f545759dfed5e060fa3cd050ef0f597bc1f96f`; suite log `0b3a502146f6a1506a22c317bb283fcfa0e911767ea056f79c350bf11241fda1`; native result `7e9423684fc65dca1fc06e045ecece634277758f397605a95f7bc3111f402634`; receipt `ab42a9d347e1cca0394d4387d6c84bbaf23c69581ad95a6dc3ec58cabb2f4767`. Wheel608/three-JS passed with wheel SHA `23da738567ec071b68ac04f99832a8055d84d37f5da08af297d5b663216b30bf`.

Copied-state rehearsal passed 14 steps/42 ticks/zero escapes. Binding content hash `bdc67bdd6b0391999b8d40b7238ff6f8551f64dfd43b50d7904b57e5469967ef`, file SHA `4d731e02b31768e5c57b1c1ef142975cbe02e3ea0e8fc70358e615d58d5941ea`, log `8a4f9bbbcc5c69a8ea1ff92712658e5fb17ebac8ee29700fa15202587bbf39bd`, report `63ab09a4ecb945188e98ee4ea0e9349c8f66f0a66e469b0f73e4d467d9b70ba9`. Independent review verified 51 confined entries, all source/plugin/host artifact hashes and zero live/provider calls. Host proof in the copy remains explicitly artifacts-confined-not-executed. An initial inert preparation attempt omitted the required managed-plugins parent and stopped before plugin staging; its partial input/host artifacts and failure receipt are retained in sibling `foundation-r18b-inert-preparation-v0-failed`. A fresh successful preparation supplied the accepted packet, without reusing that failure's artifacts. After review, 21 completed scratch SQLite files (1,422,774,272 bytes) were removed; all non-DB evidence was proved unchanged. Cleanup plan SHA `57e55d7f6443991d719c70e1dd72bfb5765d4e7835e0570a0164ca7da626db62`, receipt `94d471bfd40ff1876c05887fbda2589a995069581ffc7f0b0cdfbf772d9c2bdf`.

Accepted manifest SHA `aaca195ea19e46fff4dabeaae9fd1e75e0f65496bb7da83f0843db74047fc603`, canonical hash `5c2f46e54cb044887c3c19a2aa421fd45dbdd550dc02401cd66736e214f556fd`; all 16 bound artifacts independently verified. Deployment ran 20:39:37–20:42:51 UTC, exit 0, without rollback. Actual runtime608 files,17 model configs, mission14, broker and host patch verified. Installed receipt SHA `7e47c0eaed8027a6c9bf714ae8d805e74caec13caa2f7e8fd2bffbea05a5eafb`; host receipt SHA `7390b411989dbb3f05b1e18c5d78821dcc611ce79d037f7d703d42f9883b700f`, target bytes `a48f393e4dcb2a573ca4dd5a39a3674f56e0eaed742682ba104064e9e4ce0348`. New rollback snapshot is `deploy-rollback-20260912T203944.678060Z`, database snapshot `20260912T203951.006407Z`. Continuous45-sample/minimum660-second health observation has begun; this is not yet a deployed-verified publication. Natural product outcomes remain separately pending.

The next-candidate task-identity audit (`2f38c793`, integrated `4f2fea0a`) passed independent review and 116 related tests (27 direct audit tests independently reproduced). It distinguishes exact canonical Work/spec task identity, absent legacy proof and repair Work whose root has not been replayed; malformed/present-null and inconsistent producer suffix proofs refuse. This candidate does not modify R18b or its bound audit tool. No old IBM response is upgraded or replayed.


## R18b runtime health failed; new actual product findings (2026-09-12 21:03 UTC)

The completed health observation is **failed**, not pending or accepted: 45 samples over 670.948448 seconds, 43 healthy and two degraded (indexes15/16 at20:47:18/20:47:33 UTC). Summary SHA `eca4dbad34e4d453081c69af658b6273002402ccd1f0a989ffbd45e114a63ff1`. Both failures have fresh heartbeat/live sockets but outbox state error: its writer RPC began20:46:38 during the long bounded-planner writer tick and timed out after about31 seconds as `RemoteError: writer service is unavailable`. Later samples recovered; that does not override the failed window. Actual installed R18b remains running, while no finalization/publication was performed and owner pointers remain R17a. A successor must fix contention and repeat full acceptance, preserving this failure.

Fresh IBM work under task `af60c3495da0e329d17b79940bbffcfa5639c6683a1e6a26e5d9f1bbc4038564` did execute naturally: Work `work:cockpit-model_spec-937af127f42bc45e3b87fc1b4b127728`, hash `d7a4f803b271a01dabcf1efcd062b50355091e8b15e011ccada6160889d60317`, state `d0a21e9ff06f7d394fb8e9a393416ed028a3ad20df1655d5c13d0ea96d89d77f`, ticket `company-model-spec-run:6c09964ae738e444998cebbb`. Broker invocation `invocation:6740d85f0f26a9b34214c5a9bbc2b92f` returned46,126 input/4,561 output tokens, reported cost$0.68931 (not an independently audited bill). This response fit6000 but the whole specification was refused locally: pretax sum included `gross_profit`, which the validator cannot currently express as a pretax input. No spec was persisted or old response accepted. Its actual filing presents gross profit minus a tied company expense/other-income subtotal as pretax income, without a filed operating-income subtotal. Independent review confirms an expression gap, not grounds to invent an intermediate or relabel standard gross profit. A narrow new-contract rule is being implemented with exact filed ties and numeric replay unchanged.

The lane naturally advanced to CTSH, demonstrating fairness rather than repeating IBM. CTSH also returned a separate semantic refusal, `diluted weighted-average shares must be positive`; that is a separate pending diagnosis. The earlier historical IBM budget-refused work and response remain distinct immutable records.

## R19 integration and historical replay review

Runtime fairness correction `6e5fc4ee` serializes new outbox and bounded-planner writer work while always harvesting completed futures and errors. When both lanes are due, the older launch gets the next turn (initial tie goes to outbox), preventing starvation in either direction. The integrated service regression passed 64 tests. R18b's failed 43/45 health window remains immutable; a later recovered heartbeat cannot replace it.

Financial structure correction `395fba08` introduces inner structure contract 0.4. It permits a pretax bridge with exactly one positive gross-profit term and otherwise only company-presented component/subtotal terms, retaining exact filed ties, units, periods and numeric replay. Old structure 0.3 rules remain unchanged. The combined financial/spec/CLI/product-audit regression passed 122 tests. Old IBM responses are not accepted under the new task identity.

CTSH's 29 filed consolidated weighted-average diluted share facts are positive. The refused specification selected the correct US-GAAP concept, but the input normalizer subtracted nine-month averages from full-year averages, producing unavailable, zero or negative inferred fourth-quarter shares. The successor correction preserves filed quarters and duration facts, suppresses that subtraction only for exact basic/diluted weighted-average share concepts, and leaves missing quarters explicit. Other additive share flows retain their arithmetic. Integrated correction `d38e58fb`/`c4a611df`/`f13f5a0e` keys the behavior on the persisted inner structure 0.4, not the already-existing outer specification 0.4. Independent review reproduced the old outer-0.4/inner-0.3 fixture against untouched base `395fba08`: input hash `01f2a6f084592bac207ef3d7ac005efcea843e091a0ccc623d6384cec3cf7bde` is unchanged. Inner 0.4 omits the invented quarter while retaining both duration facts. The author passed 189 related tests and independent review passed 113 focused tests. State normalization contract `company-model-series:0.2` and authority projection 0.4 bind the new task hash `2b5493d24e1706a0ba34a8387d5bd39b3008f5aa1d90a5bbb9e2570346f46612`; old CTSH responses cannot be reused under that identity.

R19 release preparation uses one new clean source/ops freeze and a pure-preserve runtime transition. Its actual installed predecessor is R18b, whose health failed and whose owner publication never occurred; the published owner predecessor remains R17a. The already-installed R18b broker tree and host target will be reverified against their pinned receipts before and after installation and before finalization/publication. They are dependencies to preserve, not patches to apply again. Full-suite, wheel, copied-state and fresh continuous-health acceptance remain required before publication.


Transport identity audit correction `09812641` binds explicit closed `transport_retry` metadata to the canonical request suffix, in base/transport/producer order, and applies the same check to repair requests. Historical Work can contain a policy-derived suffix without the original policy metadata; those records now remain `unknown_legacy_missing_transport_retry_authority`, rather than being rejected solely for the suffix or falsely declared verified. Thirty-two direct audit tests pass. The five stored specifications used for the read-only history check were all created on September 11 under task prefix `ce8320a93301`; they are not the five fresh R18b model-spec Work orders. R18b postdeployment stored-spec count remains zero. Its actual new IBM, CTSH, EPAM, ACN and GLOB attempts are separate execution/refusal evidence, not accepted products. The integrated financial/input/service regression passed 262 tests in 10.325 seconds.
