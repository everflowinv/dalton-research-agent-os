# Verifier provider schemas — batch 2

Zero Base Review, earnings preview/calibration, and research-quality verification now select hash-bound packaged provider output schemas. Preview and calibration share one schema because both call the same semantic validator. Zero Base keeps its string finding contract; quality keeps criterion-addressed findings.

Zero Base and earnings child ticket identities include their effective packaged contract fingerprints, allowing a corrected contract to retry the same business input while preserving ordinary restart idempotency.

Focused validation covered the real adapter request controls, purpose isolation, schema vocabulary against each semantic validator, launcher behavior, and existing verifier suites. No transport or model call was made.
