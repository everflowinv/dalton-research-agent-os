# Company-model post-R16a disposition audit

This audit read the live lane tickets and summaries plus the active Scheduler, model-budget and Core databases using SQLite URI `mode=ro`, `PRAGMA query_only=ON` and explicit read transactions. It did not construct an authority, migrate a database, change configuration or budget, trigger a lane or provider, or access the network. The observation window ended at `2026-09-12T03:22:02Z`.

The zero fresh specifications and models in `product-resumed-v2` at `2026-09-12T03:09:40Z` was accurate at that instant, but it is no longer the complete lane disposition. The new task hash `807e901c433fb458f754ba6cbadbbe6323390f28ce04c0a49e85550cc917fe51` launched sequentially for all five companies after R16a. The prior UTC-day attempts had all been atomically refused with no send because $99.677501 of the $100 day cap was already committed. After the UTC rollover, IBM, CTSH, EPAM and ACN were admitted and received provider responses. DXC was admitted later but the Scheduler recorded `BUDGET_REFUSED`, `invocation:not-started` and no usage reference, so it made no provider call.

IBM, CTSH, EPAM and ACN all failed the same full pre-persistence semantic validation: `financial_statement_structure is invalid: a derived line cannot claim a filed concept`. Their prompts fit the 120,000-byte limit, with respectively 41, 5,921, 78 and 83 bytes of headroom. Each Scheduler result succeeded at the provider boundary and settled $0.752240, $0.757480, $0.758050 and $0.781280; each spec summary then correctly recorded `spec_status=refused`, zero formal authority writes and no repair attempt. The active repair contract permits one repair only for `format` and `text_length`; it explicitly makes semantic failure a whole refusal. A semantic repair therefore was not omitted by accident.

DXC used the current task identity and produced a fitted 119,837-byte prompt with 273 numeric-context cells. Its only current-cycle blocker is the legitimate atomic budget refusal before invocation. Independently, the financial-note input audit records that DXC lacks an exact standard diluted-EPS and weighted-share source target, but no live note promotion exists for any of the five companies and that source gap did not cause the current model-spec outcomes.

No fresh forecast was eligible. The latest persisted specs for IBM, CTSH, EPAM, ACN and DXC still use old task hash `ce8320a933010843ca8171324630e4de4f2d0105b65c95325a8c3d9b5fef99e7`; their latest forecast versions were created between `2026-09-11T15:30:30Z` and `15:51:15Z`. The forecast lane therefore correctly retained versions 4, 3, 3, 4 and 5 rather than consuming any refused or unavailable spec.

This snapshot shows no stale launch identity, prompt-size refusal, route/profile mismatch, provider failure, unsafe partial persistence or forecast-consumer bug. Four companies need a new valid model response under a future authorized lane attempt. DXC first needs budget re-entry; its filed diluted-EPS/weighted-share gap remains a separate prerequisite for eventual note-backed annual EPS readiness.

The private owner receipt is `foundation-r16a-release/financial-note-live-input-audit/company-model-post-r16a-disposition-audit.json`.
