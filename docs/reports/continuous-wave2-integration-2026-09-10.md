# Continuous wave 2 integration acceptance

Code baseline: `e7e06bb` (2026-09-10). This is a validated development checkpoint, not the end of the continuous-development request and not a live deployment.

Delivered: F14's 13 coordinators plus scoped permission/input recovery, replay without writes and superseded-entry retirement; zero-base real child company selection and durable per-company semantic refusals; governed HK daily acquisition/cache and closed-week incremental judgements; explicit market-proxy evidence production and model-spec consumption with legacy actual exclusion; safe stop/drain-before-runtime-upgrade ordering and HK workbook dependency.

Validation:

- Full main discovery: **6,114 tests / 495.678s / OK (skipped=1)**. Log: `/tmp/dalton-wave2-full-tests.log`.
- Current live read-only SQLite backup and deployment rehearsal: **all 12 steps passed**, 23 copied inputs / 830 MB, **67/67 schemas**, 35 registered lanes, 38 tick entries, **0 escaped**. Report: `/tmp/dalton-wave2-rehearsal-report.md`.
- Wheel built successfully; all **428 packaged Python/SQL files** exactly match the frozen source. SHA-256: `a777601fec200f8084cae865911ae6b11e2917f9108af175326b6d7994183b71`.
- Rehearsal retains real activation gaps: 11 missing may_write grants and 3 checkpoints; absent switches and unapproved connector proposals remain inactive. No production writer operation, signing, installation or deployment was performed.

Next batch is already in parallel review: W5 cost-driver templates (`beb5334`), SEC Item 5 trading arrangements (`3f58ccf`), dossier/earnings model-pair installation (`c33e6ab` + repairs), and HK coordinator failure-ledger coverage (`4bb9969` + review). These commits are not included in this 6,114-test result. Required repairs finish before their own integration run. The owner activation packet is prepared locally, awaiting a final pinned deployment version and validated execution order.
