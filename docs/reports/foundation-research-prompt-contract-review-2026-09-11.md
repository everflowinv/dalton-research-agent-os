# Foundation research prompt contract review — 2026-09-11

Scope: Initial Screen, Company Dossier, Industry Framework, Deep Insight Gate, Company Model Spec and Investment Memo producer/verifier prompts. Ask, Debate, Event and Document Extraction were excluded.

| Call path | Frozen context actually available | Prompt/verifier correction | Identity/retry binding | Remaining honest gap |
|---|---|---|---|---|
| `initial_screen_cli` → `initial_screen.build_section_prompt` | tagged Claim and number rows, mission questions, checklist and prior screen | permits qualified inference from cited premises; requires a current case, alternative trigger, impact/falsifier/next observation; source labels distinguish filings, management, consensus and house forecasts | call request already hashes the full prompt | valuation block still receives no complete valuation projection, so it remains explicitly unavailable; this is no longer described as a system-wide absence |
| `company_dossier_cli` → `company_dossier_draft` → independent verifier | closed unit structure, tagged claims/numbers/profile table/classification/prior unit | requires operational gaps and evidence-weighted judgement without turning the dossier into a recommendation; verifier checks facts and qualified inference against cited premises | draft contract `0.4`; verifier prompt contract `0.3`; full prompt hash remains in WorkOrder | existing schema has no separate structured scenario/tracking object; these remain cited prose inside the relevant slots |
| `industry_framework_cli` → `industry_framework_draft` → independent verifier | causal-chain slots, tagged evidence, computed comparison table, prior unit | preserves business-model/moat work; driver stance must choose a case and name trigger, impact, falsifier and observation; verifier checks comparisons/inference | producer already hashes prompt; verifier request now includes `industry-framework-verifier-prompt:0.2` fingerprint | no new catalyst-calendar registration is claimed |
| `deep_insight_gate_cli` → `deep_insight_gate_draft` → independent verifier | exact 12 questions, tagged dossier/debate/numeric material, prior answers | answered judgements must choose; unknown remains valid when evidence is absent; evidence-needed action must name company/metric/period/source/action | producer hashes prompt; verifier request now includes `deep-insight-gate-verifier-prompt:0.2` fingerprint | no automatic human gate decision or investment recommendation |
| `company_model_cli` → `company_model_spec.build_prompt` | filed statement structure, classification-selected driver registries and labelled market proxies | accurately says the model cannot browse; requires company-specific inference, current base case, alternative trigger, model-line impact and falsifier | `TASK_HASH` now binds `company-model-spec-prompt:0.3` | no document prose or unprovided actual can be used; proxies remain proxies |
| `investment_memo_cli` → `investment_memo_draft` → independent verifier | complete frozen evidence rows, requested sections and 12 Playbook questions | requires preferred case and falsifier while forbidding invented buy-side consensus and return arithmetic; verifier rejects evasive unranked alternatives | producer identity binds `investment-memo-prompt:0.2`; verifier request binds `investment-memo-verifier-prompt:0.2` | system still lacks a frozen holding-period return bridge and buy-side consensus authority; human approval remains mandatory |

The changes do not alter signed Playbook questions, stage gates, vocabularies, output schemas, evidence caps, model budgets or human approval. They improve analytical instructions within existing fields. Structured scenario/return attribution should be added only through separately reviewed versioned authorities rather than encoded as an ungoverned prompt-only object.
